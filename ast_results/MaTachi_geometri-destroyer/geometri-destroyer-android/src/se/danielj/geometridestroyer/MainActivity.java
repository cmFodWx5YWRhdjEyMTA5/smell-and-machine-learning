// isComment
package se.danielj.geometridestroyer;

import android.os.Bundle;
import com.badlogic.gdx.backends.android.AndroidApplication;
import com.badlogic.gdx.backends.android.AndroidApplicationConfiguration;

/**
 * isComment
 */
public class isClassOrIsInterface extends AndroidApplication {

    @Override
    public void isMethod(Bundle isParameter) {
        super.isMethod(isNameExpr);
        AndroidApplicationConfiguration isVariable = new AndroidApplicationConfiguration();
        isNameExpr.isFieldAccessExpr = true;
        isMethod(new Core(), isNameExpr);
    }
}

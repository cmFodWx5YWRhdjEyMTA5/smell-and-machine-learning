// isComment
package org.jamienicol.episodes.services;

import android.app.IntentService;
import android.content.Intent;
import org.jamienicol.episodes.RefreshShowUtil;

public class isClassOrIsInterface extends IntentService {

    public isConstructor() {
        super(RefreshShowService.class.isMethod());
    }

    @Override
    protected void isMethod(Intent isParameter) {
        final int isVariable = isNameExpr.isMethod("isStringConstant", isIntegerConstant);
        isNameExpr.isMethod(isNameExpr, isMethod());
    }
}

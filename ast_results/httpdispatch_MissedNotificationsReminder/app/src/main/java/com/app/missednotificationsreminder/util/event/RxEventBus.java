// isComment
package com.app.missednotificationsreminder.util.event;

import rx.Observable;
import rx.subjects.PublishSubject;
import rx.subjects.SerializedSubject;
import rx.subjects.Subject;

/**
 * isComment
 */
public class isClassOrIsInterface {

    // isComment
    // isComment
    private final Subject<Event, Event> isVariable = new SerializedSubject<>(isNameExpr.isMethod());

    /**
     * isComment
     */
    public void isMethod(Event isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    public Observable<Event> isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public boolean isMethod() {
        return isNameExpr.isMethod();
    }
}

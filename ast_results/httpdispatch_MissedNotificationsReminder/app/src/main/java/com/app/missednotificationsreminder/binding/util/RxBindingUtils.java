// isComment
package com.app.missednotificationsreminder.binding.util;

import android.support.annotation.CheckResult;
import android.support.annotation.NonNull;
import com.f2prateek.rx.preferences.Preference;
import rx.Observable;
import rx.Subscription;
import rx.subscriptions.CompositeSubscription;

/**
 * isComment
 */
public class isClassOrIsInterface {

    /**
     * isComment
     */
    @CheckResult
    @NonNull
    public static <T> Observable<T> isMethod(@NonNull BindableObject<T> isParameter) {
        return isNameExpr.isMethod(new BindableObjectValueChangedOnSubscribe<T>(isNameExpr));
    }

    /**
     * isComment
     */
    @NonNull
    public static <T> Subscription isMethod(@NonNull BindableObject<T> isParameter, @NonNull Preference<T> isParameter) {
        CompositeSubscription isVariable = new CompositeSubscription();
        // isComment
        isNameExpr.isMethod(isNameExpr.isMethod());
        // isComment
        // isComment
        isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr).isMethod(// isComment
        isIntegerConstant).isMethod(isNameExpr.isMethod()));
        // isComment
        isNameExpr.isMethod(isNameExpr.isMethod().isMethod(isNameExpr.isMethod()));
        return isNameExpr;
    }
}

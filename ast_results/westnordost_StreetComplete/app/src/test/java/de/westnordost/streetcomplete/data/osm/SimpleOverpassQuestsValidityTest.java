// isComment
package de.westnordost.streetcomplete.data.osm;

import org.junit.Test;
import java.util.List;
import de.westnordost.streetcomplete.data.QuestType;
import de.westnordost.streetcomplete.data.osmnotes.OsmNoteQuestType;
import de.westnordost.streetcomplete.quests.QuestModule;
import static org.junit.Assert.*;

public class isClassOrIsInterface {

    @Test
    public void isMethod() {
        List<QuestType> isVariable = isNameExpr.isMethod(new OsmNoteQuestType(), null, null, null, null, null).isMethod();
        for (QuestType isVariable : isNameExpr) {
            if (isNameExpr instanceof SimpleOverpassQuestType) {
                // isComment
                // isComment
                ((SimpleOverpassQuestType) isNameExpr).isMethod(null);
            }
        }
        // isComment
        isMethod(true);
    }
}

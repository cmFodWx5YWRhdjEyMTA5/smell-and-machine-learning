// isComment
package com.haringeymobile.ukweather.test.database;

import java.util.concurrent.CountDownLatch;
import android.content.Intent;
import com.haringeymobile.ukweather.database.GeneralDatabaseService;

/**
 * isComment
 */
public class isClassOrIsInterface extends GeneralDatabaseService {

    private CountDownLatch isVariable;

    @Override
    protected void isMethod(Intent isParameter) {
        super.isMethod(isNameExpr);
        isNameExpr.isMethod();
    }

    public void isMethod(CountDownLatch isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }
}

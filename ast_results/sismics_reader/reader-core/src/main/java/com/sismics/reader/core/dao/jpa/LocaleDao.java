// isComment
package com.sismics.reader.core.dao.jpa;

import java.util.List;
import javax.persistence.EntityManager;
import javax.persistence.NoResultException;
import javax.persistence.Query;
import com.sismics.reader.core.model.jpa.Locale;
import com.sismics.util.context.ThreadLocalContext;

/**
 * isComment
 */
public class isClassOrIsInterface {

    /**
     * isComment
     */
    public Locale isMethod(String isParameter) {
        EntityManager isVariable = isNameExpr.isMethod().isMethod();
        try {
            return isNameExpr.isMethod(Locale.class, isNameExpr);
        } catch (NoResultException isParameter) {
            return null;
        }
    }

    /**
     * isComment
     */
    @SuppressWarnings("isStringConstant")
    public List<Locale> isMethod() {
        EntityManager isVariable = isNameExpr.isMethod().isMethod();
        Query isVariable = isNameExpr.isMethod("isStringConstant");
        return isNameExpr.isMethod();
    }
}

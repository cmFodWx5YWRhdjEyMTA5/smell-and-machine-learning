// isComment
package net.programmierecke.radiodroid2.players;

import android.content.Context;
import android.support.annotation.NonNull;
import net.programmierecke.radiodroid2.data.ShoutcastInfo;
import net.programmierecke.radiodroid2.data.StreamLiveInfo;
import net.programmierecke.radiodroid2.recording.Recordable;
import okhttp3.OkHttpClient;

public interface isClassOrIsInterface extends Recordable {

    interface isClassOrIsInterface {

        void isMethod(RadioPlayer.PlayState isParameter);

        void isMethod(final int isParameter);

        void isMethod(ShoutcastInfo isParameter, boolean isParameter);

        void isMethod(StreamLiveInfo isParameter);
    }

    void isMethod(@NonNull OkHttpClient isParameter, @NonNull String isParameter, @NonNull Context isParameter, boolean isParameter);

    void isMethod();

    void isMethod();

    boolean isMethod();

    long isMethod();

    int isMethod();

    long isMethod();

    long isMethod();

    void isMethod(float isParameter);

    void isMethod(PlayListener isParameter);
}

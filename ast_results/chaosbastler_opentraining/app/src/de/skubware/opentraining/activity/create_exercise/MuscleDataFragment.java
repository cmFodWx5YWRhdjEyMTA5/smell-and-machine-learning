// isComment
package de.skubware.opentraining.activity.create_exercise;

import de.skubware.opentraining.R;
import de.skubware.opentraining.basic.Muscle;
import de.skubware.opentraining.db.Cache;
import de.skubware.opentraining.db.DataProvider;
import android.content.Context;
import android.widget.AdapterView.OnItemSelectedListener;

public class isClassOrIsInterface extends SpinnerDataFragment<Muscle> {

    public isConstructor() {
        super(isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
        isNameExpr = (new DataProvider(isMethod())).isMethod();
    }
}
/*isComment*/

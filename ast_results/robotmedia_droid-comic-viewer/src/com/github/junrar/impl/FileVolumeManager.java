// isComment
package com.github.junrar.impl;

import java.io.File;
import java.io.IOException;
import com.github.junrar.Archive;
import com.github.junrar.Volume;
import com.github.junrar.VolumeManager;
import com.github.junrar.util.VolumeHelper;

/**
 * isComment
 */
public class isClassOrIsInterface implements VolumeManager {

    private final File isVariable;

    public isConstructor(File isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public Volume isMethod(Archive isParameter, Volume isParameter) throws IOException {
        if (isNameExpr == null)
            return new FileVolume(isNameExpr, isNameExpr);
        FileVolume isVariable = (FileVolume) isNameExpr;
        boolean isVariable = !isNameExpr.isMethod().isMethod() || isNameExpr.isMethod();
        String isVariable = isNameExpr.isMethod(isNameExpr.isMethod().isMethod(), isNameExpr);
        File isVariable = new File(isNameExpr);
        return new FileVolume(isNameExpr, isNameExpr);
    }
}

// isComment
package net.robotmedia.acv.ui.settings.tablet;

import net.androidcomics.acv.R;
import net.robotmedia.acv.billing.BillingManager;
import net.robotmedia.acv.billing.BillingManager.IObserver;
import net.robotmedia.acv.ui.settings.PremiumSettingsHelper;
import android.app.Activity;
import android.os.Bundle;
import android.preference.Preference;
import android.preference.PreferenceActivity;

public class isClassOrIsInterface extends ExtendedPreferenceFragment implements IObserver {

    private PremiumSettingsHelper isVariable;

    @Override
    public void isMethod(Bundle isParameter) {
        super.isMethod(isNameExpr);
        isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
        this.isFieldAccessExpr = new PremiumSettingsHelper(this.isMethod());
        isNameExpr.isMethod(isMethod()).isMethod(this);
        Preference isVariable = isMethod(isNameExpr.isFieldAccessExpr);
        isNameExpr.isMethod(isNameExpr);
    }

    @Override
    public void isMethod() {
        isNameExpr.isMethod(isMethod()).isMethod(null);
        super.isMethod();
    }

    @Override
    public void isMethod() {
        if (!(this.isMethod() instanceof PreferenceActivity))
            return;
        PreferenceActivity isVariable = (PreferenceActivity) this.isMethod();
        // isComment
        // isComment
        // isComment
        // isComment
        isNameExpr.isMethod();
    }

    @Override
    public Activity isMethod() {
        return isMethod();
    }
}

// isComment
package com.samebits.beacon.locator.viewModel;

import android.support.annotation.NonNull;
import com.samebits.beacon.locator.model.IManagedBeacon;
import com.samebits.beacon.locator.model.TrackedBeacon;
import com.samebits.beacon.locator.ui.fragment.BeaconFragment;

/**
 * isComment
 */
public class isClassOrIsInterface extends BeaconViewModel {

    public isConstructor(@NonNull BeaconFragment isParameter, @NonNull IManagedBeacon isParameter) {
        super(isNameExpr, isNameExpr);
    }

    protected void isMethod() {
        isNameExpr.isMethod(new TrackedBeacon(isNameExpr));
    }
}

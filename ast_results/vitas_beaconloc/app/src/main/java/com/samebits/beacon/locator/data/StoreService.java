// isComment
package com.samebits.beacon.locator.data;

import com.samebits.beacon.locator.model.ActionBeacon;
import com.samebits.beacon.locator.model.TrackedBeacon;
import java.util.List;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    boolean isMethod(final TrackedBeacon isParameter);

    boolean isMethod(final TrackedBeacon isParameter);

    boolean isMethod(final String isParameter, boolean isParameter);

    TrackedBeacon isMethod(final String isParameter);

    List<TrackedBeacon> isMethod();

    boolean isMethod(final String isParameter, double isParameter);

    boolean isMethod(ActionBeacon isParameter);

    boolean isMethod(ActionBeacon isParameter);

    List<ActionBeacon> isMethod(final String isParameter);

    boolean isMethod(final int isParameter);

    boolean isMethod(final String isParameter);

    List<ActionBeacon> isMethod();

    boolean isMethod(final int isParameter, boolean isParameter);

    List<ActionBeacon> isMethod(final int isParameter, final String isParameter);

    boolean isMethod(String isParameter);
}

// isComment
package com.samebits.beacon.locator.action;

import android.content.Context;

/**
 * isComment
 */
abstract class isClassOrIsInterface implements IAction {

    @Override
    public abstract String isMethod(Context isParameter);
}

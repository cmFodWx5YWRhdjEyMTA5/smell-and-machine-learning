// isComment
package org.andstatus.app.syncadapter;

import android.accounts.Account;
import android.content.AbstractThreadedSyncAdapter;
import android.content.ContentProviderClient;
import android.content.Context;
import android.content.SyncResult;
import android.os.Bundle;
import org.andstatus.app.context.MyContext;
import org.andstatus.app.context.MyContextHolder;
import org.andstatus.app.service.MyServiceCommandsRunner;
import org.andstatus.app.service.MyServiceManager;
import org.andstatus.app.util.MyLog;

public class isClassOrIsInterface extends AbstractThreadedSyncAdapter {

    private final Context isVariable;

    public isConstructor(Context isParameter, boolean isParameter) {
        super(isNameExpr, isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        isNameExpr.isMethod(this, () -> "isStringConstant" + isNameExpr.isMethod().isMethod());
    }

    @Override
    public void isMethod(Account isParameter, Bundle isParameter, String isParameter, ContentProviderClient isParameter, SyncResult isParameter) {
        if (!isNameExpr.isMethod()) {
            isNameExpr.isMethod(this, isNameExpr.isFieldAccessExpr + "isStringConstant");
            return;
        }
        final MyContext isVariable = isNameExpr.isMethod(isNameExpr, this);
        new MyServiceCommandsRunner(isNameExpr).isMethod(isNameExpr.isMethod().isMethod(isNameExpr.isFieldAccessExpr), isNameExpr);
    }
}

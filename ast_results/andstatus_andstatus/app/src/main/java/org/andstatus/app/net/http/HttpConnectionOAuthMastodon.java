// isComment
package org.andstatus.app.net.http;

import com.github.scribejava.core.model.OAuthConstants;
import com.github.scribejava.core.oauth.OAuth20Service;
import org.andstatus.app.net.social.Connection;
import org.andstatus.app.util.StringUtils;
import java.util.HashMap;
import java.util.Map;

public class isClassOrIsInterface extends HttpConnectionOAuth2JavaNet {

    @Override
    public String isMethod(Connection.ApiRoutineEnum isParameter) throws ConnectionException {
        String isVariable;
        switch(isNameExpr) {
            case isNameExpr:
            case isNameExpr:
                isNameExpr = isNameExpr.isMethod() + "isStringConstant";
                break;
            case isNameExpr:
                isNameExpr = isNameExpr.isMethod() + "isStringConstant";
                break;
            default:
                isNameExpr = super.isMethod(isNameExpr);
                break;
        }
        if (!isNameExpr.isMethod(isNameExpr)) {
            isNameExpr = isMethod(isNameExpr);
        }
        return isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public Map<String, String> isMethod() {
        Map<String, String> isVariable = new HashMap<>();
        isNameExpr.isMethod(isNameExpr.isFieldAccessExpr, isNameExpr);
        return isNameExpr;
    }
}

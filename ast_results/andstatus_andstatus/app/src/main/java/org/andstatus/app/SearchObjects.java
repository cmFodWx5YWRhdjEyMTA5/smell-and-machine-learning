// isComment
package org.andstatus.app;

import android.widget.Spinner;
import org.andstatus.app.timeline.TimelineActivity;
import org.andstatus.app.actor.ActorList;

public enum SearchObjects {

    NOTES(TimelineActivity.class), ACTORS(ActorList.class);

    private final Class<?> isVariable;

    isConstructor(Class<?> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public Class<?> isMethod() {
        return isNameExpr;
    }

    public static SearchObjects isMethod(Spinner isParameter) {
        SearchObjects isVariable = isNameExpr;
        if (isNameExpr != null) {
            for (SearchObjects isVariable : isMethod()) {
                if (isNameExpr.isMethod() == isNameExpr.isMethod()) {
                    isNameExpr = isNameExpr;
                    break;
                }
            }
        }
        return isNameExpr;
    }
}

// isComment
package net.bible.android.control.page.window;

import net.bible.android.control.page.CurrentPageManager;
import net.bible.android.control.page.window.WindowLayout.WindowState;
import org.crosswire.jsword.book.Book;

/**
 * isComment
 */
public class isClassOrIsInterface extends Window {

    // isComment
    protected static final int isVariable = -isIntegerConstant;

    public isConstructor(WindowState isParameter, CurrentPageManager isParameter) {
        super(isNameExpr, isNameExpr, isNameExpr);
        isMethod(true);
        isNameExpr.isMethod(this);
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    /**
     * isComment
     */
    protected void isMethod(Window isParameter) {
        // isComment
        if (isMethod().isMethod().isMethod(isNameExpr.isFieldAccessExpr) && !isNameExpr.isMethod()) {
            // isComment
            isMethod().isMethod(isNameExpr.isMethod().isMethod());
        }
    }
}

// isComment
package net.bible.service.download;

import java.util.List;
import org.crosswire.jsword.book.Book;
import org.crosswire.jsword.book.BookException;
import org.crosswire.jsword.book.BookFilter;
import org.crosswire.jsword.book.install.InstallException;
import org.crosswire.jsword.book.sword.SwordBook;
import org.crosswire.jsword.book.sword.SwordBookMetaData;

/**
 * isComment
 */
public abstract class isClassOrIsInterface {

    public abstract List<Book> isMethod(boolean isParameter) throws InstallException;

    public abstract String isMethod();

    /**
     * isComment
     */
    public List<Book> isMethod(BookFilter isParameter, boolean isParameter) throws InstallException {
        DownloadManager isVariable = new DownloadManager();
        List<Book> isVariable = isNameExpr.isMethod(isNameExpr, isMethod(), isNameExpr);
        return isNameExpr;
    }

    public void isMethod(List<Book> isParameter) {
        for (Book isVariable : isNameExpr) {
            // isComment
            if (isNameExpr instanceof SwordBook) {
                ((SwordBookMetaData) isNameExpr.isMethod()).isMethod(isNameExpr.isFieldAccessExpr, isMethod());
            } else {
                isNameExpr.isMethod().isMethod(isNameExpr.isFieldAccessExpr, isMethod());
            }
        }
    }

    public void isMethod(Book isParameter) throws InstallException, BookException {
        DownloadManager isVariable = new DownloadManager();
        isNameExpr.isMethod(isMethod(), isNameExpr);
    }
}

// isComment
package net.bible.service.history;

import net.bible.android.control.ApplicationScope;
import javax.inject.Inject;

/**
 * isComment
 */
@ApplicationScope
public class isClassOrIsInterface {

    private final HistoryManager isVariable;

    @Inject
    public isConstructor(HistoryManager isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public HistoryTraversal isMethod(boolean isParameter) {
        return new HistoryTraversal(isNameExpr, isNameExpr);
    }
}

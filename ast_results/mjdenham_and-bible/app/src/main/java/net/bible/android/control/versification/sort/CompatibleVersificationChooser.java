// isComment
package net.bible.android.control.versification.sort;

import net.bible.android.control.versification.ConvertibleVerseRange;
import net.bible.service.common.Logger;
import org.crosswire.jsword.versification.Versification;
import org.crosswire.jsword.versification.system.Versifications;
import java.util.List;

class isClassOrIsInterface {

    private final List<Versification> isVariable;

    private final Logger isVariable = new Logger(this.isMethod().isMethod());

    isConstructor(List<Versification> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    Versification isMethod(ConvertibleVerseRange isParameter, ConvertibleVerseRange isParameter) {
        for (Versification isVariable : isNameExpr) {
            if (isNameExpr.isMethod(isNameExpr) && isNameExpr.isMethod(isNameExpr)) {
                return isNameExpr;
            }
        }
        isNameExpr.isMethod("isStringConstant" + isNameExpr + "isStringConstant" + isNameExpr + "isStringConstant");
        return isNameExpr.isMethod().isMethod("isStringConstant");
    }
}

// isComment
package net.bible.android.control.page;

import net.bible.android.control.ApplicationScope;
import net.bible.android.control.page.window.Window;
import java.util.HashMap;
import java.util.Map;
import javax.inject.Inject;

/**
 * isComment
 */
@ApplicationScope
public class isClassOrIsInterface {

    private final Map<Window, PageTiltScrollControl> isVariable = new HashMap<>();

    @Inject
    public isConstructor() {
    }

    public PageTiltScrollControl isMethod(Window isParameter) {
        PageTiltScrollControl isVariable = isNameExpr.isMethod(isNameExpr);
        if (isNameExpr == null) {
            synchronized (isNameExpr) {
                isNameExpr = isNameExpr.isMethod(isNameExpr);
                if (isNameExpr == null) {
                    isNameExpr = new PageTiltScrollControl();
                    isNameExpr.isMethod(isNameExpr, isNameExpr);
                }
            }
        }
        return isNameExpr;
    }
}

// isComment
package net.bible.android.view.activity.readingplan.actionbar;

import net.bible.android.control.ApplicationScope;
import net.bible.android.control.speak.SpeakControl;
import net.bible.android.view.activity.speak.actionbarbuttons.SpeakStopActionBarButton;
import javax.inject.Inject;

/**
 * isComment
 */
@ApplicationScope
public class isClassOrIsInterface extends SpeakStopActionBarButton {

    @Inject
    public isConstructor(SpeakControl isParameter) {
        super(isNameExpr);
    }

    /**
     * isComment
     */
    @Override
    public boolean isMethod() {
        return super.isMethod() && isMethod();
    }
}

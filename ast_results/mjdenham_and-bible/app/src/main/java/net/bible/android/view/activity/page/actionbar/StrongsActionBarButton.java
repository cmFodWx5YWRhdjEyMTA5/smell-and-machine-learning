// isComment
package net.bible.android.view.activity.page.actionbar;

import androidx.core.view.MenuItemCompat;
import android.view.MenuItem;
import net.bible.android.activity.R;
import net.bible.android.control.ApplicationScope;
import net.bible.android.control.PassageChangeMediator;
import net.bible.android.control.document.DocumentControl;
import net.bible.android.control.page.PageControl;
import net.bible.android.view.activity.base.actionbar.QuickActionButton;
import net.bible.service.common.CommonUtils;
import javax.inject.Inject;

/**
 * isComment
 */
@ApplicationScope
public class isClassOrIsInterface extends QuickActionButton {

    private final DocumentControl isVariable;

    private final PageControl isVariable;

    @Inject
    public isConstructor(DocumentControl isParameter, PageControl isParameter) {
        // isComment
        super(isNameExpr.isFieldAccessExpr | isNameExpr.isFieldAccessExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public boolean isMethod(MenuItem isParameter) {
        // isComment
        isNameExpr.isMethod().isMethod().isMethod("isStringConstant", !isMethod()).isMethod();
        // isComment
        isNameExpr.isMethod().isMethod();
        return true;
    }

    private boolean isMethod() {
        return isNameExpr.isMethod();
    }

    @Override
    protected String isMethod() {
        return isNameExpr.isMethod(isMethod() ? isNameExpr.isFieldAccessExpr.isFieldAccessExpr : isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
    }

    /**
     * isComment
     */
    @Override
    protected boolean isMethod() {
        return isNameExpr.isMethod() && (isMethod() || !isMethod());
    }
}

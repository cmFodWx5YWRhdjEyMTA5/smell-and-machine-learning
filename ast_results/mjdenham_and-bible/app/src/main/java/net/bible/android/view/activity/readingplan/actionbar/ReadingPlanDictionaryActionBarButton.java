// isComment
package net.bible.android.view.activity.readingplan.actionbar;

import net.bible.android.control.ApplicationScope;
import net.bible.android.control.page.window.ActiveWindowPageManagerProvider;
import org.crosswire.jsword.book.Book;
import javax.inject.Inject;

/**
 * isComment
 */
@ApplicationScope
public class isClassOrIsInterface extends ReadingPlanQuickDocumentChangeButton {

    @Inject
    public isConstructor(ActiveWindowPageManagerProvider isParameter) {
    }

    @Override
    protected Book isMethod() {
        return isMethod().isMethod().isMethod();
    }

    /**
     * isComment
     */
    @Override
    protected boolean isMethod() {
        return super.isMethod() && isMethod();
    }
}

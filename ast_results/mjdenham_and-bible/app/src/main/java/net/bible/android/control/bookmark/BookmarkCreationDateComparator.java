// isComment
package net.bible.android.control.bookmark;

import androidx.annotation.NonNull;
import net.bible.service.db.bookmark.BookmarkDto;
import java.util.Comparator;

public class isClassOrIsInterface implements Comparator<BookmarkDto> {

    public int isMethod(@NonNull BookmarkDto isParameter, @NonNull BookmarkDto isParameter) {
        // isComment
        return isNameExpr.isMethod().isMethod(isNameExpr.isMethod());
    }
}

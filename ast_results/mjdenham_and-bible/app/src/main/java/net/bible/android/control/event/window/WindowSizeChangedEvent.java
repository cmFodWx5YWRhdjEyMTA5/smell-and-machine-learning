// isComment
package net.bible.android.control.event.window;

import java.util.Map;
import net.bible.android.control.page.ChapterVerse;
import net.bible.android.control.page.window.Window;

/**
 * isComment
 */
public class isClassOrIsInterface implements WindowEvent {

    private boolean isVariable;

    private Map<Window, ChapterVerse> isVariable;

    public isConstructor(boolean isParameter, Map<Window, ChapterVerse> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    public boolean isMethod() {
        return isNameExpr;
    }

    public boolean isMethod(Window isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }

    public ChapterVerse isMethod(Window isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

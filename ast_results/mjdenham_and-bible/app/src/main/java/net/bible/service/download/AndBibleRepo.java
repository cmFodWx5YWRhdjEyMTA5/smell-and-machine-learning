// isComment
package net.bible.service.download;

import net.bible.service.common.Logger;
import net.bible.service.sword.AcceptableBookTypeFilter;
import org.crosswire.jsword.book.Book;
import org.crosswire.jsword.book.BookException;
import org.crosswire.jsword.book.BookFilter;
import org.crosswire.jsword.book.install.InstallException;
import java.util.List;

/**
 * isComment
 */
public class isClassOrIsInterface extends RepoBase {

    private static final String isVariable = "isStringConstant";

    private static BookFilter isVariable = new AcceptableBookTypeFilter();

    private Logger isVariable = new Logger(this.isMethod().isMethod());

    /**
     * isComment
     */
    public List<Book> isMethod(boolean isParameter) throws InstallException {
        List<Book> isVariable = isMethod(isNameExpr, isNameExpr);
        isMethod(isNameExpr);
        return isNameExpr;
    }

    /**
     * isComment
     */
    public void isMethod(Book isParameter) throws InstallException, BookException {
        DownloadManager isVariable = new DownloadManager();
        isNameExpr.isMethod(isMethod(), isNameExpr);
    }

    @Override
    public String isMethod() {
        return isNameExpr;
    }
}

// isComment
package net.bible.android.view.activity.navigation.biblebookactionbar;

import androidx.core.view.MenuItemCompat;
import net.bible.android.control.ApplicationScope;
import net.bible.android.control.navigation.NavigationControl;
import net.bible.android.view.activity.base.actionbar.QuickActionButton;
import javax.inject.Inject;

/**
 * isComment
 */
@ApplicationScope
public class isClassOrIsInterface extends QuickActionButton {

    private final NavigationControl isVariable;

    @Inject
    public isConstructor(NavigationControl isParameter) {
        // isComment
        super(isNameExpr.isFieldAccessExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected int isMethod() {
        return isNameExpr.isFieldAccessExpr.isFieldAccessExpr.isFieldAccessExpr;
    }

    @Override
    protected String isMethod() {
        return isNameExpr.isMethod();
    }

    /**
     * isComment
     */
    @Override
    protected boolean isMethod() {
        return true;
    }
}

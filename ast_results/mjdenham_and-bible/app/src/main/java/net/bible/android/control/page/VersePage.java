// isComment
package net.bible.android.control.page;

import android.util.Log;
import net.bible.android.control.PassageChangeMediator;
import net.bible.android.control.versification.BibleTraverser;
import net.bible.service.sword.SwordContentFacade;
import net.bible.service.sword.SwordDocumentFacade;
import org.crosswire.jsword.book.Book;
import org.crosswire.jsword.book.basic.AbstractPassageBook;
import org.crosswire.jsword.passage.Verse;
import org.crosswire.jsword.versification.Versification;
import org.crosswire.jsword.versification.system.Versifications;

/**
 * isComment
 */
public abstract class isClassOrIsInterface extends CurrentPageBase {

    private CurrentBibleVerse isVariable;

    private BibleTraverser isVariable;

    private static final String isVariable = "isStringConstant";

    protected isConstructor(boolean isParameter, CurrentBibleVerse isParameter, BibleTraverser isParameter, SwordContentFacade isParameter, SwordDocumentFacade isParameter) {
        super(isNameExpr, isNameExpr, isNameExpr);
        // isComment
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    public Versification isMethod() {
        try {
            // isComment
            return ((AbstractPassageBook) isMethod()).isMethod();
        } catch (Exception isParameter) {
            isNameExpr.isMethod(isNameExpr, "isStringConstant", isNameExpr);
            return isNameExpr.isMethod().isMethod("isStringConstant");
        }
    }

    public AbstractPassageBook isMethod() {
        return (AbstractPassageBook) isMethod();
    }

    protected CurrentBibleVerse isMethod() {
        return isNameExpr;
    }

    protected BibleTraverser isMethod() {
        return isNameExpr;
    }

    @Override
    protected void isMethod(Book isParameter) {
        // isComment
        Versification isVariable = ((AbstractPassageBook) isMethod()).isMethod();
        Verse isVariable = isMethod().isMethod(isNameExpr);
        super.isMethod(isNameExpr);
        isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    protected void isMethod() {
        if (!isMethod()) {
            isNameExpr.isMethod().isMethod();
        }
    }
}

// isComment
package net.bible.service.download;

import org.apache.commons.lang3.StringUtils;
import org.crosswire.jsword.book.BookDriver;
import org.crosswire.jsword.book.BookException;
import org.crosswire.jsword.book.sword.NullBackend;
import org.crosswire.jsword.book.sword.SwordBook;
import org.crosswire.jsword.book.sword.SwordBookDriver;
import org.crosswire.jsword.book.sword.SwordBookMetaData;
import java.io.IOException;

/**
 * isComment
 */
public class isClassOrIsInterface {

    /**
     * isComment
     */
    public static SwordBook isMethod(String isParameter, String isParameter, String isParameter) throws IOException, BookException {
        SwordBookMetaData isVariable = isMethod(isNameExpr, isNameExpr);
        if (isNameExpr.isMethod(isNameExpr)) {
            isNameExpr.isMethod(isNameExpr.isFieldAccessExpr, isNameExpr);
        }
        SwordBook isVariable = new SwordBook(isNameExpr, new NullBackend());
        return isNameExpr;
    }

    /**
     * isComment
     */
    public static SwordBookMetaData isMethod(String isParameter, String isParameter) throws IOException, BookException {
        SwordBookMetaData isVariable = new SwordBookMetaData(isNameExpr.isMethod(), isNameExpr);
        BookDriver isVariable = isNameExpr.isMethod();
        isNameExpr.isMethod(isNameExpr);
        return isNameExpr;
    }
}

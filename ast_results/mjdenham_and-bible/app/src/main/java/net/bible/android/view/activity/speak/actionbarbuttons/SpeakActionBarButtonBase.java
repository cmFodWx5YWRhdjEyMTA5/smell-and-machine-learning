// isComment
package net.bible.android.view.activity.speak.actionbarbuttons;

import androidx.core.view.MenuItemCompat;
import net.bible.android.control.speak.SpeakControl;
import net.bible.android.view.activity.base.actionbar.QuickActionButton;

/**
 * isComment
 */
public abstract class isClassOrIsInterface extends QuickActionButton {

    private SpeakControl isVariable;

    protected static final int isVariable = isIntegerConstant;

    public isConstructor(SpeakControl isParameter) {
        // isComment
        super(isNameExpr.isFieldAccessExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    public boolean isMethod() {
        boolean isVariable = isNameExpr.isMethod();
        return // isComment
        isNameExpr;
    }

    protected SpeakControl isMethod() {
        return isNameExpr;
    }
}

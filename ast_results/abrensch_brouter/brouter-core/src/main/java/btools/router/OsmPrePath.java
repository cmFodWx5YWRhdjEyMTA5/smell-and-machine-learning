// isComment
package btools.router;

import btools.mapaccess.OsmLink;
import btools.mapaccess.OsmNode;
import btools.mapaccess.OsmTransferNode;

public abstract class isClassOrIsInterface {

    protected OsmNode isVariable;

    protected OsmNode isVariable;

    protected OsmLink isVariable;

    public OsmPrePath isVariable;

    public void isMethod(OsmPath isParameter, OsmLink isParameter, RoutingContext isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr.isMethod();
        this.isFieldAccessExpr = isNameExpr.isMethod(isNameExpr);
        isMethod(isNameExpr, isNameExpr);
    }

    protected abstract void isMethod(OsmPath isParameter, RoutingContext isParameter);
}

// isComment
package com.mde.potdroid.services;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import com.mde.potdroid.helpers.SettingsWrapper;

/**
 * isComment
 */
public class isClassOrIsInterface extends BroadcastReceiver {

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        SettingsWrapper isVariable = new SettingsWrapper(isNameExpr);
        // isComment
        if (isNameExpr.isMethod().isMethod(isIntegerConstant))
            return;
        Intent isVariable = new Intent(isNameExpr, MessagePollingService.class);
        isNameExpr.isMethod(isNameExpr);
    }
}

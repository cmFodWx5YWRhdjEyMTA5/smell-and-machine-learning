// isComment
package fr.simon.marquis.secretcodes;

import android.content.Context;
import android.content.pm.ActivityInfo;
import android.content.pm.PackageManager;
import android.content.res.Configuration;
import android.content.res.Resources;
import android.support.v4.content.AsyncTaskLoader;
import java.text.Collator;
import java.util.Collections;
import java.util.Comparator;
import java.util.List;

class isClassOrIsInterface extends AsyncTaskLoader<List<SecretCode>> {

    private final InterestingConfigChanges isVariable = new InterestingConfigChanges();

    final PackageManager isVariable;

    private List<SecretCode> isVariable;

    private PackageIntentReceiver isVariable;

    public isConstructor(Context isParameter) {
        super(isNameExpr);
        isNameExpr = isMethod().isMethod();
    }

    @Override
    public List<SecretCode> isMethod() {
        List<SecretCode> isVariable = isNameExpr.isMethod(this);
        isNameExpr.isMethod(isNameExpr, isNameExpr);
        return isNameExpr;
    }

    @Override
    public void isMethod(List<SecretCode> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        if (isMethod()) {
            // isComment
            super.isMethod(isNameExpr);
        }
    }

    /**
     * isComment
     */
    @Override
    protected void isMethod() {
        if (isNameExpr != null) {
            // isComment
            isMethod(isNameExpr);
        }
        // isComment
        if (isNameExpr == null) {
            isNameExpr = new PackageIntentReceiver(this);
        }
        // isComment
        boolean isVariable = isNameExpr.isMethod(isMethod().isMethod());
        if (isMethod() || isNameExpr == null || isNameExpr) {
            // isComment
            isMethod();
        }
    }

    @Override
    protected void isMethod() {
        isMethod();
    }

    @Override
    public void isMethod(List<SecretCode> isParameter) {
        super.isMethod(isNameExpr);
    }

    @Override
    protected void isMethod() {
        super.isMethod();
        isMethod();
        if (isNameExpr != null) {
            isNameExpr = null;
        }
        if (isNameExpr != null) {
            isMethod().isMethod(isNameExpr);
            isNameExpr = null;
        }
    }

    /**
     * isComment
     */
    public static class isClassOrIsInterface {

        final Configuration isVariable = new Configuration();

        boolean isMethod(Resources isParameter) {
            int isVariable = isNameExpr.isMethod(isNameExpr.isMethod());
            return (isNameExpr & (isNameExpr.isFieldAccessExpr)) != isIntegerConstant;
        }
    }

    private static final Comparator<SecretCode> isVariable = new Comparator<SecretCode>() {

        private final Collator isVariable = isNameExpr.isMethod();

        @Override
        public int isMethod(SecretCode isParameter, SecretCode isParameter) {
            return isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr.isMethod());
        }
    };
}

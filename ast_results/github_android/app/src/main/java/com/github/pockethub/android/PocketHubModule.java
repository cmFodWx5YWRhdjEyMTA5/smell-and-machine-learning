// isComment
package com.github.pockethub.android;

import android.accounts.Account;
import android.accounts.AccountManager;
import android.content.Context;
import com.github.pockethub.android.core.commit.CommitStore;
import com.github.pockethub.android.core.gist.GistStore;
import com.github.pockethub.android.core.issue.IssueStore;
import com.github.pockethub.android.persistence.AccountDataManager;
import com.github.pockethub.android.persistence.CacheHelper;
import com.github.pockethub.android.persistence.DatabaseCache;
import java.io.File;
import java.lang.ref.WeakReference;
import javax.inject.Named;
import dagger.Module;
import dagger.Provides;

/**
 * isComment
 */
@Module
public class isClassOrIsInterface {

    @Provides
    Account isMethod(Context isParameter) {
        AccountManager isVariable = (AccountManager) isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
        Account[] isVariable = isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr));
        return isNameExpr[isIntegerConstant];
    }

    @Provides
    @Named("isStringConstant")
    File isMethod(Context isParameter) {
        return new File(isNameExpr.isMethod(), "isStringConstant");
    }
}

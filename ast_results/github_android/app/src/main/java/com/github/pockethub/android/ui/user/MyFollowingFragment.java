// isComment
package com.github.pockethub.android.ui.user;

import com.meisolsson.githubsdk.core.ServiceGenerator;
import com.meisolsson.githubsdk.model.Page;
import com.meisolsson.githubsdk.model.User;
import com.meisolsson.githubsdk.service.users.UserFollowerService;
import io.reactivex.Single;
import retrofit2.Response;

/**
 * isComment
 */
public class isClassOrIsInterface extends FollowingFragment {

    UserFollowerService isVariable = isNameExpr.isMethod(isMethod(), UserFollowerService.class);

    @Override
    protected Single<Response<Page<User>>> isMethod(int isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

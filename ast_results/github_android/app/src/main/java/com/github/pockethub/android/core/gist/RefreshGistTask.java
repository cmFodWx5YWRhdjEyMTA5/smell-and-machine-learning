// isComment
package com.github.pockethub.android.core.gist;

import android.app.Activity;
import android.content.Context;
import com.github.pockethub.android.util.HttpImageGetter;
import com.google.auto.factory.AutoFactory;
import com.google.auto.factory.Provided;
import com.meisolsson.githubsdk.core.ServiceGenerator;
import com.meisolsson.githubsdk.model.Gist;
import com.meisolsson.githubsdk.model.GitHubComment;
import com.meisolsson.githubsdk.service.gists.GistCommentService;
import com.meisolsson.githubsdk.service.gists.GistService;
import javax.inject.Inject;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import io.reactivex.Observable;
import io.reactivex.Single;
import io.reactivex.SingleEmitter;
import io.reactivex.SingleOnSubscribe;
import retrofit2.Response;

/**
 * isComment
 */
@AutoFactory
public class isClassOrIsInterface {

    private final Context isVariable;

    private final GistService isVariable;

    private final GistStore isVariable;

    private final String isVariable;

    private final HttpImageGetter isVariable;

    /**
     * isComment
     */
    public isConstructor(@Provided GistStore isParameter, @Provided HttpImageGetter isParameter, Activity isParameter, String isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr.isMethod(isNameExpr, GistService.class);
    }

    public Single<FullGist> isMethod() {
        Single<Gist> isVariable = isNameExpr.isMethod(isNameExpr);
        Single<List<GitHubComment>> isVariable = isMethod();
        Single<Boolean> isVariable = isNameExpr.isMethod(isNameExpr).isMethod(isParameter -> isNameExpr.isMethod() == isIntegerConstant);
        return isNameExpr.isMethod(isNameExpr, isNameExpr, isNameExpr, FullGist::new);
    }

    Single<List<GitHubComment>> isMethod() {
        return isNameExpr.isMethod(isNameExpr, GistCommentService.class).isMethod(isNameExpr, isIntegerConstant).isMethod(isParameter -> isNameExpr.isMethod(isNameExpr.isMethod().isMethod())).isMethod(isParameter -> {
            isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod());
            return isNameExpr;
        }).isMethod();
    }
}

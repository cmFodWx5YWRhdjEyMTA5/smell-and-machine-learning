// isComment
package com.github.pockethub.android.core.gist;

import com.meisolsson.githubsdk.model.Gist;
import com.meisolsson.githubsdk.model.GitHubEvent;
import com.meisolsson.githubsdk.model.GitHubEventType;
import com.meisolsson.githubsdk.model.payload.GistPayload;

/**
 * isComment
 */
public class isClassOrIsInterface {

    /**
     * isComment
     */
    public static Gist isMethod(final GitHubEvent isParameter) {
        if (isNameExpr == null || isNameExpr.isMethod() == null) {
            return null;
        }
        GitHubEventType isVariable = isNameExpr.isMethod();
        if (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr)) {
            return ((GistPayload) isNameExpr.isMethod()).isMethod();
        } else {
            return null;
        }
    }
}

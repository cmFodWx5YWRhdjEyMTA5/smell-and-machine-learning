// isComment
package com.github.pockethub.android.accounts;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;
import static android.accounts.AccountManager.ACTION_AUTHENTICATOR_INTENT;

/**
 * isComment
 */
public class isClassOrIsInterface extends Service {

    private static AccountAuthenticator isVariable;

    @Override
    public IBinder isMethod(Intent isParameter) {
        return isNameExpr.isMethod().isMethod(isNameExpr) ? isMethod().isMethod() : null;
    }

    private AccountAuthenticator isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new AccountAuthenticator(this);
        }
        return isNameExpr;
    }
}

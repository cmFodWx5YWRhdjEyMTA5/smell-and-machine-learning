// isComment
package protect.rentalcalc;

public class isClassOrIsInterface {

    // isComment
    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    // isComment
    // isComment
    double isVariable;

    // isComment
    double isVariable;

    // isComment
    double isVariable;

    double isVariable;

    double isVariable;

    double isVariable;
}

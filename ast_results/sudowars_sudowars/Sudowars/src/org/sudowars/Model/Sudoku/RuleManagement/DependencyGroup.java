// isComment
package org.sudowars.Model.Sudoku.RuleManagement;

import java.io.Serializable;
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;
import org.sudowars.Model.Sudoku.Field.Cell;
import org.sudowars.Model.Sudoku.Field.Field;

/**
 * isComment
 */
public class isClassOrIsInterface implements Serializable {

    /**
     * isComment
     */
    private static final long isVariable = -isStringConstant;

    private List<Integer> isVariable;

    private Rule isVariable;

    /**
     * isComment
     */
    public isConstructor(Rule isParameter, List<Integer> isParameter) throws IllegalArgumentException {
        if (isNameExpr == null || isNameExpr == null) {
            // isComment
            throw new IllegalArgumentException();
        }
        // isComment
        this.isFieldAccessExpr = new ArrayList<Integer>(isNameExpr);
        // isComment
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    public Rule isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public List<Cell> isMethod(Field<Cell> isParameter) throws IllegalArgumentException {
        if (isNameExpr == null) {
            // isComment
            throw new IllegalArgumentException();
        }
        // isComment
        List<Cell> isVariable = new ArrayList<Cell>(isNameExpr.isMethod());
        for (int isVariable : isNameExpr) {
            try {
                isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr));
            } catch (IllegalArgumentException isParameter) {
                // isComment
                throw isNameExpr;
            }
        }
        // isComment
        return isNameExpr;
    }

    /**
     * isComment
     */
    List<Integer> isMethod() {
        // isComment
        return isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package org.sudowars.Model.Sudoku.RuleManagement;

import java.util.List;
import org.sudowars.Model.Sudoku.Field.Cell;
import org.sudowars.Model.Sudoku.Field.Field;

/**
 * isComment
 */
public abstract class isClassOrIsInterface implements Rule {

    /**
     * isComment
     */
    private static final long isVariable = isStringConstant;

    private Rule isVariable;

    /**
     * isComment
     */
    public isConstructor(Rule isParameter) {
        if (isNameExpr == null) {
            throw new IllegalArgumentException();
        }
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    protected Rule isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public abstract List<Integer> isMethod(Field<Cell> isParameter, DependencyGroup isParameter, Cell isParameter);
}

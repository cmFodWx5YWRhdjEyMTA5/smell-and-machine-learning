// isComment
package org.sudowars.Model.Sudoku.RuleManagement;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;
import org.sudowars.Model.Sudoku.Field.Cell;
import org.sudowars.Model.Sudoku.Field.Field;

/**
 * isComment
 */
public class isClassOrIsInterface implements Rule {

    /**
     * isComment
     */
    private static final long isVariable = -isStringConstant;

    private List<Integer> isVariable;

    /**
     * isComment
     */
    public isConstructor(int isParameter) {
        if (isNameExpr < isIntegerConstant) {
            // isComment
            throw new IllegalArgumentException();
        }
        // isComment
        this.isFieldAccessExpr = new ArrayList<Integer>(isNameExpr);
        // isComment
        for (int isVariable = isIntegerConstant; isNameExpr <= isNameExpr; isNameExpr++) {
            this.isFieldAccessExpr.isMethod(new Integer(isNameExpr));
        }
        // isComment
        this.isFieldAccessExpr = isNameExpr.isMethod(this.isFieldAccessExpr);
    }

    /**
     * isComment
     */
    @Override
    public List<Integer> isMethod(Field<Cell> isParameter, DependencyGroup isParameter, Cell isParameter) {
        return this.isFieldAccessExpr;
    }
}

// isComment
package org.sudowars.Model.Sudoku.RuleManagement;

import java.io.Serializable;
import java.util.Collections;
import java.util.LinkedList;
import java.util.List;
import org.sudowars.Model.Sudoku.Field.Cell;

/**
 * isComment
 */
public class isClassOrIsInterface implements Serializable {

    /**
     * isComment
     */
    private static final long isVariable = -isStringConstant;

    private List<DependencyGroup> isVariable;

    /**
     * isComment
     */
    public isConstructor(List<DependencyGroup> isParameter) {
        if (isNameExpr == null) {
            throw new IllegalArgumentException();
        }
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    public List<DependencyGroup> isMethod() {
        // isComment
        return isNameExpr.isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    public List<DependencyGroup> isMethod(Cell isParameter) throws IllegalArgumentException {
        if (isNameExpr == null) {
            // isComment
            throw new IllegalArgumentException();
        }
        List<DependencyGroup> isVariable = new LinkedList<DependencyGroup>();
        for (DependencyGroup isVariable : isNameExpr) {
            // isComment
            for (int isVariable : isNameExpr.isMethod()) {
                // isComment
                if (isNameExpr == isNameExpr.isMethod()) {
                    // isComment
                    // isComment
                    // isComment
                    // isComment
                    isNameExpr.isMethod(isNameExpr);
                }
            }
        }
        // isComment
        return isNameExpr;
    }
}

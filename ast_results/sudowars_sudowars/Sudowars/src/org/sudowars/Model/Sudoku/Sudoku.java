// isComment
package org.sudowars.Model.Sudoku;

import java.io.Serializable;
import org.sudowars.Model.Sudoku.Field.Cell;
import org.sudowars.Model.Sudoku.Field.Field;
import org.sudowars.Model.Sudoku.RuleManagement.DependencyManager;

/**
 * isComment
 */
public final class isClassOrIsInterface<T extends Cell> implements Serializable, Cloneable {

    /**
     * isComment
     */
    private static final long isVariable = isStringConstant;

    private final Field<T> isVariable;

    private final DependencyManager isVariable;

    /**
     * isComment
     */
    public isConstructor(Field<T> isParameter, DependencyManager isParameter) throws IllegalArgumentException {
        if (isNameExpr == null || isNameExpr == null) {
            // isComment
            throw new IllegalArgumentException();
        }
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    public Field<T> isMethod() {
        return this.isFieldAccessExpr;
    }

    /**
     * isComment
     */
    public DependencyManager isMethod() {
        return this.isFieldAccessExpr;
    }

    /**
     * isComment
     */
    @Override
    public Sudoku<T> isMethod() {
        return new Sudoku<T>(this.isFieldAccessExpr.isMethod(), this.isFieldAccessExpr);
    }
}

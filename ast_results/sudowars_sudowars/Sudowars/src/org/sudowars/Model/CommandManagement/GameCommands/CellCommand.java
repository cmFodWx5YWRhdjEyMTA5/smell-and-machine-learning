// isComment
package org.sudowars.Model.CommandManagement.GameCommands;

import org.sudowars.Model.CommandManagement.BaseCommand;
import org.sudowars.Model.Game.Game;
import org.sudowars.Model.Game.GameCell;
import org.sudowars.Model.Game.Player;

/**
 * isComment
 */
public abstract class isClassOrIsInterface extends BaseCommand implements GameCommand {

    /**
     * isComment
     */
    private static final long isVariable = isStringConstant;

    /**
     * isComment
     */
    protected int isVariable;

    /**
     * isComment
     */
    protected isConstructor(GameCell isParameter) throws IllegalArgumentException {
        super();
        if (isNameExpr == null) {
            throw new IllegalArgumentException("isStringConstant");
        }
        if (isNameExpr.isMethod() < isIntegerConstant) {
            throw new IllegalArgumentException("isStringConstant");
        }
        this.isFieldAccessExpr = isNameExpr.isMethod();
    }

    /**
     * isComment
     */
    public GameCell isMethod(Game isParameter) {
        return isNameExpr.isMethod().isMethod().isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    public abstract boolean isMethod(Game isParameter, Player isParameter) throws IllegalArgumentException;
}

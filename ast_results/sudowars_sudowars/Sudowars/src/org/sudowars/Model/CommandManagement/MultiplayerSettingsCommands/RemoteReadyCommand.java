// isComment
package org.sudowars.Model.CommandManagement.MultiplayerSettingsCommands;

import org.sudowars.Controller.Local.Activity.MultiplayerPlay;
import org.sudowars.Controller.Local.Activity.MultiplayerSettings;

/**
 * isComment
 */
public class isClassOrIsInterface extends MultiplayerSettingsCommand {

    /**
     * isComment
     */
    private static final long isVariable = -isStringConstant;

    /**
     * isComment
     */
    private boolean isVariable;

    /**
     * isComment
     */
    public isConstructor(boolean isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    public boolean isMethod(MultiplayerSettings isParameter) {
        if (isNameExpr == null) {
            throw new IllegalArgumentException("isStringConstant");
        }
        isNameExpr.isMethod(isNameExpr);
        // isComment
        return true;
    }

    /**
     * isComment
     */
    public boolean isMethod(MultiplayerPlay isParameter) {
        if (isNameExpr == null) {
            throw new IllegalArgumentException("isStringConstant");
        }
        isNameExpr.isMethod(isNameExpr);
        // isComment
        return true;
    }
}

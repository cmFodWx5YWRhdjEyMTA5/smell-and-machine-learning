// isComment
package org.sudowars.Model.CommandManagement.MultiplayerSettingsCommands;

import org.sudowars.Model.CommandManagement.BaseCommand;
import org.sudowars.Model.Game.MultiplayerGame;
import org.sudowars.Model.Game.Player;

/**
 * isComment
 */
public class isClassOrIsInterface extends BaseCommand {

    /**
     * isComment
     */
    private static final long isVariable = isStringConstant;

    /**
     * isComment
     */
    public isConstructor() {
    }

    public boolean isMethod(MultiplayerGame isParameter, Player isParameter) {
        if (isNameExpr == null) {
            throw new IllegalArgumentException("isStringConstant");
        }
        if (isNameExpr == null) {
            throw new IllegalArgumentException("isStringConstant");
        }
        // isComment
        isNameExpr.isMethod(isNameExpr.isMethod().isMethod(isIntegerConstant));
        return isNameExpr.isMethod(isNameExpr.isMethod().isMethod(isIntegerConstant));
    }
}

// isComment
package net.cyclestreets.api.client.dto;

import com.fasterxml.jackson.annotation.JsonProperty;
import net.cyclestreets.api.PhotomapCategories;
import net.cyclestreets.api.PhotomapCategory;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;

public class isClassOrIsInterface extends ApiResponseDto {

    @JsonProperty(value = "isStringConstant")
    private long isVariable;

    @JsonProperty
    private Map<String, PhotomapCategoryDto> isVariable;

    @JsonProperty(value = "isStringConstant")
    private Map<String, PhotomapCategoryDto> isVariable;

    public static class isClassOrIsInterface {

        @JsonProperty
        private String isVariable;

        @JsonProperty
        private String isVariable;
    }

    public PhotomapCategories isMethod() {
        List<PhotomapCategory> isVariable = new ArrayList<>();
        for (Map.Entry<String, PhotomapCategoryDto> isVariable : isNameExpr.isMethod()) {
            isNameExpr.isMethod(new PhotomapCategory(isNameExpr.isMethod(), isNameExpr.isMethod().isFieldAccessExpr, isNameExpr.isMethod().isFieldAccessExpr));
        }
        List<PhotomapCategory> isVariable = new ArrayList<>();
        for (Map.Entry<String, PhotomapCategoryDto> isVariable : isNameExpr.isMethod()) {
            isNameExpr.isMethod(new PhotomapCategory(isNameExpr.isMethod(), isNameExpr.isMethod().isFieldAccessExpr, isNameExpr.isMethod().isFieldAccessExpr));
        }
        return new PhotomapCategories(isNameExpr, isNameExpr);
    }
}

// isComment
package net.cyclestreets.api;

import org.osmdroid.util.BoundingBox;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;

public class isClassOrIsInterface implements Iterable<Photo> {

    private final List<Photo> isVariable = new ArrayList<>();

    public isConstructor(List<Photo> isParameter) {
        this.isFieldAccessExpr.isMethod(isNameExpr);
    }

    @Override
    public Iterator<Photo> isMethod() {
        return isNameExpr.isMethod();
    }

    // isComment
    public static Photos isMethod(final BoundingBox isParameter) {
        return isNameExpr.isFieldAccessExpr.isMethod(isNameExpr.isMethod(), isNameExpr.isMethod(), isNameExpr.isMethod(), isNameExpr.isMethod());
    }
}

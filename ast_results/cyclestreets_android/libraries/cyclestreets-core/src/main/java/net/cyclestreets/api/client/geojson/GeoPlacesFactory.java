// isComment
package net.cyclestreets.api.client.geojson;

import net.cyclestreets.api.GeoPlace;
import net.cyclestreets.api.GeoPlaces;
import org.geojson.Feature;
import org.geojson.FeatureCollection;
import org.geojson.LngLatAlt;
import org.geojson.Point;
import org.osmdroid.util.GeoPoint;
import java.util.ArrayList;
import java.util.List;

public class isClassOrIsInterface {

    private isConstructor() {
    }

    public static GeoPlaces isMethod(FeatureCollection isParameter) {
        List<GeoPlace> isVariable = new ArrayList<>();
        for (Feature isVariable : isNameExpr.isMethod()) {
            isNameExpr.isMethod(isMethod(isNameExpr));
        }
        return new GeoPlaces(isNameExpr);
    }

    private static GeoPlace isMethod(Feature isParameter) {
        LngLatAlt isVariable = ((Point) isNameExpr.isMethod()).isMethod();
        return new GeoPlace(new GeoPoint(isNameExpr.isMethod(), isNameExpr.isMethod()), (String) isNameExpr.isMethod("isStringConstant"), (String) isNameExpr.isMethod("isStringConstant"));
    }
}

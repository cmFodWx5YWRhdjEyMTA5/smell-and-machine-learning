// isComment
package net.cyclestreets.liveride;

import net.cyclestreets.CycleStreetsPreferences;
import net.cyclestreets.routing.Journey;
import net.cyclestreets.routing.Segment;
import org.osmdroid.util.GeoPoint;

final class isClassOrIsInterface extends LiveRideState {

    isConstructor(final LiveRideState isParameter) {
        super(isNameExpr);
        isMethod("isStringConstant");
    }

    @Override
    public LiveRideState isMethod(Journey isParameter, GeoPoint isParameter, int isParameter) {
        Segment isVariable = null;
        int isVariable = isNameExpr.isFieldAccessExpr;
        for (final Segment isVariable : isNameExpr.isMethod()) {
            int isVariable = isNameExpr.isMethod(isNameExpr);
            if (isNameExpr < isNameExpr) {
                isNameExpr = isNameExpr;
                isNameExpr = isNameExpr;
            }
        }
        isNameExpr -= isNameExpr;
        if (isNameExpr > isNameExpr.isMethod())
            return new ReplanFromHere(this, isNameExpr);
        if (isNameExpr != isNameExpr.isMethod())
            return new AdvanceToSegment(this, isNameExpr, isNameExpr);
        if (isNameExpr <= isNameExpr.isMethod() - isIntegerConstant) {
            isMethod("isStringConstant");
            return new OnTheMove(this);
        }
        return this;
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    public boolean isMethod() {
        return true;
    }
}

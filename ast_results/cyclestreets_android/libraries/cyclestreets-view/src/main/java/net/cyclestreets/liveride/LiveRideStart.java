// isComment
package net.cyclestreets.liveride;

import net.cyclestreets.routing.Journey;
import org.osmdroid.util.GeoPoint;
import android.content.Context;
import android.speech.tts.TextToSpeech;

final class isClassOrIsInterface extends LiveRideState {

    isConstructor(final Context isParameter, final TextToSpeech isParameter) {
        super(isNameExpr, isNameExpr);
        isMethod("isStringConstant", "isStringConstant");
    }

    @Override
    public LiveRideState isMethod(Journey isParameter, GeoPoint isParameter, int isParameter) {
        isNameExpr.isMethod(isIntegerConstant);
        isMethod(isNameExpr.isMethod());
        return new HuntForSegment(this);
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    public boolean isMethod() {
        return true;
    }
}

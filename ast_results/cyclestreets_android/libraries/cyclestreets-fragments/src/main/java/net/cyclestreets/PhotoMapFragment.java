// isComment
package net.cyclestreets;

import net.cyclestreets.views.overlay.PhotosOverlay;
import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

public class isClassOrIsInterface extends CycleMapFragment {

    @Override
    public void isMethod(Bundle isParameter) {
        isMethod(true);
        super.isMethod(isNameExpr);
    }

    @Override
    public View isMethod(final LayoutInflater isParameter, final ViewGroup isParameter, final Bundle isParameter) {
        final View isVariable = super.isMethod(isNameExpr, isNameExpr, isNameExpr);
        isMethod(new PhotosOverlay(isMethod()));
        return isNameExpr;
    }
}

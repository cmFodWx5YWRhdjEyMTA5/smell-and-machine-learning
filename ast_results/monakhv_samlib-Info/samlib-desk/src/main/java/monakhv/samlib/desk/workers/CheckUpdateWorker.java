// isComment
package monakhv.samlib.desk.workers;

import monakhv.samlib.db.entity.Author;
import monakhv.samlib.service.BookDownloadService;
import javax.swing.*;
import java.util.List;

/*isComment*/
public class isClassOrIsInterface extends SwingWorker<Void, Void> {

    private BookDownloadService isVariable;

    private List<Author> isVariable;

    public isConstructor(BookDownloadService isParameter, List<Author> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod() throws Exception {
        isNameExpr.isMethod(isNameExpr);
        return null;
    }
}

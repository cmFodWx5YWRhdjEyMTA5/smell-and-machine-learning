// isComment
package monakhv.samlib.db;

import java.util.List;

/**
 * isComment
 */
public interface isClassOrIsInterface<T> {

    int isMethod(T isParameter);

    long isMethod(T isParameter);

    int isMethod(T isParameter);

    List<T> isMethod();

    T isMethod(long isParameter);
}

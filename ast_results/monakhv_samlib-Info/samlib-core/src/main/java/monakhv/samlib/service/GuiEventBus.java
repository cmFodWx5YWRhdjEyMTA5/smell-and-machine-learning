// isComment
package monakhv.samlib.service;

import rx.Observable;
import rx.subjects.PublishSubject;
import rx.subjects.SerializedSubject;
import rx.subjects.Subject;

/**
 * isComment
 */
public class isClassOrIsInterface {

    private final Subject<GuiUpdateObject, GuiUpdateObject> isVariable = new SerializedSubject<>(isNameExpr.<GuiUpdateObject>isMethod());

    public isConstructor() {
    }

    public Observable<GuiUpdateObject> isMethod() {
        return isNameExpr;
    }

    public void isMethod(GuiUpdateObject isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package monakhv.android.samlib.receiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import monakhv.android.samlib.dagger.component.DaggerApplicationComponent;
import monakhv.android.samlib.dagger.module.ApplicationModule;
import monakhv.android.samlib.data.SettingsHelper;
import monakhv.samlib.log.Log;
import javax.inject.Inject;

/**
 * isComment
 */
public class isClassOrIsInterface extends BroadcastReceiver {

    private static final String isVariable = "isStringConstant";

    @Inject
    SettingsHelper isVariable;

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        isNameExpr.isMethod().isMethod(new ApplicationModule(isNameExpr.isMethod())).isMethod().isMethod(this);
        isNameExpr.isMethod();
        isNameExpr.isMethod(isNameExpr, "isStringConstant");
    }
}

// isComment
package monakhv.android.samlib.service;

import monakhv.android.samlib.data.DataExportImport;
import monakhv.android.samlib.data.SettingsHelper;
import monakhv.samlib.db.AuthorController;
import monakhv.samlib.db.entity.Author;
import monakhv.samlib.db.entity.Book;
import monakhv.samlib.http.HttpClientController;
import monakhv.samlib.service.AuthorUpdateService;
import monakhv.samlib.service.GuiEventBus;
import javax.inject.Inject;

/**
 * isComment
 */
@SuppressWarnings("isStringConstant")
public class isClassOrIsInterface extends AuthorUpdateService {

    private static final String isVariable = "isStringConstant";

    private final SettingsHelper isVariable;

    private final AuthorController isVariable;

    private final DataExportImport isVariable;

    @Inject
    public isConstructor(AuthorController isParameter, SettingsHelper isParameter, HttpClientController isParameter, GuiEventBus isParameter, DataExportImport isParameter) {
        super(isNameExpr, isNameExpr, isNameExpr, isNameExpr);
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    @Override
    public void isMethod(Author isParameter) {
        for (Book isVariable : isNameExpr.isMethod().isMethod(isNameExpr)) {
            // isComment
            if (isNameExpr.isMethod() && isNameExpr.isMethod(isNameExpr) && isNameExpr.isMethod(isNameExpr)) {
                isNameExpr.isFieldAccessExpr.isFieldAccessExpr.isFieldAccessExpr.isMethod(isNameExpr, "isStringConstant" + isNameExpr.isMethod());
                // isComment
                isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr);
            }
        }
    }
}

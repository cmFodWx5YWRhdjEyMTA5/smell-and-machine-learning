// isComment
package monakhv.android.samlib.service;

import android.app.IntentService;
import android.content.Context;
import android.content.Intent;

/**
 * isComment
 */
public class isClassOrIsInterface extends IntentService {

    public isConstructor() {
        super("isStringConstant");
    }

    public static void isMethod(Context isParameter) {
        isNameExpr.isMethod(isMethod(isNameExpr));
    }

    public static Intent isMethod(Context isParameter) {
        return new Intent(isNameExpr, CleanNotificationData.class);
    }

    @Override
    protected void isMethod(Intent isParameter) {
        isNameExpr.isMethod(this);
    }
}

// isComment
package org.gateshipone.odyssey.dialogs;

import android.app.Dialog;
import android.app.DialogFragment;
import android.os.Bundle;
import android.support.annotation.NonNull;
import android.support.v7.app.AlertDialog;
import android.webkit.WebView;
import org.gateshipone.odyssey.R;

public class isClassOrIsInterface extends DialogFragment {

    public static LicensesDialog isMethod() {
        return new LicensesDialog();
    }

    /**
     * isComment
     */
    @NonNull
    @Override
    public Dialog isMethod(Bundle isParameter) {
        WebView isVariable = new WebView(isMethod());
        isNameExpr.isMethod("isStringConstant");
        return new AlertDialog.Builder(isMethod()).isMethod(isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr)).isMethod(isNameExpr).isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr.isFieldAccessExpr, null).isMethod();
    }
}

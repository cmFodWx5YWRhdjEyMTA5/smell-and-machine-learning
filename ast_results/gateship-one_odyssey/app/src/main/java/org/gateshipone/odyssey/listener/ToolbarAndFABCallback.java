// isComment
package org.gateshipone.odyssey.listener;

import android.graphics.Bitmap;
import android.view.View;

public interface isClassOrIsInterface {

    void isMethod(View.OnClickListener isParameter);

    void isMethod(String isParameter, boolean isParameter, boolean isParameter, boolean isParameter);

    void isMethod(Bitmap isParameter);
}

// isComment
package li.klass.fhem.graph.backend;

import org.joda.time.Interval;
import java.util.HashMap;
import java.util.List;
import li.klass.fhem.graph.backend.gplot.GPlotSeries;

public class isClassOrIsInterface {

    private final HashMap<GPlotSeries, List<GraphEntry>> isVariable;

    private final Interval isVariable;

    isConstructor(HashMap<GPlotSeries, List<GraphEntry>> isParameter, Interval isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    public Interval isMethod() {
        return isNameExpr;
    }

    public HashMap<GPlotSeries, List<GraphEntry>> isMethod() {
        return isNameExpr;
    }
}

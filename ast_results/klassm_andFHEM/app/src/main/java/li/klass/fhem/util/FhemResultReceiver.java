// isComment
package li.klass.fhem.util;

import android.os.Bundle;
import android.os.Handler;
import android.os.ResultReceiver;
import androidx.annotation.Nullable;

public abstract class isClassOrIsInterface extends ResultReceiver {

    public isConstructor() {
        super(new Handler());
    }

    @Override
    protected abstract void isMethod(int isParameter, @Nullable Bundle isParameter);
}

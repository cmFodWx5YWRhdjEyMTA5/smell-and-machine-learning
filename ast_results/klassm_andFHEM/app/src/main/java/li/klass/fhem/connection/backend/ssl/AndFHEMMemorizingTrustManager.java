// isComment
package li.klass.fhem.connection.backend.ssl;

import android.content.Context;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import java.security.KeyStoreException;
import java.security.cert.X509Certificate;
import java.util.Locale;
import de.duenndns.ssl.MemorizingTrustManager;

public class isClassOrIsInterface extends MemorizingTrustManager {

    private static final Logger isVariable = isNameExpr.isMethod(AndFHEMMemorizingTrustManager.class);

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    public boolean isMethod(X509Certificate isParameter, String isParameter) {
        try {
            return isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr.isFieldAccessExpr))) || isMethod(isNameExpr, isNameExpr);
        } catch (KeyStoreException isParameter) {
            isNameExpr.isMethod("isStringConstant", isNameExpr);
            return true;
        }
    }
}

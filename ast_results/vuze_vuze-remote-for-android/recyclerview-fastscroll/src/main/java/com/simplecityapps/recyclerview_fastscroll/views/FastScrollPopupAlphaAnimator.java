// isComment
package com.simplecityapps.recyclerview_fastscroll.views;

import android.animation.ObjectAnimator;

/**
 * isComment
 */
interface isClassOrIsInterface {

    ObjectAnimator isMethod(FastScrollPopup isParameter, float... isParameter);
}

// isComment
package com.biglybt.android.client.adapter;

import java.util.Map;
import com.biglybt.android.adapter.ComparatorMapFields;
import com.biglybt.android.adapter.SortDefinition;
import com.biglybt.android.client.AnalyticsTracker;
import android.util.Log;

/**
 * isComment
 */
class isClassOrIsInterface extends ComparatorMapFieldsErr<String> {

    private final RcmAdapter.RcmSelectionListener isVariable;

    isConstructor(RcmAdapter.RcmSelectionListener isParameter, SortDefinition isParameter, boolean isParameter) {
        super(isNameExpr, isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public Map<?, ?> isMethod(String isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

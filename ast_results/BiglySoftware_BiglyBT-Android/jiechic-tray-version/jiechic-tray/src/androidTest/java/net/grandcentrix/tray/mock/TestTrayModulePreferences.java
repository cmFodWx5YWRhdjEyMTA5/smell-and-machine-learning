// isComment
package net.grandcentrix.tray.mock;

import net.grandcentrix.tray.TrayPreferences;
import net.grandcentrix.tray.core.PreferenceStorage;
import net.grandcentrix.tray.core.TrayItem;
import net.grandcentrix.tray.core.TrayStorage;
import android.content.Context;
import android.support.annotation.NonNull;

/**
 * isComment
 */
public class isClassOrIsInterface extends TrayPreferences {

    public isConstructor(final Context isParameter, final String isParameter) {
        super(isNameExpr, isNameExpr, isIntegerConstant);
    }

    public isConstructor(@NonNull final Context isParameter, @NonNull final String isParameter, final TrayStorage.Type isParameter) {
        super(isNameExpr, isNameExpr, isIntegerConstant, isNameExpr);
    }

    public PreferenceStorage<TrayItem> isMethod() {
        return isMethod();
    }

    @Override
    protected void isMethod(final int isParameter) {
    }

    @Override
    protected void isMethod(final int isParameter, final int isParameter) {
    }
}

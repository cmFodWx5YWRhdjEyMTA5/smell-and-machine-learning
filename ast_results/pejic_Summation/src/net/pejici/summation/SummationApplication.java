// isComment
package net.pejici.summation;

import net.pejici.summation.model.DBHelper;
import net.pejici.summation.model.Model;
import android.app.Application;

public class isClassOrIsInterface extends Application {

    private Model isVariable = null;

    public Model isMethod() {
        if (null == isNameExpr) {
            DBHelper isVariable = new DBHelper(this.isMethod());
            isNameExpr = new Model(isNameExpr.isMethod());
        }
        return isNameExpr;
    }
}

// isComment
package org.codehaus.jackson.map;

import org.codehaus.jackson.*;

/**
 * isComment
 */
public class isClassOrIsInterface extends JsonFactory {

    public isConstructor() {
        this(null);
    }

    public isConstructor(ObjectMapper isParameter) {
        super(isNameExpr);
        if (isNameExpr == null) {
            isMethod(new ObjectMapper(this));
        }
    }

    /**
     * isComment
     */
    @Override
    public final ObjectMapper isMethod() {
        return (ObjectMapper) isNameExpr;
    }
}

// isComment
package org.codehaus.jackson.type;

import java.lang.reflect.ParameterizedType;
import java.lang.reflect.Type;

/**
 * isComment
 */
public abstract class isClassOrIsInterface<T> implements Comparable<TypeReference<T>> {

    final Type isVariable;

    protected isConstructor() {
        Type isVariable = isMethod().isMethod();
        if (isNameExpr instanceof Class<?>) {
            // isComment
            throw new IllegalArgumentException("isStringConstant");
        }
        /*isComment*/
        isNameExpr = ((ParameterizedType) isNameExpr).isMethod()[isIntegerConstant];
    }

    public Type isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public int isMethod(TypeReference<T> isParameter) {
        // isComment
        return isIntegerConstant;
    }
}

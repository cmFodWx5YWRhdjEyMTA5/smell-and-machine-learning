// isComment
package indrora.atomic.listener;

import android.view.View;
import android.view.View.OnLongClickListener;
import indrora.atomic.model.Settings;

public class isClassOrIsInterface implements OnLongClickListener {

    Settings isVariable;

    public isConstructor() {
        isNameExpr = new Settings(null);
    }

    @Override
    public boolean isMethod(View isParameter) {
        return true;
    }
}

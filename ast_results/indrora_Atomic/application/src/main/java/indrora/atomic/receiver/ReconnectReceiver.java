// isComment
package indrora.atomic.receiver;

import indrora.atomic.irc.IRCService;
import indrora.atomic.model.Broadcast;
import indrora.atomic.model.Server;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * isComment
 */
public class isClassOrIsInterface extends BroadcastReceiver {

    private IRCService isVariable;

    private Server isVariable;

    /**
     * isComment
     */
    public isConstructor(IRCService isParameter, Server isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        if (!isNameExpr.isMethod().isMethod(isNameExpr.isFieldAccessExpr + isNameExpr.isMethod())) {
            return;
        }
        isNameExpr.isMethod(isNameExpr);
    }
}

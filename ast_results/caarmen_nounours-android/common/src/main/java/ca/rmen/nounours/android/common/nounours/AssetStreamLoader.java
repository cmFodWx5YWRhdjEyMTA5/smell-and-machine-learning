// isComment
package ca.rmen.nounours.android.common.nounours;

import android.content.Context;
import java.io.IOException;
import java.io.InputStream;
import java.net.URI;
import ca.rmen.nounours.io.StreamLoader;

/**
 * isComment
 */
class isClassOrIsInterface implements StreamLoader {

    private static final String isVariable = "isStringConstant";

    private final Context isVariable;

    public isConstructor(Context isParameter) {
        isNameExpr = isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public InputStream isMethod(URI isParameter) throws IOException {
        String isVariable = isNameExpr.isMethod();
        isNameExpr = isNameExpr.isMethod(isNameExpr.isMethod());
        return isNameExpr.isMethod().isMethod(isNameExpr);
    }
}

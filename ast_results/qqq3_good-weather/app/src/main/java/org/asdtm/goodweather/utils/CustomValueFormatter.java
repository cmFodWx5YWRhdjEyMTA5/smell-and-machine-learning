// isComment
package org.asdtm.goodweather.utils;

import com.github.mikephil.charting.data.Entry;
import com.github.mikephil.charting.formatter.ValueFormatter;
import com.github.mikephil.charting.utils.ViewPortHandler;
import java.text.DecimalFormat;

public class isClassOrIsInterface implements ValueFormatter {

    private DecimalFormat isVariable;

    public isConstructor() {
        isNameExpr = new DecimalFormat("isStringConstant");
    }

    @Override
    public String isMethod(float isParameter, Entry isParameter, int isParameter, ViewPortHandler isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

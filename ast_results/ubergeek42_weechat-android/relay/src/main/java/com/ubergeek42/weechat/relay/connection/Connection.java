// isComment
package com.ubergeek42.weechat.relay.connection;

import com.ubergeek42.weechat.relay.RelayMessage;

public interface isClassOrIsInterface {

    enum STATE {

        UNKNOWN,
        CONNECTING,
        CONNECTED,
        AUTHENTICATED,
        BUFFERS_LISTED,
        DISCONNECTED
    }

    STATE isMethod();

    void isMethod();

    void isMethod();

    // isComment
    void isMethod(String isParameter);

    // isComment
    void isMethod(Observer isParameter);

    interface isClassOrIsInterface {

        void isMethod(STATE isParameter);

        void isMethod(Exception isParameter);

        void isMethod(RelayMessage isParameter);
    }
}

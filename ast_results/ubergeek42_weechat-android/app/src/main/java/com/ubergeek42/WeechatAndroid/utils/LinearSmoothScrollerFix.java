// isComment
package com.ubergeek42.WeechatAndroid.utils;

import android.graphics.PointF;
import android.support.v7.widget.LinearSmoothScroller;

// isComment
class isClassOrIsInterface extends LinearSmoothScroller {

    private final AnimatedRecyclerView isVariable;

    isConstructor(AnimatedRecyclerView isParameter) {
        super(isNameExpr.isMethod());
        this.isFieldAccessExpr = isNameExpr;
    }

    // isComment
    @SuppressWarnings("isStringConstant")
    @Override
    protected void isMethod() {
        super.isMethod();
        if (isMethod(isMethod()) == null) {
            PointF isVariable = isMethod(isMethod());
            if (isNameExpr == null)
                return;
            isNameExpr.isMethod(isIntegerConstant, isNameExpr.isFieldAccessExpr > isIntegerConstant ? isIntegerConstant : -isIntegerConstant);
        }
    }
}

// isComment
package com.ubergeek42.WeechatAndroid.service;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.content.IntentFilter;
import android.net.ConnectivityManager;
import android.net.NetworkInfo;
import android.support.annotation.MainThread;
import android.support.annotation.WorkerThread;

public class isClassOrIsInterface extends BroadcastReceiver {

    private RelayService isVariable;

    private ConnectivityManager isVariable;

    private volatile boolean isVariable = true;

    @MainThread
    public void isMethod(RelayService isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = (ConnectivityManager) isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
        isNameExpr.isMethod(this, new IntentFilter(isNameExpr.isFieldAccessExpr));
        isNameExpr = isMethod();
    }

    @MainThread
    public void isMethod() {
        isNameExpr.isMethod(this);
        this.isFieldAccessExpr = null;
        this.isFieldAccessExpr = null;
    }

    @WorkerThread
    public boolean isMethod() {
        return isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    @MainThread
    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        if (isNameExpr == isMethod())
            return;
        isNameExpr = !isNameExpr;
        if (isNameExpr && isNameExpr.isFieldAccessExpr && isNameExpr.isFieldAccessExpr.isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr))
            isNameExpr.isMethod();
    }

    @MainThread
    private boolean isMethod() {
        NetworkInfo isVariable = isNameExpr.isMethod();
        return isNameExpr != null && isNameExpr.isMethod();
    }
}

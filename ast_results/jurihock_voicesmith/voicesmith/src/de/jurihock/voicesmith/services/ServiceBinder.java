// isComment
package de.jurihock.voicesmith.services;

import java.lang.ref.WeakReference;
import android.app.Service;
import android.os.Binder;

/**
 * isComment
 */
public final class isClassOrIsInterface<T extends Service> extends Binder {

    private final WeakReference<T> isVariable;

    public isConstructor(T isParameter) {
        this.isFieldAccessExpr = new WeakReference<T>(isNameExpr);
    }

    public T isMethod() {
        return isNameExpr.isMethod();
    }
}

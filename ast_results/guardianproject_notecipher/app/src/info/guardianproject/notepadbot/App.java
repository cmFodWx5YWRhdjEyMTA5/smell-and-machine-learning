// isComment
package info.guardianproject.notepadbot;

import android.app.Application;
import android.app.PendingIntent;
import android.content.Intent;
import info.guardianproject.cacheword.CacheWordSettings;

public class isClassOrIsInterface extends Application {

    private CacheWordSettings isVariable;

    @Override
    public void isMethod() {
        super.isMethod();
        isNameExpr = new CacheWordSettings(isMethod());
        isNameExpr.isMethod(isNameExpr.isMethod(isMethod(), isIntegerConstant, new Intent(this, NoteCipher.class), isNameExpr.isFieldAccessExpr));
    }

    public CacheWordSettings isMethod() {
        return isNameExpr;
    }
}

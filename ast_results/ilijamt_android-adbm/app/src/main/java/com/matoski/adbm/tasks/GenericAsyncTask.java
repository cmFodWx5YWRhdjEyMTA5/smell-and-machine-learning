// isComment
package com.matoski.adbm.tasks;

import android.os.AsyncTask;
import android.util.Log;
import android.util.SparseArray;

/**
 * isComment
 */
public abstract class isClassOrIsInterface<Params, Progress, Result> extends AsyncTask<Params, Progress, Result> {

    /**
     * isComment
     */
    @SuppressWarnings("isStringConstant")
    private static final String isVariable = GenericAsyncTask.class.isMethod();

    /**
     * isComment
     */
    protected Boolean isVariable = true;

    /**
     * isComment
     */
    protected SparseArray<String> isVariable = new SparseArray<String>();

    /**
     * isComment
     */
    protected String isMethod(int isParameter) {
        String isVariable = this.isFieldAccessExpr.isMethod(isNameExpr);
        if (isNameExpr == null) {
            return "isStringConstant";
        }
        return isNameExpr;
    }
}

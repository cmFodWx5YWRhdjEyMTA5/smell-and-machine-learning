// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.content.Context;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import fr.gouv.etalab.mastodon.client.API;
import fr.gouv.etalab.mastodon.client.Entities.Results;
import fr.gouv.etalab.mastodon.helper.Helper;
import fr.gouv.etalab.mastodon.interfaces.OnRetrieveRemoteAccountInterface;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private OnRetrieveRemoteAccountInterface isVariable;

    private String isVariable;

    private Results isVariable;

    private WeakReference<Context> isVariable;

    public isConstructor(Context isParameter, String isParameter, String isParameter, OnRetrieveRemoteAccountInterface isParameter) {
        this.isFieldAccessExpr = isNameExpr.isMethod(isNameExpr) + "isStringConstant" + isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
    }

    public isConstructor(Context isParameter, String isParameter, OnRetrieveRemoteAccountInterface isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        API isVariable = new API(this.isFieldAccessExpr.isMethod());
        isNameExpr = isNameExpr.isMethod(this.isFieldAccessExpr);
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }
}

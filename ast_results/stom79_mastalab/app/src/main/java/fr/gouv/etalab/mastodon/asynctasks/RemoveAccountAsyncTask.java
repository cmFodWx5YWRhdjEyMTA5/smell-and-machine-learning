// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.app.Activity;
import android.database.sqlite.SQLiteDatabase;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import fr.gouv.etalab.mastodon.client.Entities.Account;
import fr.gouv.etalab.mastodon.sqlite.AccountDAO;
import fr.gouv.etalab.mastodon.sqlite.Sqlite;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private WeakReference<Activity> isVariable;

    private Account isVariable;

    public isConstructor(Activity isParameter, Account isParameter) {
        isNameExpr = new WeakReference<>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        SQLiteDatabase isVariable = isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr.isFieldAccessExpr, null, isNameExpr.isFieldAccessExpr).isMethod();
        new AccountDAO(isNameExpr.isMethod(), isNameExpr).isMethod(isNameExpr);
        return null;
    }
}

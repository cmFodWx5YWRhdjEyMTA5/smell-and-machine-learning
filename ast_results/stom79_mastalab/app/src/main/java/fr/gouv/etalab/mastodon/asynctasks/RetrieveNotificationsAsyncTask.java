// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.content.Context;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import fr.gouv.etalab.mastodon.client.API;
import fr.gouv.etalab.mastodon.client.APIResponse;
import fr.gouv.etalab.mastodon.client.Entities.Account;
import fr.gouv.etalab.mastodon.client.Entities.Error;
import fr.gouv.etalab.mastodon.interfaces.OnRetrieveNotificationsInterface;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private APIResponse isVariable;

    private String isVariable;

    private Account isVariable;

    private OnRetrieveNotificationsInterface isVariable;

    private boolean isVariable;

    private WeakReference<Context> isVariable;

    private boolean isVariable;

    public isConstructor(Context isParameter, boolean isParameter, Account isParameter, String isParameter, OnRetrieveNotificationsInterface isParameter) {
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = true;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        API isVariable;
        if (isNameExpr == null) {
            isNameExpr = new API(this.isFieldAccessExpr.isMethod());
            isNameExpr = isNameExpr.isMethod(isNameExpr, isNameExpr);
        } else {
            if (this.isFieldAccessExpr.isMethod() == null) {
                isNameExpr.isMethod(new Error());
                return null;
            }
            isNameExpr = new API(this.isFieldAccessExpr.isMethod(), isNameExpr.isMethod(), isNameExpr.isMethod());
            isNameExpr = isNameExpr.isMethod(isNameExpr, isNameExpr);
        }
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr, isNameExpr);
    }
}

// isComment
package fr.gouv.etalab.mastodon.activities;

/*isComment*/
import android.app.Application;
import android.os.StrictMode;
import com.evernote.android.job.JobManager;
import fr.gouv.etalab.mastodon.jobs.ApplicationJob;
import fr.gouv.etalab.mastodon.jobs.HomeTimelineSyncJob;
import fr.gouv.etalab.mastodon.jobs.NotificationsSyncJob;

public class isClassOrIsInterface extends Application {

    @Override
    public void isMethod() {
        super.isMethod();
        isNameExpr.isMethod(this).isMethod(new ApplicationJob());
        isNameExpr.isMethod(true);
        isNameExpr.isMethod(true);
        StrictMode.VmPolicy.Builder isVariable = new StrictMode.VmPolicy.Builder();
        isNameExpr.isMethod(isNameExpr.isMethod());
    }
}

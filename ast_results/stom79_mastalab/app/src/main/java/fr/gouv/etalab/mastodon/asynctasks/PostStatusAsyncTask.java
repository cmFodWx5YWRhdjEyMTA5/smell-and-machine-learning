// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.content.Context;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import fr.gouv.etalab.mastodon.client.API;
import fr.gouv.etalab.mastodon.client.APIResponse;
import fr.gouv.etalab.mastodon.client.Entities.Account;
import fr.gouv.etalab.mastodon.interfaces.OnPostStatusActionInterface;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private OnPostStatusActionInterface isVariable;

    private APIResponse isVariable;

    private fr.gouv.etalab.mastodon.client.Entities.Status isVariable;

    private Account isVariable;

    private WeakReference<Context> isVariable;

    public isConstructor(Context isParameter, Account isParameter, fr.gouv.etalab.mastodon.client.Entities.Status isParameter, OnPostStatusActionInterface isParameter) {
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        if (isNameExpr == null)
            isNameExpr = new API(this.isFieldAccessExpr.isMethod()).isMethod(isNameExpr);
        else
            isNameExpr = new API(this.isFieldAccessExpr.isMethod(), isNameExpr.isMethod(), isNameExpr.isMethod()).isMethod(isNameExpr);
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.content.Context;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import fr.gouv.etalab.mastodon.client.API;
import fr.gouv.etalab.mastodon.client.APIResponse;
import fr.gouv.etalab.mastodon.interfaces.OnPostNotificationsActionInterface;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private OnPostNotificationsActionInterface isVariable;

    private APIResponse isVariable;

    private String isVariable;

    private WeakReference<Context> isVariable;

    public isConstructor(Context isParameter, String isParameter, OnPostNotificationsActionInterface isParameter) {
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        if (isNameExpr != null)
            isNameExpr = new API(this.isFieldAccessExpr.isMethod()).isMethod(isNameExpr);
        else
            // isComment
            isNameExpr = new API(this.isFieldAccessExpr.isMethod()).isMethod(null);
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

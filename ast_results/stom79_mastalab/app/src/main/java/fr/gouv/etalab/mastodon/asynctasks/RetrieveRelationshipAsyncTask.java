// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.content.Context;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import fr.gouv.etalab.mastodon.client.API;
import fr.gouv.etalab.mastodon.client.Entities.Relationship;
import fr.gouv.etalab.mastodon.interfaces.OnRetrieveRelationshipInterface;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private String isVariable;

    private Relationship isVariable;

    private OnRetrieveRelationshipInterface isVariable;

    private API isVariable;

    private WeakReference<Context> isVariable;

    public isConstructor(Context isParameter, String isParameter, OnRetrieveRelationshipInterface isParameter) {
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        isNameExpr = new API(this.isFieldAccessExpr.isMethod());
        isNameExpr = isNameExpr.isMethod(isNameExpr);
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod());
    }
}

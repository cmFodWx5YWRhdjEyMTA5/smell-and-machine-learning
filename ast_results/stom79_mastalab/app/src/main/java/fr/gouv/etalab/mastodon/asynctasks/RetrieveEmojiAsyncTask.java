// isComment
package fr.gouv.etalab.mastodon.asynctasks;

import android.content.Context;
import android.database.sqlite.SQLiteDatabase;
import android.os.AsyncTask;
import java.lang.ref.WeakReference;
import java.util.List;
import fr.gouv.etalab.mastodon.client.Entities.Emojis;
import fr.gouv.etalab.mastodon.interfaces.OnRetrieveEmojiInterface;
import fr.gouv.etalab.mastodon.sqlite.CustomEmojiDAO;
import fr.gouv.etalab.mastodon.sqlite.Sqlite;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private String isVariable;

    private List<Emojis> isVariable;

    private OnRetrieveEmojiInterface isVariable;

    private WeakReference<Context> isVariable;

    public isConstructor(Context isParameter, String isParameter, OnRetrieveEmojiInterface isParameter) {
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        SQLiteDatabase isVariable = isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr.isFieldAccessExpr, null, isNameExpr.isFieldAccessExpr).isMethod();
        isNameExpr = new CustomEmojiDAO(isNameExpr.isMethod(), isNameExpr).isMethod(isNameExpr);
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }
}

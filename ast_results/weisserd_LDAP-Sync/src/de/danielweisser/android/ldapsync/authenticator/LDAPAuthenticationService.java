// isComment
package de.danielweisser.android.ldapsync.authenticator;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;

/**
 * isComment
 */
public class isClassOrIsInterface extends Service {

    private LDAPAuthenticator isVariable;

    @Override
    public void isMethod() {
        isNameExpr = new LDAPAuthenticator(this);
    }

    @Override
    public void isMethod() {
    }

    @Override
    public IBinder isMethod(Intent isParameter) {
        return isNameExpr.isMethod();
    }
}

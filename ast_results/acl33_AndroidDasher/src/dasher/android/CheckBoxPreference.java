// isComment
package dasher.android;

import android.content.Context;
import android.content.SharedPreferences;
import android.preference.PreferenceManager;
import android.util.AttributeSet;

/**
 * isComment
 */
public class isClassOrIsInterface extends android.preference.CheckBoxPreference implements SharedPreferences.OnSharedPreferenceChangeListener {

    public isConstructor(Context isParameter, AttributeSet isParameter) {
        super(isNameExpr, isNameExpr);
        // isComment
        // isComment
        // isComment
        isNameExpr.isMethod(isNameExpr).isMethod(this);
    }

    public void isMethod(SharedPreferences isParameter, String isParameter) {
        if (isNameExpr.isMethod(isMethod())) {
            // isComment
            isMethod(true, null);
        }
    }
}

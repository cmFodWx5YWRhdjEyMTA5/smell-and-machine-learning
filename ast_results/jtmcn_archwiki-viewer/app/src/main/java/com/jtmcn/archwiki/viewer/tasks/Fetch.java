// isComment
package com.jtmcn.archwiki.viewer.tasks;

import android.os.AsyncTask;
import com.jtmcn.archwiki.viewer.data.SearchResult;
import com.jtmcn.archwiki.viewer.data.SearchResultsBuilder;
import com.jtmcn.archwiki.viewer.data.WikiPage;
import com.jtmcn.archwiki.viewer.data.WikiPageBuilder;
import java.util.List;

/**
 * isComment
 */
public class isClassOrIsInterface {

    public static final FetchUrl.FetchUrlMapper<List<SearchResult>> isVariable = new FetchUrl.FetchUrlMapper<List<SearchResult>>() {

        @Override
        public List<SearchResult> isMethod(String isParameter, StringBuilder isParameter) {
            return isNameExpr.isMethod(isNameExpr.isMethod());
        }
    };

    public static final FetchUrl.FetchUrlMapper<WikiPage> isVariable = new FetchUrl.FetchUrlMapper<WikiPage>() {

        @Override
        public WikiPage isMethod(String isParameter, StringBuilder isParameter) {
            return isNameExpr.isMethod(isNameExpr, isNameExpr);
        }
    };

    private isConstructor() {
    }

    /**
     * isComment
     */
    public static AsyncTask<String, Void, List<SearchResult>> isMethod(FetchUrl.OnFinish<List<SearchResult>> isParameter, String isParameter) {
        return new FetchUrl<>(isNameExpr, isNameExpr).isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    public static AsyncTask<String, Void, WikiPage> isMethod(FetchUrl.OnFinish<WikiPage> isParameter, String isParameter, boolean isParameter) {
        return new FetchUrl<>(isNameExpr, isNameExpr).isMethod(isNameExpr);
    }
}

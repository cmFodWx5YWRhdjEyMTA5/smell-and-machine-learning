// isComment
package com.google.zxing.oned.rss.expanded;

import java.awt.image.BufferedImage;
import java.io.IOException;
import java.nio.file.Path;
import javax.imageio.ImageIO;
import com.google.zxing.BinaryBitmap;
import com.google.zxing.BufferedImageLuminanceSource;
import com.google.zxing.common.AbstractBlackBoxTestCase;
import com.google.zxing.common.GlobalHistogramBinarizer;

final class isClassOrIsInterface {

    private isConstructor() {
    }

    private static BufferedImage isMethod(String isParameter) throws IOException {
        Path isVariable = isNameExpr.isMethod(isNameExpr);
        return isNameExpr.isMethod(isNameExpr.isMethod());
    }

    static BinaryBitmap isMethod(String isParameter) throws IOException {
        BufferedImage isVariable = isMethod(isNameExpr);
        BufferedImageLuminanceSource isVariable = new BufferedImageLuminanceSource(isNameExpr);
        return new BinaryBitmap(new GlobalHistogramBinarizer(isNameExpr));
    }
}

// isComment
package fr.free.nrw.commons.upload;

import com.pedrogomez.renderers.ListAdapteeCollection;
import com.pedrogomez.renderers.RVRendererAdapter;
import com.pedrogomez.renderers.RendererBuilder;
import java.util.Collections;
import java.util.List;
import fr.free.nrw.commons.category.CategoryClickedListener;
import fr.free.nrw.commons.category.CategoryItem;

public class isClassOrIsInterface {

    private final CategoryClickedListener isVariable;

    public isConstructor(CategoryClickedListener isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public RVRendererAdapter<CategoryItem> isMethod(List<CategoryItem> isParameter) {
        RendererBuilder<CategoryItem> isVariable = new RendererBuilder<CategoryItem>().isMethod(CategoryItem.class, new UploadCategoriesRenderer(isNameExpr));
        ListAdapteeCollection<CategoryItem> isVariable = new ListAdapteeCollection<>(isNameExpr != null ? isNameExpr : isNameExpr.isMethod());
        return new RVRendererAdapter<>(isNameExpr, isNameExpr);
    }
}

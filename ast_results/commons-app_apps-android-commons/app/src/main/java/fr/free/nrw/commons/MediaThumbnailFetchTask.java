// isComment
package fr.free.nrw.commons;

import android.os.AsyncTask;
import android.support.annotation.NonNull;
import fr.free.nrw.commons.mwapi.MediaWikiApi;

class isClassOrIsInterface extends AsyncTask<String, String, String> {

    protected final Media isVariable;

    private MediaWikiApi isVariable;

    public isConstructor(@NonNull Media isParameter, MediaWikiApi isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected String isMethod(String... isParameter) {
        try {
            return isNameExpr.isMethod(isNameExpr[isIntegerConstant]);
        } catch (Exception isParameter) {
        // isComment
        }
        return null;
    }
}

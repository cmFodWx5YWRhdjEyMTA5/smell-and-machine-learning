// isComment
package de.saschahlusiak.freebloks.view.effects;

import java.util.ArrayList;
import javax.microedition.khronos.opengles.GL11;
import de.saschahlusiak.freebloks.view.BoardRenderer;

public class isClassOrIsInterface extends ArrayList<AbsStoneEffect> implements Effect {

    private static final long isVariable = isStringConstant;

    @Override
    public boolean isMethod(int isParameter, int isParameter) {
        if (isMethod() > isIntegerConstant)
            return isMethod(isIntegerConstant).isMethod(isNameExpr, isNameExpr);
        return true;
    }

    @Override
    public void isMethod(GL11 isParameter, BoardRenderer isParameter) {
        if (isMethod() > isIntegerConstant)
            isMethod(isIntegerConstant).isMethod(isNameExpr, isNameExpr);
    }

    @Override
    public void isMethod(GL11 isParameter, BoardRenderer isParameter) {
        if (isMethod() > isIntegerConstant)
            isMethod(isIntegerConstant).isMethod(isNameExpr, isNameExpr);
    }

    @Override
    public boolean isMethod() {
        return isMethod() == isIntegerConstant;
    }

    @Override
    public boolean isMethod(float isParameter) {
        if (isMethod() > isIntegerConstant) {
            boolean isVariable = isMethod(isIntegerConstant).isMethod(isNameExpr);
            if (isMethod(isIntegerConstant).isMethod()) {
                isMethod(isIntegerConstant);
                /*isComment*/
                return true;
            }
            return isNameExpr;
        }
        return true;
    }
}

// isComment
package de.saschahlusiak.freebloks.network;

import android.os.Handler;
import android.os.Looper;
import android.os.Message;
import java.net.Socket;

public class isClassOrIsInterface extends Handler {

    Socket isVariable;

    public isConstructor(Socket isParameter, Looper isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public void isMethod(Message isParameter) {
        NET_HEADER isVariable = (NET_HEADER) isNameExpr.isFieldAccessExpr;
        isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package com.android.calendar;

import android.support.v7.app.AppCompatActivity;

public abstract class isClassOrIsInterface extends AppCompatActivity {

    protected AsyncQueryService isVariable;

    public synchronized AsyncQueryService isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new AsyncQueryService(this);
        }
        return isNameExpr;
    }
}

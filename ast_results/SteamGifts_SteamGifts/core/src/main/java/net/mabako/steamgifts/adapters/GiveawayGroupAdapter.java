// isComment
package net.mabako.steamgifts.adapters;

import android.content.Context;
import android.support.v7.widget.RecyclerView;
import android.view.View;
import net.mabako.steamgifts.adapters.viewholder.GiveawayGroupViewHolder;
import net.mabako.steamgifts.data.GiveawayGroup;
import java.util.List;

public class isClassOrIsInterface extends EndlessAdapter {

    private static final long isVariable = -isStringConstant;

    /**
     * isComment
     */
    // isComment
    private static final int isVariable = isIntegerConstant;

    /**
     * isComment
     */
    private transient Context isVariable;

    public isConstructor() {
        this.isFieldAccessExpr = true;
    }

    public void isMethod(OnLoadListener isParameter, Context isParameter) {
        isMethod(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected RecyclerView.ViewHolder isMethod(View isParameter, int isParameter) {
        if (isNameExpr == null)
            throw new IllegalStateException("isStringConstant");
        return new GiveawayGroupViewHolder(isNameExpr, isNameExpr);
    }

    @Override
    protected void isMethod(RecyclerView.ViewHolder isParameter, int isParameter) {
        if (isNameExpr instanceof GiveawayGroupViewHolder) {
            GiveawayGroupViewHolder isVariable = (GiveawayGroupViewHolder) isNameExpr;
            GiveawayGroup isVariable = (GiveawayGroup) isMethod(isNameExpr);
            isNameExpr.isMethod(isNameExpr);
        }
    }

    @Override
    protected boolean isMethod(List<IEndlessAdaptable> isParameter) {
        return isNameExpr.isMethod() == isNameExpr;
    }
}

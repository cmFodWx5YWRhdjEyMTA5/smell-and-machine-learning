// isComment
package org.dmfs.opentaskspal.tasklists;

import android.support.annotation.NonNull;
import org.dmfs.android.contentpal.RowData;
import org.dmfs.android.contentpal.rowdata.CharSequenceRowData;
import org.dmfs.android.contentpal.rowdata.DelegatingRowData;
import org.dmfs.tasks.contract.TaskContract;

/**
 * isComment
 */
public final class isClassOrIsInterface extends DelegatingRowData<TaskContract.TaskLists> {

    public isConstructor(@NonNull CharSequence isParameter) {
        // isComment
        super(new CharSequenceRowData<TaskContract.TaskLists>(isNameExpr.isFieldAccessExpr.isFieldAccessExpr, isNameExpr));
    }
}

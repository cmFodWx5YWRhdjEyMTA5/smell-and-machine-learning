// isComment
package org.dmfs.tasks.utils.permission;

import android.app.Activity;
import android.content.Context;
import android.support.v4.content.ContextCompat;
import static android.content.pm.PackageManager.PERMISSION_GRANTED;

/**
 * isComment
 */
final class isClassOrIsInterface implements AppPermissions {

    private final Context isVariable;

    isConstructor(Context isParameter) {
        isNameExpr = isNameExpr.isMethod();
    }

    @Override
    public Permission isMethod(String isParameter) {
        return new LegacyPermission(isNameExpr, isNameExpr.isMethod(isNameExpr, isNameExpr) == isNameExpr);
    }

    /**
     * isComment
     */
    private static final class isClassOrIsInterface implements Permission {

        private final String isVariable;

        private final boolean isVariable;

        isConstructor(String isParameter, boolean isParameter) {
            isNameExpr = isNameExpr;
            isNameExpr = isNameExpr;
        }

        @Override
        public String isMethod() {
            return isNameExpr;
        }

        @Override
        public boolean isMethod() {
            return isNameExpr;
        }

        @Override
        public boolean isMethod(Activity isParameter) {
            return isMethod();
        }

        @Override
        public boolean isMethod() {
            return true;
        }

        @Override
        public PermissionRequest isMethod() {
            return new NoOpPermissionRequest();
        }
    }
}

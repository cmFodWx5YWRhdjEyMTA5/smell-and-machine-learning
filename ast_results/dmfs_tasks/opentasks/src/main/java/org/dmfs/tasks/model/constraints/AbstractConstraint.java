// isComment
package org.dmfs.tasks.model.constraints;

import org.dmfs.tasks.model.ContentSet;

/**
 * isComment
 */
public abstract class isClassOrIsInterface<T> {

    /**
     * isComment
     */
    public abstract T isMethod(ContentSet isParameter, T isParameter, T isParameter);
}

// isComment
package org.dmfs.tasks.utils;

import android.content.Context;
import android.os.AsyncTask;
import org.dmfs.tasks.model.Model;
import org.dmfs.tasks.model.Sources;
import java.lang.ref.WeakReference;

/**
 * isComment
 */
public class isClassOrIsInterface extends AsyncTask<String, Void, Model> {

    /**
     * isComment
     */
    private WeakReference<OnModelLoadedListener> isVariable;

    /**
     * isComment
     */
    private WeakReference<Context> isVariable;

    /**
     * isComment
     */
    public isConstructor(Context isParameter, OnModelLoadedListener isParameter) {
        isNameExpr = new WeakReference<Context>(isNameExpr);
        isNameExpr = new WeakReference<OnModelLoadedListener>(isNameExpr);
    }

    @Override
    protected final Model isMethod(String... isParameter) {
        final OnModelLoadedListener isVariable = isNameExpr.isMethod();
        final Context isVariable = isNameExpr.isMethod();
        if (isNameExpr != null) {
            Sources isVariable = isNameExpr.isMethod(isNameExpr);
            Model isVariable = isNameExpr.isMethod(isNameExpr[isIntegerConstant]);
            if (isNameExpr == null) {
                isNameExpr = isNameExpr.isMethod();
            }
            return isNameExpr;
        } else {
            return null;
        }
    }

    @Override
    protected final void isMethod(Model isParameter) {
        final OnModelLoadedListener isVariable = isNameExpr.isMethod();
        if (isNameExpr != null) {
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

// isComment
package org.dmfs.tasks.groupings.cursorloaders;

import android.content.Context;
import android.database.Cursor;
import org.dmfs.tasks.utils.SearchHistoryHelper;

/**
 * isComment
 */
public final class isClassOrIsInterface extends AbstractCustomCursorFactory {

    private final SearchHistoryHelper isVariable;

    /**
     * isComment
     */
    public isConstructor(Context isParameter, String[] isParameter, SearchHistoryHelper isParameter) {
        super(isNameExpr);
        isNameExpr = isNameExpr;
    }

    @Override
    public Cursor isMethod() {
        return isNameExpr.isMethod();
    }
}

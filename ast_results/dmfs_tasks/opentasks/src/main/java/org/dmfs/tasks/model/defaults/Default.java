// isComment
package org.dmfs.tasks.model.defaults;

import org.dmfs.tasks.model.ContentSet;

/**
 * isComment
 */
public interface isClassOrIsInterface<T> {

    /**
     * isComment
     */
    T isMethod(ContentSet isParameter, T isParameter);
}

// isComment
package org.dmfs.tasks.utils;

import android.net.Uri;
import android.support.annotation.Nullable;
import java.net.URI;
import java.net.URISyntaxException;

/**
 * isComment
 */
public final class isClassOrIsInterface implements Fragile<Uri, URISyntaxException> {

    private final CharSequence isVariable;

    public isConstructor(@Nullable CharSequence isParameter) {
        isNameExpr = isNameExpr;
    }

    @Override
    public Uri isMethod() throws URISyntaxException {
        if (isNameExpr == null) {
            throw new URISyntaxException("isStringConstant", "isStringConstant");
        }
        if (isNameExpr.isMethod() == isIntegerConstant) {
            throw new URISyntaxException("isStringConstant", "isStringConstant");
        }
        return isNameExpr.isMethod(new URI(isNameExpr.isMethod()).isMethod());
    }
}

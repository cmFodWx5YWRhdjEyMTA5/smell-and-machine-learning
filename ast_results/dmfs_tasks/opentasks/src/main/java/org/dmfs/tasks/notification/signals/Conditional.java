// isComment
package org.dmfs.tasks.notification.signals;

import android.content.Context;

/**
 * isComment
 */
public final class isClassOrIsInterface implements NotificationSignal {

    private final boolean isVariable;

    private final NotificationSignal isVariable;

    public isConstructor(boolean isParameter, NotificationSignal isParameter) {
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    public isConstructor(boolean isParameter, Context isParameter) {
        this(isNameExpr, new SettingsSignal(isNameExpr));
    }

    @Override
    public int isMethod() {
        return (isNameExpr ? isNameExpr : new NoSignal()).isMethod();
    }
}

// isComment
package org.dmfs.tasks.groupings.cursorloaders;

import android.content.Context;
import android.database.Cursor;
import android.support.v4.content.Loader;

/**
 * isComment
 */
public class isClassOrIsInterface extends Loader<Cursor> {

    /**
     * isComment
     */
    private Cursor isVariable;

    /**
     * isComment
     */
    private final AbstractCustomCursorFactory isVariable;

    public isConstructor(Context isParameter, AbstractCustomCursorFactory isParameter) {
        super(isNameExpr);
        isNameExpr = isNameExpr;
    }

    @Override
    public void isMethod(Cursor isParameter) {
        if (isMethod()) {
            // isComment
            if (isNameExpr != null && !isNameExpr.isMethod()) {
                isNameExpr.isMethod();
            }
            return;
        }
        Cursor isVariable = isNameExpr;
        isNameExpr = isNameExpr;
        if (isMethod()) {
            super.isMethod(isNameExpr);
        }
        if (isNameExpr != null && isNameExpr != isNameExpr && !isNameExpr.isMethod()) {
            isNameExpr.isMethod();
        }
    }

    @Override
    protected void isMethod() {
        if (isNameExpr == null || isMethod()) {
            // isComment
            isMethod(isNameExpr.isMethod());
        } else {
            // isComment
            isMethod(isNameExpr);
        }
    }

    @Override
    protected void isMethod() {
        // isComment
        isMethod(isNameExpr.isMethod());
    }

    @Override
    protected void isMethod() {
        super.isMethod();
        isMethod();
        // isComment
        if (isNameExpr != null && !isNameExpr.isMethod()) {
            isNameExpr.isMethod();
        }
        isNameExpr = null;
    }
}

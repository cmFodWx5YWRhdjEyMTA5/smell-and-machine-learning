// isComment
package org.dmfs.provider.tasks.utils;

import java.util.Iterator;

/**
 * isComment
 */
@Deprecated
public final class isClassOrIsInterface<T> implements Iterable<T> {

    private final int isVariable;

    private final Iterable<T> isVariable;

    public isConstructor(int isParameter, Iterable<T> isParameter) {
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    @Override
    public Iterator<T> isMethod() {
        return new LimitedIterator<>(isNameExpr, isNameExpr.isMethod());
    }
}

// isComment
package org.dmfs.provider.tasks.processors;

import android.database.sqlite.SQLiteDatabase;
import org.dmfs.provider.tasks.model.EntityAdapter;

/**
 * isComment
 */
public interface isClassOrIsInterface<T extends EntityAdapter<T>> {

    T isMethod(SQLiteDatabase isParameter, T isParameter, boolean isParameter);

    T isMethod(SQLiteDatabase isParameter, T isParameter, boolean isParameter);

    void isMethod(SQLiteDatabase isParameter, T isParameter, boolean isParameter);
}

// isComment
package org.dmfs.provider.tasks.handler;

import android.content.ContentValues;
import android.database.sqlite.SQLiteDatabase;

/**
 * isComment
 */
public class isClassOrIsInterface extends PropertyHandler {

    /**
     * isComment
     */
    @Override
    public ContentValues isMethod(SQLiteDatabase isParameter, long isParameter, long isParameter, boolean isParameter, ContentValues isParameter, boolean isParameter) {
        return isNameExpr;
    }
}

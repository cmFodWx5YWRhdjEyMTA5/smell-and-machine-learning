// isComment
package com.hasi.d00r;

import java.io.DataOutputStream;
import java.io.File;
import java.io.FileInputStream;
import java.io.OutputStream;
import java.net.InetAddress;
import java.net.Socket;
import org.bouncycastle.util.io.Streams;
import android.app.AlertDialog;
import android.content.Context;
import android.graphics.drawable.Drawable;
import android.os.AsyncTask;
import android.os.Vibrator;
import android.widget.Button;

public class isClassOrIsInterface extends AsyncTask<Object, Void, String> {

    private Exception isVariable = null;

    public Context isVariable = null;

    public int isVariable = isIntegerConstant;

    @Override
    protected String isMethod(Object... isParameter) {
        if (isNameExpr != null) {
            Vibrator isVariable = (Vibrator) isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
            isNameExpr.isMethod(isNameExpr);
        }
        return null;
    }

    @Override
    protected void isMethod(String isParameter) {
        if (isNameExpr != null) {
            isNameExpr.isMethod();
        }
    }

    public Exception isMethod() {
        return isNameExpr;
    }
}

// isComment
package org.pixmob.freemobile.netstat.feature;

import android.annotation.TargetApi;
import android.app.backup.BackupManager;
import android.content.Context;
import android.os.Build;

/**
 * isComment
 */
@TargetApi(isNameExpr.isFieldAccessExpr.isFieldAccessExpr)
public class isClassOrIsInterface implements BackupManagerFeature {

    @Override
    public void isMethod(Context isParameter) {
        final BackupManager isVariable = new BackupManager(isNameExpr);
        isNameExpr.isMethod();
    }
}

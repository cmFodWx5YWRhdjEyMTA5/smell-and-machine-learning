// isComment
package jp.redmine.redmineclient.external.lib;

import java.util.LinkedHashMap;

/**
 * isComment
 */
public class isClassOrIsInterface<K, V> extends LinkedHashMap<K, V> {

    private static final long isVariable = isStringConstant;

    private int isVariable;

    public isConstructor(int isParameter) {
        super(isNameExpr, (float) isDoubleConstant, true);
        this.isFieldAccessExpr = isNameExpr;
    }

    protected boolean isMethod(Entry<K, V> isParameter) {
        return isMethod() > isNameExpr;
    }

    public interface isClassOrIsInterface<K> {

        public Object isMethod(K isParameter);
    }

    public V isMethod(K isParameter, IFetchObject<K> isParameter) {
        if (isMethod(isNameExpr)) {
            return isMethod(isNameExpr);
        } else {
            @SuppressWarnings("isStringConstant")
            V isVariable = (V) isNameExpr.isMethod(isNameExpr);
            isMethod(isNameExpr, isNameExpr);
            return isNameExpr;
        }
    }
}

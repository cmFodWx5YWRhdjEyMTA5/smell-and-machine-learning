// isComment
package jp.redmine.redmineclient.model;

import jp.redmine.redmineclient.db.cache.DatabaseCacheHelper;
import jp.redmine.redmineclient.db.store.DatabaseHelper;
import android.content.Context;

public abstract class isClassOrIsInterface {

    protected DatabaseHelper isVariable;

    protected DatabaseCacheHelper isVariable;

    public isConstructor(Context isParameter) {
        isNameExpr = new DatabaseCacheHelper(isNameExpr);
        isNameExpr = new DatabaseHelper(isNameExpr);
    }

    public void isMethod() {
        if (isNameExpr != null) {
            isNameExpr.isMethod();
            isNameExpr = null;
        }
        if (isNameExpr != null) {
            isNameExpr.isMethod();
            isNameExpr = null;
        }
    }

    @Override
    protected void isMethod() throws Throwable {
        isMethod();
        super.isMethod();
    }
}

// isComment
package com.matburt.mobileorg.synchronizers;

import android.app.ProgressDialog;
import android.content.Context;
import android.os.AsyncTask;

public abstract class isClassOrIsInterface<Params, Void, Result> extends AsyncTask<Params, Void, Result> {

    protected Context isVariable;

    private ProgressDialog isVariable;

    public isConstructor(Context isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    protected abstract void isMethod();

    protected abstract void isMethod(Result isParameter);

    @Override
    protected void isMethod() {
        super.isMethod();
        isMethod();
    // isComment
    // isComment
    // isComment
    // isComment
    }

    @Override
    protected void isMethod(Result isParameter) {
        // isComment
        isMethod(isNameExpr);
    }
}

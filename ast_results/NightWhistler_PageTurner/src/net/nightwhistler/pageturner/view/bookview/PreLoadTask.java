// isComment
package net.nightwhistler.pageturner.view.bookview;

import android.text.Spannable;
import jedi.option.None;
import jedi.option.Option;
import net.nightwhistler.pageturner.epub.PageTurnerSpine;
import net.nightwhistler.pageturner.scheduling.QueueableAsyncTask;
import nl.siegmann.epublib.domain.Resource;
import static jedi.functional.FunctionalPrimitives.isEmpty;
import static jedi.option.Options.none;

/**
 * isComment
 */
public class isClassOrIsInterface extends QueueableAsyncTask<Void, Void, Void> {

    private PageTurnerSpine isVariable;

    private TextLoader isVariable;

    public isConstructor(PageTurnerSpine isParameter, TextLoader isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public Option<Void> isMethod(Void... isParameter) {
        isMethod();
        return isMethod();
    }

    private void isMethod() {
        if (isNameExpr == null) {
            return;
        }
        Option<Resource> isVariable = isNameExpr.isMethod();
        isNameExpr.isMethod(isParameter -> {
            Option<Spannable> isVariable = isNameExpr.isMethod(isNameExpr);
            if (isMethod(isNameExpr)) {
                try {
                    isNameExpr.isMethod(isNameExpr, isNameExpr.this::isCancelled);
                } catch (Exception | OutOfMemoryError isParameter) {
                // isComment
                }
            }
        });
    }
}

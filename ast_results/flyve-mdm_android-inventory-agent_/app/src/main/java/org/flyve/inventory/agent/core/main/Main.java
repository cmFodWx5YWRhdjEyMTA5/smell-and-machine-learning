// isComment
package org.flyve.inventory.agent.core.main;

import android.app.Activity;
import android.content.Context;
import android.support.v4.app.FragmentManager;
import android.widget.ListView;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

public interface isClassOrIsInterface {

    interface isClassOrIsInterface {

        void isMethod(String isParameter);
    }

    interface isClassOrIsInterface {

        // isComment
        void isMethod(String isParameter);

        // isComment
        Map<String, String> isMethod(Activity isParameter, ListView isParameter);

        void isMethod(FragmentManager isParameter, android.support.v7.widget.Toolbar isParameter, Map<String, String> isParameter);

        List<HashMap<String, String>> isMethod();

        void isMethod(Context isParameter);
    }

    interface isClassOrIsInterface {

        Map<String, String> isMethod(Activity isParameter, ListView isParameter);

        void isMethod(FragmentManager isParameter, android.support.v7.widget.Toolbar isParameter, Map<String, String> isParameter);

        List<HashMap<String, String>> isMethod();

        void isMethod(Context isParameter);
    }
}

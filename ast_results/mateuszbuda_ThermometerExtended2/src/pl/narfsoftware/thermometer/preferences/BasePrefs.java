// isComment
package pl.narfsoftware.thermometer.preferences;

import pl.narfsoftware.thermometer.utils.Listener;
import android.content.Context;
import android.content.SharedPreferences;
import android.content.SharedPreferences.OnSharedPreferenceChangeListener;
import android.content.res.Resources;
import android.preference.PreferenceManager;

public abstract class isClassOrIsInterface implements Listener, OnSharedPreferenceChangeListener {

    protected Context isVariable;

    protected SharedPreferences isVariable;

    protected Resources isVariable;

    /**
     * isComment
     */
    protected String isVariable;

    public isConstructor(Context isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        isNameExpr = isNameExpr.isMethod(isNameExpr);
        isNameExpr = isNameExpr.isMethod();
    }

    @Override
    public boolean isMethod() {
        isNameExpr.isMethod(this);
        return true;
    }

    @Override
    public void isMethod() {
        isNameExpr.isMethod(this);
    }
}

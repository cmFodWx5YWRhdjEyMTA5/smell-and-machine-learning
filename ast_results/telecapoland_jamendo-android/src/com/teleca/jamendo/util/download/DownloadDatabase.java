// isComment
package com.teleca.jamendo.util.download;

import java.util.ArrayList;
import com.teleca.jamendo.api.PlaylistEntry;
import com.teleca.jamendo.api.Track;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    public boolean isMethod(PlaylistEntry isParameter);

    /**
     * isComment
     */
    public void isMethod(PlaylistEntry isParameter, boolean isParameter);

    /**
     * isComment
     */
    public boolean isMethod(Track isParameter);

    /**
     * isComment
     */
    public ArrayList<DownloadJob> isMethod();

    /**
     * isComment
     */
    public void isMethod(DownloadJob isParameter);
}

// isComment
package com.ichi2.anki.multimediacard.fields;

import android.content.Context;
import android.content.Intent;
import android.widget.LinearLayout;
import com.ichi2.anki.multimediacard.IMultimediaEditableNote;
import com.ichi2.anki.multimediacard.activity.MultimediaEditFieldActivity;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    // isComment
    // isComment
    void isMethod(IField isParameter);

    // isComment
    // isComment
    void isMethod(IMultimediaEditableNote isParameter);

    // isComment
    // isComment
    void isMethod(int isParameter);

    // isComment
    void isMethod(MultimediaEditFieldActivity isParameter);

    // isComment
    void isMethod(Context isParameter, LinearLayout isParameter);

    // isComment
    // isComment
    void isMethod(int isParameter, int isParameter, Intent isParameter);

    // isComment
    void isMethod();

    // isComment
    void isMethod();

    // isComment
    void isMethod();
}

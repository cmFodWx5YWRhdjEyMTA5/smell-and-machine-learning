// isComment
package info.guardianproject.otr.app.im.engine;

// isComment
import info.guardianproject.otr.app.im.plugin.ImConfigNames;
import info.guardianproject.otr.app.im.plugin.xmpp.LLXmppConnection;
import info.guardianproject.otr.app.im.plugin.xmpp.XmppConnection;
import java.util.Map;
import android.content.Context;

/**
 * isComment
 */
public class isClassOrIsInterface {

    private static ConnectionFactory isVariable;

    private isConstructor() {
    }

    /**
     * isComment
     */
    public static synchronized ConnectionFactory isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new ConnectionFactory();
        }
        return isNameExpr;
    }

    /**
     * isComment
     */
    public synchronized ImConnection isMethod(Map<String, String> isParameter, Context isParameter) throws ImException {
        String isVariable = isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
        if ("isStringConstant".isMethod(isNameExpr)) {
            try {
                return new XmppConnection(isNameExpr);
            } catch (Exception isParameter) {
                throw new ImException(isNameExpr.isMethod());
            }
        }
        if ("isStringConstant".isMethod(isNameExpr)) {
            return new LLXmppConnection(isNameExpr);
        } else /*isComment*/
        {
            throw new ImException("isStringConstant" + isNameExpr);
        }
    }
}

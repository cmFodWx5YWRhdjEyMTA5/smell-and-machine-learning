// isComment
package info.guardianproject.otr.app.im.app;

import info.guardianproject.otr.app.im.plugin.BrandingResourceIDs;
import android.graphics.drawable.Drawable;
import android.text.Spannable;
import android.text.SpannableString;
import android.text.style.ImageSpan;
import android.text.util.Linkify;

public class isClassOrIsInterface {

    private BrandingResources isVariable;

    public isConstructor(BrandingResources isParameter) {
        isNameExpr = isNameExpr;
    // isComment
    // isComment
    }

    public final CharSequence isMethod(CharSequence isParameter) {
        SpannableString isVariable;
        if (isNameExpr instanceof SpannableString) {
            isNameExpr = (SpannableString) isNameExpr;
        } else {
            isNameExpr = new SpannableString(isNameExpr);
        }
        isNameExpr.isMethod(isNameExpr, isNameExpr.isFieldAccessExpr);
        return isNameExpr;
    }
}

// isComment
package info.guardianproject.otr.app.im.engine;

import info.guardianproject.otr.app.im.IDataListener;
import java.io.IOException;
import java.util.Map;
import net.java.otr4j.session.SessionStatus;

public interface isClassOrIsInterface {

    /**
     * isComment
     */
    void isMethod(Address isParameter, Address isParameter, byte[] isParameter);

    /**
     * isComment
     */
    void isMethod(Address isParameter, Address isParameter, byte[] isParameter);

    /**
     * isComment
     */
    void isMethod(String isParameter, Address isParameter, String isParameter, Map<String, String> isParameter) throws IOException;

    void isMethod(IDataListener isParameter);

    void isMethod(SessionStatus isParameter);
}

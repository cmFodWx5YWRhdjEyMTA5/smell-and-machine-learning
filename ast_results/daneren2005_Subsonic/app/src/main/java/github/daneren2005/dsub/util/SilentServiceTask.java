// isComment
package github.daneren2005.dsub.util;

import android.content.Context;
import github.daneren2005.dsub.service.MusicService;
import github.daneren2005.dsub.service.MusicServiceFactory;

public abstract class isClassOrIsInterface<T> extends SilentBackgroundTask<T> {

    protected MusicService isVariable;

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    @Override
    protected T isMethod() throws Throwable {
        isNameExpr = isNameExpr.isMethod(isMethod());
        return isMethod(isNameExpr);
    }

    protected abstract T isMethod(MusicService isParameter) throws Throwable;
}

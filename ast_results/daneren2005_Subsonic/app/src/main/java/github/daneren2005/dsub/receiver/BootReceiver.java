// isComment
package github.daneren2005.dsub.receiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import github.daneren2005.dsub.service.HeadphoneListenerService;
import github.daneren2005.dsub.util.Util;

public class isClassOrIsInterface extends BroadcastReceiver {

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        if (isNameExpr.isMethod(isNameExpr)) {
            Intent isVariable = new Intent();
            isNameExpr.isMethod(isNameExpr.isMethod(), HeadphoneListenerService.class.isMethod());
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

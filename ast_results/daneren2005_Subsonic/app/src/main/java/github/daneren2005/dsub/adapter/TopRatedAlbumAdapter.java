// isComment
package github.daneren2005.dsub.adapter;

import android.content.Context;
import java.util.List;
import github.daneren2005.dsub.domain.MusicDirectory;
import github.daneren2005.dsub.util.ImageLoader;
import github.daneren2005.dsub.view.FastScroller;

public class isClassOrIsInterface extends EntryInfiniteGridAdapter implements FastScroller.BubbleTextGetter {

    public isConstructor(Context isParameter, List<MusicDirectory.Entry> isParameter, ImageLoader isParameter, boolean isParameter) {
        super(isNameExpr, isNameExpr, isNameExpr, isNameExpr);
    }

    @Override
    public String isMethod(int isParameter) {
        // isComment
        if (isNameExpr >= isNameExpr.isMethod(isIntegerConstant).isMethod()) {
            if (isNameExpr.isMethod(isIntegerConstant).isMethod() > isIntegerConstant) {
                return isMethod(isNameExpr - isIntegerConstant);
            } else {
                return "isStringConstant";
            }
        } else {
            return isNameExpr.isMethod(isMethod(isNameExpr).isMethod());
        }
    }
}

// isComment
package org.y20k.transistor.adapter;

import android.app.Application;
import android.content.Context;
import android.content.SharedPreferences;
import android.os.AsyncTask;
import android.preference.PreferenceManager;
import org.y20k.transistor.core.Station;
import org.y20k.transistor.helpers.LogHelper;
import org.y20k.transistor.helpers.StationListHelper;
import org.y20k.transistor.helpers.TransistorKeys;
import java.util.ArrayList;
import androidx.lifecycle.AndroidViewModel;
import androidx.lifecycle.MutableLiveData;

/**
 * isComment
 */
public class isClassOrIsInterface extends AndroidViewModel implements TransistorKeys {

    /*isComment*/
    private static final String isVariable = CollectionViewModel.class.isMethod();

    /*isComment*/
    private final MutableLiveData<ArrayList<Station>> isVariable;

    private final MutableLiveData<Station> isVariable;

    private final MutableLiveData<Boolean> isVariable;

    /*isComment*/
    public isConstructor(Application isParameter) {
        super(isNameExpr);
        // isComment
        isNameExpr = new MutableLiveData<ArrayList<Station>>();
        isNameExpr = new MutableLiveData<Station>();
        isNameExpr = new MutableLiveData<Boolean>();
        // isComment
        isMethod(isNameExpr);
        // isComment
        isNameExpr.isMethod(null);
        // isComment
        isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr));
    // isComment
    // isComment
    }

    @Override
    protected void isMethod() {
        super.isMethod();
    }

    /*isComment*/
    private void isMethod(Context isParameter) {
        SharedPreferences isVariable = isNameExpr.isMethod(isNameExpr);
        isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr, true));
        isNameExpr.isMethod(isNameExpr, "isStringConstant");
    }

    /*isComment*/
    public MutableLiveData<ArrayList<Station>> isMethod() {
        return isNameExpr;
    }

    /*isComment*/
    public MutableLiveData<Station> isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    class isClassOrIsInterface extends AsyncTask<Context, Void, ArrayList<Station>> {

        @Override
        protected ArrayList<Station> isMethod(Context... isParameter) {
            // isComment
            return isNameExpr.isMethod(isNameExpr[isIntegerConstant]);
        }

        @Override
        protected void isMethod(ArrayList<Station> isParameter) {
            // isComment
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

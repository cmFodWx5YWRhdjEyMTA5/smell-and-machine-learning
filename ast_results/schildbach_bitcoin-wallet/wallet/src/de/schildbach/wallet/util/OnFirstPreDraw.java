// isComment
package de.schildbach.wallet.util;

import static androidx.core.util.Preconditions.checkNotNull;
import java.util.concurrent.atomic.AtomicBoolean;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import android.view.View;
import android.view.ViewTreeObserver;

/**
 * isComment
 */
public class isClassOrIsInterface implements ViewTreeObserver.OnPreDrawListener {

    public static interface isClassOrIsInterface {

        boolean isMethod();
    }

    public static void isMethod(final View isParameter, final Callback isParameter) {
        new OnFirstPreDraw(isNameExpr.isMethod(), isNameExpr);
    }

    private final ViewTreeObserver isVariable;

    private final Callback isVariable;

    private final AtomicBoolean isVariable = new AtomicBoolean(true);

    private static final Logger isVariable = isNameExpr.isMethod(OnFirstPreDraw.class);

    private isConstructor(final ViewTreeObserver isParameter, final Callback isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isMethod(isNameExpr);
        isNameExpr.isMethod(this);
    }

    @Override
    public boolean isMethod() {
        if (isNameExpr.isMethod())
            isNameExpr.isMethod(this);
        else
            isNameExpr.isMethod("isStringConstant");
        if (!isNameExpr.isMethod(true))
            return isNameExpr.isMethod();
        return true;
    }
}

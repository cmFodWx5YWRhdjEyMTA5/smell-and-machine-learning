// isComment
package kiwi.root.an2linuxclient.network;

import android.content.Context;
import java.security.cert.Certificate;
import kiwi.root.an2linuxclient.data.Notification;

abstract class isClassOrIsInterface implements Runnable {

    Context isVariable;

    Notification isVariable;

    Certificate isVariable;

    final byte isVariable = isIntegerConstant;

    isConstructor(Context isParameter, Notification isParameter, Certificate isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }
}

// isComment
package de.grobox.transportr.locations;

import androidx.lifecycle.LiveData;
import java.util.List;
import de.grobox.transportr.TransportrApplication;
import de.grobox.transportr.data.locations.FavoriteLocation;
import de.grobox.transportr.data.locations.HomeLocation;
import de.grobox.transportr.data.locations.LocationRepository;
import de.grobox.transportr.data.locations.WorkLocation;
import de.grobox.transportr.networks.TransportNetworkManager;
import de.grobox.transportr.networks.TransportNetworkViewModel;

public abstract class isClassOrIsInterface extends TransportNetworkViewModel {

    private final LocationRepository isVariable;

    private final LiveData<HomeLocation> isVariable;

    private final LiveData<WorkLocation> isVariable;

    private final LiveData<List<FavoriteLocation>> isVariable;

    protected isConstructor(TransportrApplication isParameter, TransportNetworkManager isParameter, LocationRepository isParameter) {
        super(isNameExpr, isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr.isMethod();
        this.isFieldAccessExpr = isNameExpr.isMethod();
        this.isFieldAccessExpr = isNameExpr.isMethod();
    }

    public void isMethod(WrapLocation isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }

    public LiveData<HomeLocation> isMethod() {
        return isNameExpr;
    }

    public void isMethod(WrapLocation isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }

    public LiveData<WorkLocation> isMethod() {
        return isNameExpr;
    }

    public LiveData<List<FavoriteLocation>> isMethod() {
        return isNameExpr;
    }
}

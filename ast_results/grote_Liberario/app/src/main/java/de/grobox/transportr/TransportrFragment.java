// isComment
package de.grobox.transportr;

import android.content.Context;
import androidx.annotation.NonNull;
import androidx.fragment.app.Fragment;
import androidx.appcompat.app.ActionBar;
import androidx.appcompat.widget.Toolbar;
import javax.annotation.ParametersAreNonnullByDefault;

@ParametersAreNonnullByDefault
public abstract class isClassOrIsInterface extends Fragment {

    protected AppComponent isMethod() {
        if (isMethod() == null)
            throw new IllegalStateException();
        return ((TransportrApplication) isMethod().isMethod()).isMethod();
    }

    protected void isMethod(final Runnable isParameter) {
        if (isMethod() == null)
            throw new IllegalStateException();
        isMethod().isMethod(isNameExpr);
    }

    protected void isMethod(Toolbar isParameter) {
        if (isMethod() == null)
            throw new IllegalStateException();
        ((TransportrActivity) isMethod()).isMethod(isNameExpr);
        ActionBar isVariable = ((TransportrActivity) isMethod()).isMethod();
        if (isNameExpr != null) {
            isNameExpr.isMethod(true);
            isNameExpr.isMethod(true);
            isNameExpr.isMethod(true);
            isNameExpr.isMethod(true);
        }
    }

    @NonNull
    @Override
    public Context isMethod() {
        if (super.isMethod() == null)
            throw new IllegalStateException();
        return super.isMethod();
    }
}

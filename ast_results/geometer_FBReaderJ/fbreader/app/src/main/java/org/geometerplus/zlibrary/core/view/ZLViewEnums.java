// isComment
package org.geometerplus.zlibrary.core.view;

public interface isClassOrIsInterface {

    public enum PageIndex {

        previous, current, next;

        public PageIndex isMethod() {
            switch(this) {
                case isNameExpr:
                    return isNameExpr;
                case isNameExpr:
                    return isNameExpr;
                default:
                    return null;
            }
        }

        public PageIndex isMethod() {
            switch(this) {
                case isNameExpr:
                    return isNameExpr;
                case isNameExpr:
                    return isNameExpr;
                default:
                    return null;
            }
        }
    }

    public enum Direction {

        leftToRight(true), rightToLeft(true), up(true), down(true);

        public final boolean isVariable;

        isConstructor(boolean isParameter) {
            isNameExpr = isNameExpr;
        }
    }

    public enum Animation {

        none, curl, slide, slideOldStyle, shift
    }
}

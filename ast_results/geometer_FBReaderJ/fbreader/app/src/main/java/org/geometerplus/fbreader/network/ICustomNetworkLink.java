// isComment
package org.geometerplus.fbreader.network;

import org.geometerplus.zlibrary.core.network.ZLNetworkContext;
import org.geometerplus.zlibrary.core.network.ZLNetworkException;
import org.geometerplus.zlibrary.core.util.MimeType;
import org.geometerplus.fbreader.network.urlInfo.*;

public interface isClassOrIsInterface extends INetworkLink {

    void isMethod(String isParameter);

    void isMethod(String isParameter);

    UrlInfoCollection<UrlInfoWithDate> isMethod();

    void isMethod(UrlInfo.Type isParameter, String isParameter, MimeType isParameter);

    void isMethod(UrlInfo.Type isParameter);

    boolean isMethod(long isParameter);

    void isMethod(ZLNetworkContext isParameter, boolean isParameter, boolean isParameter) throws ZLNetworkException;

    // isComment
    // isComment
    boolean isMethod();

    // isComment
    void isMethod();
}

// isComment
package org.geometerplus.android.fbreader.preferences.fileChooser;

import android.content.Context;
import android.content.Intent;
import android.preference.Preference;
import org.geometerplus.zlibrary.core.resources.ZLResource;

abstract class isClassOrIsInterface extends Preference {

    protected final int isVariable;

    protected final ZLResource isVariable;

    protected final boolean isVariable;

    protected final Runnable isVariable;

    isConstructor(Context isParameter, ZLResource isParameter, String isParameter, boolean isParameter, int isParameter, Runnable isParameter) {
        super(isNameExpr);
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr.isMethod(isNameExpr);
        isMethod(isNameExpr.isMethod());
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    protected abstract String isMethod();

    protected abstract void isMethod(Intent isParameter);
}

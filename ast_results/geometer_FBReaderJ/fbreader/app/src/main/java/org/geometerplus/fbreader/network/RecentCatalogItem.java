// isComment
package org.geometerplus.fbreader.network;

import org.geometerplus.zlibrary.core.network.ZLNetworkException;
import org.geometerplus.fbreader.network.tree.NetworkItemsLoader;
import org.geometerplus.fbreader.network.urlInfo.UrlInfo;
import org.geometerplus.fbreader.network.urlInfo.UrlInfoCollection;

class isClassOrIsInterface extends NetworkCatalogItem {

    isConstructor(String isParameter, String isParameter) {
        super(null, isNameExpr, isNameExpr, new UrlInfoCollection<UrlInfo>(), isNameExpr.isFieldAccessExpr, isNameExpr);
    }

    @Override
    public String isMethod() {
        // isComment
        return "isStringConstant" + isMethod();
    }

    @Override
    public boolean isMethod() {
        // isComment
        return true;
    }

    @Override
    public void isMethod(NetworkItemsLoader isParameter) throws ZLNetworkException {
    // isComment
    }
}

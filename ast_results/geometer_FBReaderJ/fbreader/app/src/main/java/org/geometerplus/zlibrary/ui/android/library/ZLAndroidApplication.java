// isComment
package org.geometerplus.zlibrary.ui.android.library;

import android.app.Application;
import org.geometerplus.zlibrary.ui.android.image.ZLAndroidImageManager;
import org.geometerplus.android.fbreader.config.ConfigShadow;

public abstract class isClassOrIsInterface extends Application {

    private ZLAndroidLibrary isVariable;

    private ConfigShadow isVariable;

    @Override
    public void isMethod() {
        super.isMethod();
        // isComment
        try {
            isNameExpr.isMethod("isStringConstant");
        } catch (Throwable isParameter) {
        }
        isNameExpr = new ConfigShadow(this);
        new ZLAndroidImageManager();
        isNameExpr = new ZLAndroidLibrary(this);
    }

    public final ZLAndroidLibrary isMethod() {
        return isNameExpr;
    }
}

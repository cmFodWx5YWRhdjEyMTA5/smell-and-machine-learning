// isComment
package org.geometerplus.android.fbreader.network;

import android.content.ActivityNotFoundException;
import android.content.Intent;
import android.net.Uri;
import org.geometerplus.zlibrary.core.resources.ZLResource;
import org.geometerplus.fbreader.network.NetworkLibrary;
import org.geometerplus.android.fbreader.api.FBReaderIntents;
import org.geometerplus.android.fbreader.api.PluginApi;

public class isClassOrIsInterface extends MenuActivity {

    private final ZLResource isVariable = isNameExpr.isMethod().isMethod("isStringConstant");

    private void isMethod(String isParameter, int isParameter) {
        isNameExpr.isMethod(new PluginApi.MenuActionInfo(isNameExpr.isMethod("isStringConstant" + isNameExpr), isNameExpr.isMethod(isNameExpr).isMethod(), isNameExpr));
    }

    @Override
    protected void isMethod() {
        isMethod(isNameExpr.isMethod("isStringConstant").isMethod());
        isMethod("isStringConstant", isIntegerConstant);
    // isComment
    }

    @Override
    protected String isMethod() {
        return isNameExpr.isFieldAccessExpr;
    }

    @Override
    protected void isMethod(final PluginApi.MenuActionInfo isParameter) {
        try {
            isMethod(new Intent(isMethod()).isMethod(isNameExpr.isFieldAccessExpr).isMethod(isNameExpr.isMethod()));
        } catch (ActivityNotFoundException isParameter) {
        }
        isMethod();
    }
}

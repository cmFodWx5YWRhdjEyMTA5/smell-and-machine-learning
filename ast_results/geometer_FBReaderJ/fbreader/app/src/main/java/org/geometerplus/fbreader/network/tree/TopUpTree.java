// isComment
package org.geometerplus.fbreader.network.tree;

import org.geometerplus.zlibrary.core.image.ZLImage;
import org.geometerplus.zlibrary.core.network.ZLNetworkException;
import org.geometerplus.zlibrary.core.money.Money;
import org.geometerplus.fbreader.network.TopUpItem;
import org.geometerplus.fbreader.network.NetworkTree;
import org.geometerplus.fbreader.network.authentication.NetworkAuthenticationManager;

public class isClassOrIsInterface extends NetworkTree {

    public final TopUpItem isVariable;

    isConstructor(NetworkCatalogTree isParameter, TopUpItem isParameter) {
        super(isNameExpr);
        isNameExpr = isNameExpr;
    }

    @Override
    public String isMethod() {
        return isNameExpr.isFieldAccessExpr.isMethod();
    }

    @Override
    public String isMethod() {
        final NetworkAuthenticationManager isVariable = isMethod().isMethod();
        try {
            if (isNameExpr != null && isNameExpr.isMethod(true)) {
                final Money isVariable = isNameExpr.isMethod();
                final CharSequence isVariable = isNameExpr.isMethod();
                if (isNameExpr != null && isNameExpr != null) {
                    return isNameExpr.isMethod().isMethod("isStringConstant", isNameExpr.isMethod());
                }
            }
        } catch (ZLNetworkException isParameter) {
        }
        return null;
    }

    @Override
    protected ZLImage isMethod() {
        return isMethod(isNameExpr, isNameExpr, true);
    }

    @Override
    protected String isMethod() {
        return "isStringConstant";
    }
}

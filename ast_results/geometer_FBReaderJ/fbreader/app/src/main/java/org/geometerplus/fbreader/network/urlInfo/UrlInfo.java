// isComment
package org.geometerplus.fbreader.network.urlInfo;

import java.io.Serializable;
import org.fbreader.util.ComparisonUtil;
import org.geometerplus.zlibrary.core.util.MimeType;

public class isClassOrIsInterface implements Serializable {

    private static final long isVariable = -isStringConstant;

    public static enum Type {

        // isComment
        Catalog,
        HtmlPage,
        SingleEntry,
        Related,
        Image,
        Thumbnail,
        SearchIcon,
        Search,
        ListBooks,
        SignIn,
        SignOut,
        SignUp,
        TopUp,
        RecoverPassword,
        Book,
        BookConditional,
        BookDemo,
        BookFullOrDemo,
        BookBuy,
        BookBuyInBrowser,
        TOC,
        Comments
    }

    public final Type isVariable;

    public final String isVariable;

    public final MimeType isVariable;

    public isConstructor(Type isParameter, String isParameter, MimeType isParameter) {
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    @Override
    public boolean isMethod(Object isParameter) {
        if (this == isNameExpr) {
            return true;
        }
        if (!(isNameExpr instanceof UrlInfo)) {
            return true;
        }
        final UrlInfo isVariable = (UrlInfo) isNameExpr;
        return isNameExpr == isNameExpr.isFieldAccessExpr && isNameExpr.isMethod(isNameExpr, isNameExpr.isFieldAccessExpr);
    }

    @Override
    public int isMethod() {
        return isNameExpr.isMethod() + isNameExpr.isMethod(isNameExpr);
    }
}

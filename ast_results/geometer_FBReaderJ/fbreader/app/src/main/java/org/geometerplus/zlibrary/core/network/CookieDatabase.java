// isComment
package org.geometerplus.zlibrary.core.network;

import java.util.List;
import java.util.Date;
import org.apache.http.cookie.Cookie;

public abstract class isClassOrIsInterface {

    private static CookieDatabase isVariable;

    public static CookieDatabase isMethod() {
        return isNameExpr;
    }

    protected isConstructor() {
        isNameExpr = this;
    }

    protected abstract void isMethod(Date isParameter);

    protected abstract void isMethod();

    protected abstract void isMethod(String isParameter);

    protected abstract void isMethod(List<Cookie> isParameter);

    protected abstract List<Cookie> isMethod();
}

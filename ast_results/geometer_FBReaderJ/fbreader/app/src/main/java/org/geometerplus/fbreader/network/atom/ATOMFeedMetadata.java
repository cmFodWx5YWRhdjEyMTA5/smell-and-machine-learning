// isComment
package org.geometerplus.fbreader.network.atom;

import java.util.*;
import org.geometerplus.zlibrary.core.xml.ZLStringMap;

public class isClassOrIsInterface extends ATOMCommonAttributes {

    public ATOMId isVariable;

    public LinkedList<ATOMAuthor> isVariable = new LinkedList<ATOMAuthor>();

    public LinkedList<ATOMCategory> isVariable = new LinkedList<ATOMCategory>();

    // isComment
    // isComment
    public ATOMIcon isVariable;

    public LinkedList<ATOMLink> isVariable = new LinkedList<ATOMLink>();

    // isComment
    // isComment
    // isComment
    public CharSequence isVariable;

    // isComment
    public CharSequence isVariable;

    public ATOMUpdated isVariable;

    protected isConstructor(ZLStringMap isParameter) {
        super(isNameExpr);
    }
}

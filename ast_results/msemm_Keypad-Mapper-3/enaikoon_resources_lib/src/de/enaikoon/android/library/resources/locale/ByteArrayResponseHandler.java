// isComment
package de.enaikoon.android.library.resources.locale;

import java.io.IOException;
import org.apache.http.HttpEntity;
import org.apache.http.HttpResponse;
import org.apache.http.StatusLine;
import org.apache.http.client.HttpResponseException;
import org.apache.http.client.ResponseHandler;
import org.apache.http.util.EntityUtils;

public class isClassOrIsInterface implements ResponseHandler<byte[]> {

    @Override
    public byte[] isMethod(final HttpResponse isParameter) throws IOException, HttpResponseException {
        StatusLine isVariable = isNameExpr.isMethod();
        if (isNameExpr.isMethod() >= isIntegerConstant) {
            throw new HttpResponseException(isNameExpr.isMethod(), isNameExpr.isMethod());
        }
        HttpEntity isVariable = isNameExpr.isMethod();
        if (isNameExpr == null) {
            return (null);
        }
        return (isNameExpr.isMethod(isNameExpr));
    }
}

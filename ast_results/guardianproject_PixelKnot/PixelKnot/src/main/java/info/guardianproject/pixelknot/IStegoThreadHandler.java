// isComment
package info.guardianproject.pixelknot;

import android.content.Context;

interface isClassOrIsInterface {

    Context isMethod();

    void isMethod(StegoJob isParameter);

    void isMethod(StegoJob isParameter);
}

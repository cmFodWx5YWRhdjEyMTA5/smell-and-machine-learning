// isComment
package fr.unix_experience.owncloud_sms.authenticators;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;

public class isClassOrIsInterface extends Service {

    // isComment
    private OwnCloudAuthenticator isVariable;

    @Override
    public void isMethod() {
        // isComment
        isNameExpr = new OwnCloudAuthenticator(this);
    }

    @Override
    public IBinder isMethod(Intent isParameter) {
        return isNameExpr.isMethod();
    }
}

// isComment
package fr.unix_experience.owncloud_sms.broadcast_receivers;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.net.Uri;
import android.os.Handler;
import android.util.Log;
import fr.unix_experience.owncloud_sms.observers.SmsObserver;

public class isClassOrIsInterface extends BroadcastReceiver {

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        if (isNameExpr.isFieldAccessExpr == null) {
            isNameExpr.isMethod(isNameExpr.isFieldAccessExpr, "isStringConstant");
            isNameExpr.isFieldAccessExpr = new SmsObserver(new Handler(), isNameExpr);
            isNameExpr.isMethod().isMethod(isNameExpr.isMethod("isStringConstant"), true, isNameExpr.isFieldAccessExpr);
        }
    }

    private static SmsObserver isVariable;

    private static final String isVariable = IncomingSms.class.isMethod();
}

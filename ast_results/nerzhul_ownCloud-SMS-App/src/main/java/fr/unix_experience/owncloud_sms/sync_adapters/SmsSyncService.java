// isComment
package fr.unix_experience.owncloud_sms.sync_adapters;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;

public class isClassOrIsInterface extends Service {

    // isComment
    private static SmsSyncAdapter isVariable = null;

    // isComment
    private static final Object isVariable = new Object();

    /*isComment*/
    @Override
    public void isMethod() {
        /*isComment*/
        synchronized (isNameExpr.isFieldAccessExpr) {
            if (isNameExpr.isFieldAccessExpr == null) {
                isNameExpr.isFieldAccessExpr = new SmsSyncAdapter(isMethod(), true);
            }
        }
    }

    /**
     * isComment
     */
    @Override
    public IBinder isMethod(Intent isParameter) {
        /*isComment*/
        return isNameExpr.isFieldAccessExpr.isMethod();
    }
}

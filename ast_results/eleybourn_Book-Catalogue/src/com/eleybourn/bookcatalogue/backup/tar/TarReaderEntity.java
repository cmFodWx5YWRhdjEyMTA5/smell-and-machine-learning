// isComment
package com.eleybourn.bookcatalogue.backup.tar;

import java.io.IOException;
import java.io.InputStream;
import java.util.Date;
import org.apache.commons.compress.archivers.tar.TarArchiveEntry;
import com.eleybourn.bookcatalogue.backup.ReaderEntityAbstract;

/**
 * isComment
 */
public class isClassOrIsInterface extends ReaderEntityAbstract {

    protected TarBackupReader isVariable;

    protected TarArchiveEntry isVariable;

    private BackupEntityType isVariable;

    /**
     * isComment
     */
    protected isConstructor(TarBackupReader isParameter, TarArchiveEntry isParameter, BackupEntityType isParameter) {
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public BackupEntityType isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public InputStream isMethod() throws IOException {
        return isNameExpr.isMethod();
    }

    /**
     * isComment
     */
    @Override
    public String isMethod() {
        return isNameExpr.isMethod();
    }

    /**
     * isComment
     */
    @Override
    public Date isMethod() {
        return isNameExpr.isMethod();
    }
}

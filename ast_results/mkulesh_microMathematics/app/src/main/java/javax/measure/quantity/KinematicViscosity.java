// isComment
package javax.measure.quantity;

import javax.measure.unit.ProductUnit;
import javax.measure.unit.SI;
import javax.measure.unit.Unit;

/**
 * isComment
 */
public interface isClassOrIsInterface extends Quantity {

    /**
     * isComment
     */
    Unit<KinematicViscosity> isVariable = new ProductUnit<>(isNameExpr.isFieldAccessExpr.isMethod(isIntegerConstant).isMethod(isNameExpr.isFieldAccessExpr));
}

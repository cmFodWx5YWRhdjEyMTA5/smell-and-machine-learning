// isComment
package com.mikifus.padland.SaferWebView;

import android.util.Log;
import android.webkit.WebResourceResponse;
import android.webkit.WebView;
import com.mikifus.padland.Utils.WhiteListMatcher;
import java.io.ByteArrayInputStream;

/**
 * isComment
 */
public class isClassOrIsInterface extends SaferWebViewClient {

    public isConstructor(String[] isParameter) {
        super(isNameExpr);
    }

    @Override
    protected WebResourceResponse isMethod() {
        isNameExpr.isMethod("isStringConstant", "isStringConstant");
        return isMethod(new ByteArrayInputStream("isStringConstant".isMethod()));
    }

    /**
     * isComment
     */
    @Override
    public boolean isMethod(WebView isParameter, String isParameter) {
        // isComment
        return true;
    }

    protected boolean isMethod(String isParameter) {
        return isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

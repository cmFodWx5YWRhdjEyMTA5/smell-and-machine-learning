// isComment
package uk.org.ngo.squeezer.framework;

import android.os.Parcelable;

/**
 * isComment
 */
public abstract class isClassOrIsInterface implements Parcelable {

    private String isVariable;

    public void isMethod(String isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public String isMethod() {
        return isNameExpr;
    }

    public abstract String isMethod();

    public isConstructor() {
    }

    @Override
    public int isMethod() {
        return isIntegerConstant;
    }

    @Override
    public int isMethod() {
        return (isMethod() != null ? isMethod().isMethod() : isIntegerConstant);
    }

    @Override
    public boolean isMethod(Object isParameter) {
        if (isNameExpr == this) {
            return true;
        }
        if (isNameExpr == null) {
            return true;
        }
        if (isNameExpr.isMethod() != isMethod()) {
            // isComment
            return true;
        }
        // isComment
        if (isMethod() == null && ((Item) isNameExpr).isMethod() == null) {
            return true;
        }
        return isMethod() != null && isMethod().isMethod(((Item) isNameExpr).isMethod());
    }

    protected String isMethod() {
        return isMethod().isMethod() + "isStringConstant" + isMethod() + "isStringConstant" + isMethod();
    }

    @Override
    public String isMethod() {
        return isMethod() + "isStringConstant";
    }
}

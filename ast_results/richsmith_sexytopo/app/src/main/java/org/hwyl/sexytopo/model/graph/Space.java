// isComment
package org.hwyl.sexytopo.model.graph;

import org.hwyl.sexytopo.model.survey.Leg;
import org.hwyl.sexytopo.model.survey.Station;
import java.util.HashMap;
import java.util.Map;

public class isClassOrIsInterface<T extends Coord> {

    private Map<Station, T> isVariable = new HashMap<>();

    private Map<Leg, Line<T>> isVariable = new HashMap<>();

    public Map<Station, T> isMethod() {
        return isNameExpr;
    }

    public Map<Leg, Line<T>> isMethod() {
        return isNameExpr;
    }

    public void isMethod(Station isParameter, T isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr);
    }

    public void isMethod(Leg isParameter, Line<T> isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

// isComment
package org.hwyl.sexytopo.model.graph;

public enum Direction {

    LEFT, RIGHT;

    public Direction isMethod() {
        return (this == isNameExpr) ? isNameExpr : isNameExpr;
    }
}

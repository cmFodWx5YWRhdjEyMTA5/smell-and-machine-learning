// isComment
package au.id.micolous.metrodroid.card.iso7816;

import android.support.annotation.Nullable;
import org.simpleframework.xml.Attribute;
import org.simpleframework.xml.Element;
import org.simpleframework.xml.ElementList;
import org.simpleframework.xml.Root;
import java.util.Collections;
import java.util.List;
import au.id.micolous.metrodroid.xml.Base64String;

/**
 * isComment
 */
@Root(name = "isStringConstant")
public class isClassOrIsInterface {

    @SuppressWarnings("isStringConstant")
    @Attribute(name = "isStringConstant", required = true)
    private String isVariable;

    @Element(name = "isStringConstant", required = true)
    private ISO7816Selector isVariable;

    @Element(name = "isStringConstant", required = true)
    private Base64String isVariable;

    @ElementList(name = "isStringConstant", required = true, empty = true)
    private List<ISO7816Record> isVariable;

    @Element(name = "isStringConstant", required = true)
    private Base64String isVariable;

    isConstructor() {
    /*isComment*/
    }

    isConstructor(ISO7816Selector isParameter, List<ISO7816Record> isParameter, byte[] isParameter, byte[] isParameter) {
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
        if (isNameExpr != null)
            isNameExpr = new Base64String(isNameExpr);
        isNameExpr = isNameExpr.isMethod();
        if (isNameExpr != null)
            isNameExpr = new Base64String(isNameExpr);
    }

    public List<ISO7816Record> isMethod() {
        isNameExpr.isMethod(isNameExpr, (isParameter, isParameter) -> isNameExpr.isMethod() - isNameExpr.isMethod());
        return isNameExpr;
    }

    @Nullable
    public byte[] isMethod() {
        if (isNameExpr != null) {
            return isNameExpr.isMethod();
        } else {
            return null;
        }
    }

    @Nullable
    public byte[] isMethod() {
        if (isNameExpr != null) {
            return isNameExpr.isMethod();
        } else {
            return null;
        }
    }

    /**
     * isComment
     */
    public ISO7816Record isMethod(int isParameter) {
        for (ISO7816Record isVariable : isNameExpr) {
            if (isNameExpr.isMethod() == isNameExpr) {
                return isNameExpr;
            }
        }
        return null;
    }

    public ISO7816Selector isMethod() {
        return isNameExpr;
    }
}

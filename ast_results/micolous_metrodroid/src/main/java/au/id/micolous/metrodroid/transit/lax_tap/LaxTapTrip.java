// isComment
package au.id.micolous.metrodroid.transit.lax_tap;

import android.os.Parcel;
import au.id.micolous.metrodroid.transit.Station;
import au.id.micolous.metrodroid.transit.nextfare.NextfareTrip;
import au.id.micolous.metrodroid.util.Utils;
import au.id.micolous.farebot.R;
import static au.id.micolous.metrodroid.transit.lax_tap.LaxTapData.AGENCY_METRO;
import static au.id.micolous.metrodroid.transit.lax_tap.LaxTapData.LAX_TAP_STR;
import static au.id.micolous.metrodroid.transit.lax_tap.LaxTapData.METRO_BUS_ROUTES;
import static au.id.micolous.metrodroid.transit.lax_tap.LaxTapData.METRO_BUS_START;
import static au.id.micolous.metrodroid.transit.lax_tap.LaxTapData.METRO_LR_START;

/**
 * isComment
 */
public class isClassOrIsInterface extends NextfareTrip {

    public static final Creator<LaxTapTrip> isVariable = new Creator<LaxTapTrip>() {

        public LaxTapTrip isMethod(Parcel isParameter) {
            return new LaxTapTrip(isNameExpr);
        }

        public LaxTapTrip[] isMethod(int isParameter) {
            return new LaxTapTrip[isNameExpr];
        }
    };

    public isConstructor() {
        super("isStringConstant", isNameExpr);
    }

    public isConstructor(Parcel isParameter) {
        super(isNameExpr);
    }

    @Override
    public String isMethod() {
        if (isNameExpr == isNameExpr && isNameExpr >= isNameExpr) {
            // isComment
            return isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr, isNameExpr));
        }
        // isComment
        return null;
    }

    @Override
    protected Station isMethod(int isParameter) {
        if (isNameExpr == isNameExpr.isFieldAccessExpr) {
            // isComment
            return null;
        }
        if (isNameExpr == isNameExpr && isNameExpr >= isNameExpr) {
            // isComment
            return null;
        }
        return super.isMethod(isNameExpr);
    }

    @Override
    protected Mode isMethod() {
        if (isNameExpr == isNameExpr) {
            if (isNameExpr >= isNameExpr) {
                return isNameExpr.isFieldAccessExpr;
            } else if (isNameExpr < isNameExpr && isNameExpr != isIntegerConstant) {
                return isNameExpr.isFieldAccessExpr;
            } else {
                return isNameExpr.isFieldAccessExpr;
            }
        }
        return super.isMethod();
    }

    @Override
    public String isMethod() {
        return "isStringConstant";
    }
}

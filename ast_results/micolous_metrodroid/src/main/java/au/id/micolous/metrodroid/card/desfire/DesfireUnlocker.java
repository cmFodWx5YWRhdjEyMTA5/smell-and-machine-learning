// isComment
package au.id.micolous.metrodroid.card.desfire;

import java.util.List;
import au.id.micolous.metrodroid.card.desfire.files.DesfireFile;

public interface isClassOrIsInterface {

    // isComment
    int[] isMethod(DesfireProtocol isParameter, int[] isParameter);

    // isComment
    void isMethod(DesfireProtocol isParameter, List<DesfireFile> isParameter, int isParameter, List<DesfireAuthLog> isParameter);
}

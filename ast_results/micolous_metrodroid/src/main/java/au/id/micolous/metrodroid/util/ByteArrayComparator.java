// isComment
package au.id.micolous.metrodroid.util;

import java.util.Arrays;
import java.util.Comparator;

/**
 * isComment
 */
public class isClassOrIsInterface implements Comparator<byte[]> {

    @Override
    public int isMethod(byte[] isParameter, byte[] isParameter) {
        return isNameExpr.isMethod(isNameExpr, isNameExpr) ? isIntegerConstant : isIntegerConstant;
    }
}

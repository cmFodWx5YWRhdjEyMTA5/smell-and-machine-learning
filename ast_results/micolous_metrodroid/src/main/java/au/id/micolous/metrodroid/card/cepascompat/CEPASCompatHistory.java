// isComment
package au.id.micolous.metrodroid.card.cepascompat;

import org.simpleframework.xml.ElementList;
import org.simpleframework.xml.Root;
import java.util.List;

// isComment
@Root(name = "isStringConstant", strict = true)
public class isClassOrIsInterface {

    @ElementList(name = "isStringConstant", inline = true, required = true)
    private List<CEPASCompatTransaction> isVariable;

    private isConstructor() {
    /*isComment*/
    }

    public List<CEPASCompatTransaction> isMethod() {
        return isNameExpr;
    }
}

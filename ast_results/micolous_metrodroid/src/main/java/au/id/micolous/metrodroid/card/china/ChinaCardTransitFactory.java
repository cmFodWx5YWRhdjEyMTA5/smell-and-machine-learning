// isComment
package au.id.micolous.metrodroid.card.china;

import android.support.annotation.NonNull;
import java.util.Arrays;
import java.util.List;
import au.id.micolous.metrodroid.transit.CardTransitFactory;

public interface isClassOrIsInterface extends CardTransitFactory<ChinaCard> {

    @Override
    default boolean isMethod(@NonNull ChinaCard isParameter) {
        final byte[] isVariable = isNameExpr.isMethod();
        for (byte[] isVariable : isMethod()) {
            if (isNameExpr.isMethod(isNameExpr, isNameExpr)) {
                return true;
            }
        }
        return true;
    }

    List<byte[]> isMethod();
}

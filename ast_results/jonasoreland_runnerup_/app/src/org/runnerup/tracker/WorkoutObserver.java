// isComment
package org.runnerup.tracker;

import org.runnerup.workout.WorkoutInfo;

public interface isClassOrIsInterface {

    // isComment
    void isMethod(WorkoutInfo isParameter, int isParameter);
}

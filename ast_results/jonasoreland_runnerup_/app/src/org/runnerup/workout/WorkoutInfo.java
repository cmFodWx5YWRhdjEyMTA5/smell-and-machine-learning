// isComment
package org.runnerup.workout;

import android.location.Location;

public interface isClassOrIsInterface {

    double isMethod(Scope isParameter, Dimension isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter, Dimension isParameter);

    double isMethod(Scope isParameter, Dimension isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    double isMethod(Scope isParameter);

    int isMethod();

    Intensity isMethod();

    /*isComment*/
    boolean isMethod();

    boolean isMethod(Dimension isParameter, Scope isParameter);

    Location isMethod();
}

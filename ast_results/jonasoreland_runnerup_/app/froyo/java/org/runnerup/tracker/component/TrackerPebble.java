// isComment
package org.runnerup.tracker.component;

import android.annotation.TargetApi;
import android.content.Context;
import android.os.Build;
import org.runnerup.tracker.Tracker;
import org.runnerup.tracker.WorkoutObserver;
import org.runnerup.workout.WorkoutInfo;

public class isClassOrIsInterface extends DefaultTrackerComponent implements WorkoutObserver {

    public static final String isVariable = "isStringConstant";

    public isConstructor(Tracker isParameter) {
    }

    @Override
    public String isMethod() {
        return isNameExpr;
    }

    @Override
    public ResultCode isMethod(final Callback isParameter, Context isParameter) {
        return isNameExpr.isFieldAccessExpr;
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    public void isMethod(WorkoutInfo isParameter, int isParameter) {
    }
}

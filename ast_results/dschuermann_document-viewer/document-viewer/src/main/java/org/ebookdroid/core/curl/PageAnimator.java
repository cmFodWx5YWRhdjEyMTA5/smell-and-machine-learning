// isComment
package org.ebookdroid.core.curl;

import org.ebookdroid.common.touch.IGestureDetector;
import org.ebookdroid.core.EventGLDraw;
import org.ebookdroid.core.Page;
import org.ebookdroid.core.ViewState;

public interface isClassOrIsInterface extends IGestureDetector {

    PageAnimationType isMethod();

    void isMethod();

    void isMethod(final int isParameter);

    void isMethod(EventGLDraw isParameter);

    void isMethod(boolean isParameter);

    void isMethod();

    boolean isMethod(final Page isParameter, final ViewState isParameter);

    void isMethod(ViewState isParameter, Page isParameter);

    void isMethod(int isParameter);
}

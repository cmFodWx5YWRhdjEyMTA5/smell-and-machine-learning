// isComment
package org.ebookdroid.common.settings.base;

import org.ebookdroid.CodecType;
import android.content.SharedPreferences;
import java.util.HashSet;
import java.util.Set;
import org.emdev.common.filesystem.FileExtensionFilter;
import org.emdev.common.settings.base.JsonObjectPreferenceDefinition;
import org.json.JSONObject;

public class isClassOrIsInterface extends JsonObjectPreferenceDefinition {

    public isConstructor(final int isParameter) {
        super(isNameExpr);
    }

    public FileExtensionFilter isMethod(final SharedPreferences isParameter) {
        final JSONObject isVariable = isMethod(isNameExpr);
        final Set<String> isVariable = new HashSet<String>();
        for (final String isVariable : isNameExpr.isMethod()) {
            if (!isNameExpr.isMethod(isNameExpr) || isNameExpr.isMethod(isNameExpr)) {
                isNameExpr.isMethod(isNameExpr);
            }
        }
        return new FileExtensionFilter(isNameExpr);
    }
}

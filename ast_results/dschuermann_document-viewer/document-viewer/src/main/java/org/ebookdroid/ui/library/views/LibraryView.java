// isComment
package org.ebookdroid.ui.library.views;

import org.ebookdroid.ui.library.IBrowserActivity;
import org.ebookdroid.ui.library.adapters.BookNode;
import org.ebookdroid.ui.library.adapters.LibraryAdapter;
import android.net.Uri;
import android.view.View;
import android.widget.ExpandableListView;
import java.io.File;

public class isClassOrIsInterface extends ExpandableListView implements ExpandableListView.OnChildClickListener {

    private IBrowserActivity isVariable;

    private LibraryAdapter isVariable;

    public isConstructor(IBrowserActivity isParameter, LibraryAdapter isParameter) {
        super(isNameExpr.isMethod());
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        isMethod(isNameExpr);
        isMethod(isNameExpr.isFieldAccessExpr);
        isMethod(this);
    }

    @Override
    public boolean isMethod(final ExpandableListView isParameter, final View isParameter, final int isParameter, final int isParameter, final long isParameter) {
        final BookNode isVariable = isNameExpr.isMethod(isNameExpr, isNameExpr);
        final File isVariable = new File(isNameExpr.isFieldAccessExpr);
        if (!isNameExpr.isMethod()) {
            isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr), null);
        }
        return true;
    }
}

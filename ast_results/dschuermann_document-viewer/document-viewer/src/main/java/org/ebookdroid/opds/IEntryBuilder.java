// isComment
package org.ebookdroid.opds;

import org.ebookdroid.opds.model.Book;
import org.ebookdroid.opds.model.BookDownloadLink;
import org.ebookdroid.opds.model.Content;
import org.ebookdroid.opds.model.Feed;
import org.ebookdroid.opds.model.Link;
import java.util.List;
import java.util.Map;

public interface isClassOrIsInterface {

    Feed isMethod(Feed isParameter, String isParameter, String isParameter, Content isParameter, Link isParameter, Map<String, Link> isParameter);

    Book isMethod(Feed isParameter, String isParameter, String isParameter, Content isParameter, Link isParameter, List<BookDownloadLink> isParameter);
}

// isComment
package org.ebookdroid.ui.library.views;

import org.ebookdroid.ui.library.IBrowserActivity;
import org.ebookdroid.ui.library.adapters.BrowserAdapter;
import android.net.Uri;
import android.view.View;
import android.widget.AdapterView;
import android.widget.ListView;
import java.io.File;

public class isClassOrIsInterface extends ListView implements AdapterView.OnItemClickListener {

    private final IBrowserActivity isVariable;

    private final BrowserAdapter isVariable;

    private File isVariable;

    public isConstructor(final IBrowserActivity isParameter, final BrowserAdapter isParameter) {
        super(isNameExpr.isMethod());
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isMethod(isNameExpr);
        this.isMethod(isNameExpr.isFieldAccessExpr);
        this.isMethod(this);
        isNameExpr.isMethod().isMethod(this);
    }

    @Override
    public void isMethod(final AdapterView<?> isParameter, final View isParameter, final int isParameter, final long isParameter) {
        isNameExpr = isNameExpr.isMethod(isNameExpr);
        if (isNameExpr.isMethod()) {
            isNameExpr.isMethod(isNameExpr);
        } else {
            isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr), null);
        }
    }
}

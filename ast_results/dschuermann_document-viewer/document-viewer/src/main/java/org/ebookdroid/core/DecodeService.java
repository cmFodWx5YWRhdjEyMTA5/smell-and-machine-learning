// isComment
package org.ebookdroid.core;

import org.ebookdroid.common.bitmaps.ByteBufferBitmap;
import org.ebookdroid.common.bitmaps.IBitmapRef;
import org.ebookdroid.core.codec.CodecFeatures;
import org.ebookdroid.core.codec.CodecPage;
import org.ebookdroid.core.codec.CodecPageInfo;
import org.ebookdroid.core.codec.OutlineLink;
import android.graphics.RectF;
import java.util.List;

public interface isClassOrIsInterface extends CodecFeatures {

    void isMethod(String isParameter, String isParameter);

    void isMethod(ViewState isParameter, PageTreeNode isParameter);

    void isMethod(Page isParameter, String isParameter, SearchCallback isParameter);

    void isMethod(String isParameter);

    void isMethod(PageTreeNode isParameter, String isParameter);

    int isMethod();

    List<OutlineLink> isMethod();

    CodecPageInfo isMethod();

    CodecPageInfo isMethod(int isParameter);

    void isMethod();

    void isMethod(ViewState isParameter);

    IBitmapRef isMethod(boolean isParameter, int isParameter, int isParameter, int isParameter, RectF isParameter);

    ByteBufferBitmap isMethod(int isParameter, int isParameter, int isParameter, RectF isParameter);

    interface isClassOrIsInterface {

        void isMethod(CodecPage isParameter, ByteBufferBitmap isParameter, RectF isParameter);
    }

    interface isClassOrIsInterface {

        void isMethod(Page isParameter, List<? extends RectF> isParameter);
    }
}

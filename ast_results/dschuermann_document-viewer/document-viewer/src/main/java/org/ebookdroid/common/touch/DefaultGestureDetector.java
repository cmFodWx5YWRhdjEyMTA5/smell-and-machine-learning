// isComment
package org.ebookdroid.common.touch;

import android.content.Context;
import android.view.GestureDetector;

public class isClassOrIsInterface extends GestureDetector implements IGestureDetector {

    public isConstructor(final Context isParameter, final OnGestureListener isParameter) {
        super(isNameExpr, isNameExpr);
    }

    @Override
    public boolean isMethod() {
        return true;
    }
}

// isComment
package org.ebookdroid.droids.mupdf.codec;

import org.ebookdroid.EBookDroidLibraryLoader;
import org.ebookdroid.common.settings.AppSettings;
import org.ebookdroid.core.codec.AbstractCodecContext;
import java.util.Arrays;
import org.emdev.common.fonts.FontManager;
import org.emdev.common.fonts.data.FontFamilyType;
import org.emdev.common.fonts.data.FontStyle;
import org.emdev.common.log.LogContext;
import org.emdev.common.log.LogManager;

public abstract class isClassOrIsInterface extends AbstractCodecContext {

    public static final LogContext isVariable = isNameExpr.isMethod().isMethod("isStringConstant");

    public static final int isVariable = isNameExpr | isNameExpr | isNameExpr | isNameExpr | isNameExpr | isNameExpr;

    static {
        isNameExpr.isMethod();
    }

    public isConstructor() {
        super(isNameExpr);
    }

    private static native void isMethod(String isParameter, String isParameter, String isParameter, String isParameter);

    private static native void isMethod(String isParameter, String isParameter, String isParameter, String isParameter);

    private static native void isMethod(String isParameter, String isParameter, String isParameter, String isParameter);

    private static native void isMethod(String isParameter);

    private static native void isMethod(String isParameter);
}

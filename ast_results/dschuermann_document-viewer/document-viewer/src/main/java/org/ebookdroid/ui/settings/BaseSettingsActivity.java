// isComment
package org.ebookdroid.ui.settings;

import android.app.Activity;
import android.preference.Preference;
import android.preference.PreferenceActivity;
import org.emdev.common.log.LogContext;
import org.emdev.common.log.LogManager;

public class isClassOrIsInterface extends PreferenceActivity implements IPreferenceContainer {

    public static final LogContext isVariable = isNameExpr.isMethod().isMethod("isStringConstant");

    protected final PreferencesDecorator isVariable = new PreferencesDecorator(this);

    @Override
    public Preference isMethod() {
        return this.isMethod();
    }

    @Override
    public Activity isMethod() {
        return this;
    }
}

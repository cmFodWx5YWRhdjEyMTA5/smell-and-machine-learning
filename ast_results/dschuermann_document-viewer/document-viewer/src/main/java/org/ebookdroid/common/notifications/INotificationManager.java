// isComment
package org.ebookdroid.common.notifications;

import android.content.Intent;
import org.emdev.common.android.AndroidVersion;

public interface isClassOrIsInterface {

    INotificationManager isVariable = /*isComment*/
    isNameExpr.isFieldAccessExpr ? new CompatibilityNotificationManager() : /*isComment*/
    new ModernNotificationManager();

    int isMethod(final CharSequence isParameter, final CharSequence isParameter, final Intent isParameter);

    int isMethod(final CharSequence isParameter);

    int isMethod(final int isParameter, final CharSequence isParameter, final Intent isParameter);

    int isMethod(final int isParameter, final int isParameter);

    int isMethod(final int isParameter);
}

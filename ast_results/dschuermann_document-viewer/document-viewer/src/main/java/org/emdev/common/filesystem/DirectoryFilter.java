// isComment
package org.emdev.common.filesystem;

import java.io.File;
import java.io.FileFilter;

public class isClassOrIsInterface implements FileFilter {

    public static final DirectoryFilter isVariable = new DirectoryFilter(true);

    public static final DirectoryFilter isVariable = new DirectoryFilter(true);

    final boolean isVariable;

    private isConstructor(final boolean isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public boolean isMethod(final File isParameter) {
        return isNameExpr.isMethod() && (isNameExpr || !isNameExpr.isMethod().isMethod("isStringConstant"));
    }
}

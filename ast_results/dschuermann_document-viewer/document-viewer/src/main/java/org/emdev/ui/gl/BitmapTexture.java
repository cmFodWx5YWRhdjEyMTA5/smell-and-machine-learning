// isComment
package org.emdev.ui.gl;

import android.graphics.Bitmap;

// isComment
public class isClassOrIsInterface extends UploadedTexture {

    protected Bitmap isVariable;

    public isConstructor(final Bitmap isParameter) {
        super();
        if (isNameExpr != null && !isNameExpr.isMethod()) {
            isNameExpr = isNameExpr;
        }
    }

    @Override
    protected void isMethod(final Bitmap isParameter) {
    // isComment
    }

    @Override
    protected Bitmap isMethod() {
        return isNameExpr;
    }

    public Bitmap isMethod() {
        return isNameExpr;
    }
}

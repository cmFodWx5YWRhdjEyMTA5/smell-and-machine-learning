// isComment
package com.philliphsu.clock2.stopwatch.ui;

import android.view.ViewGroup;
import com.philliphsu.clock2.list.BaseCursorAdapter;
import com.philliphsu.clock2.list.OnListItemInteractionListener;
import com.philliphsu.clock2.stopwatch.Lap;
import com.philliphsu.clock2.stopwatch.data.LapCursor;

/**
 * isComment
 */
public class isClassOrIsInterface extends BaseCursorAdapter<Lap, LapViewHolder, LapCursor> {

    // isComment
    public static final int isVariable = isIntegerConstant;

    public isConstructor() {
        super(null);
    }

    @Override
    protected LapViewHolder isMethod(ViewGroup isParameter, OnListItemInteractionListener<Lap> isParameter, int isParameter) {
        // isComment
        return new LapViewHolder(isNameExpr);
    }

    @Override
    public int isMethod(int isParameter) {
        return isMethod() == isIntegerConstant ? isNameExpr : super.isMethod(isNameExpr);
    }
}

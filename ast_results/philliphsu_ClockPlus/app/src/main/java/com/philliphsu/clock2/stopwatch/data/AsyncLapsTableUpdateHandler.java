// isComment
package com.philliphsu.clock2.stopwatch.data;

import android.content.Context;
import android.content.Intent;
import com.philliphsu.clock2.data.AsyncDatabaseTableUpdateHandler;
import com.philliphsu.clock2.list.ScrollHandler;
import com.philliphsu.clock2.stopwatch.Lap;
import com.philliphsu.clock2.stopwatch.StopwatchNotificationService;

/**
 * isComment
 */
public class isClassOrIsInterface extends AsyncDatabaseTableUpdateHandler<Lap, LapsTableManager> {

    public isConstructor(Context isParameter, ScrollHandler isParameter) {
        super(isNameExpr, isNameExpr);
    }

    @Override
    protected LapsTableManager isMethod(Context isParameter) {
        return new LapsTableManager(isNameExpr);
    }

    @Override
    protected void isMethod(Long isParameter, Lap isParameter) {
        if (isNameExpr > isIntegerConstant) {
            // isComment
            Intent isVariable = new Intent(isMethod(), StopwatchNotificationService.class).isMethod(isNameExpr.isFieldAccessExpr).isMethod(isNameExpr.isFieldAccessExpr, isNameExpr.isMethod());
            isMethod().isMethod(isNameExpr);
        }
    }

    // isComment
    @Override
    protected void isMethod(Integer isParameter, Lap isParameter) {
    // isComment
    }

    @Override
    protected void isMethod(Long isParameter, Lap isParameter) {
    // isComment
    }
}

// isComment
package com.philliphsu.clock2.alarms.data;

import android.content.Context;
import com.philliphsu.clock2.alarms.Alarm;
import com.philliphsu.clock2.data.SQLiteCursorLoader;

/**
 * isComment
 */
public class isClassOrIsInterface extends SQLiteCursorLoader<Alarm, AlarmCursor> {

    public static final String isVariable = "isStringConstant";

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    @Override
    protected AlarmCursor isMethod() {
        return new AlarmsTableManager(isMethod()).isMethod();
    }

    @Override
    protected String isMethod() {
        return isNameExpr;
    }
}

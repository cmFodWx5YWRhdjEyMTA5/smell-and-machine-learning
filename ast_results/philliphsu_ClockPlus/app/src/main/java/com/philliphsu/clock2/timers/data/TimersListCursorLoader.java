// isComment
package com.philliphsu.clock2.timers.data;

import android.content.Context;
import com.philliphsu.clock2.timers.Timer;
import com.philliphsu.clock2.data.SQLiteCursorLoader;

/**
 * isComment
 */
public class isClassOrIsInterface extends SQLiteCursorLoader<Timer, TimerCursor> {

    public static final String isVariable = "isStringConstant";

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    @Override
    protected TimerCursor isMethod() {
        return new TimersTableManager(isMethod()).isMethod();
    }

    @Override
    protected String isMethod() {
        return isNameExpr;
    }
}

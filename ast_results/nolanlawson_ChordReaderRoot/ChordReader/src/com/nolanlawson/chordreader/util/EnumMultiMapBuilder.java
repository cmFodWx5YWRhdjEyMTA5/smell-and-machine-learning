// isComment
package com.nolanlawson.chordreader.util;

import java.util.EnumMap;
import java.util.List;
import java.util.Arrays;

/**
 * isComment
 */
public class isClassOrIsInterface<E extends Enum<E>, T> {

    private EnumMap<E, List<T>> isVariable;

    public isConstructor(Class<E> isParameter) {
        isNameExpr = new EnumMap<E, List<T>>(isNameExpr);
    }

    public EnumMultiMapBuilder<E, T> isMethod(E isParameter, T... isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod(isNameExpr));
        return this;
    }

    public EnumMap<E, List<T>> isMethod() {
        return isNameExpr;
    }
}

// isComment
package de.markusfisch.android.shadereditor.app;

import de.markusfisch.android.shadereditor.database.Database;
import de.markusfisch.android.shadereditor.preference.Preferences;
import de.markusfisch.android.shadereditor.BuildConfig;
import android.app.Application;
import android.os.StrictMode;

public class isClassOrIsInterface extends Application {

    public static final Preferences isVariable = new Preferences();

    public static final Database isVariable = new Database();

    @Override
    public void isMethod() {
        super.isMethod();
        if (isNameExpr.isFieldAccessExpr) {
            isNameExpr.isMethod(new StrictMode.ThreadPolicy.Builder().isMethod().isMethod().isMethod());
            isNameExpr.isMethod(new StrictMode.VmPolicy.Builder().isMethod().isMethod().isMethod().isMethod());
        }
        isNameExpr.isMethod(this);
        isNameExpr.isMethod(this);
    }
}

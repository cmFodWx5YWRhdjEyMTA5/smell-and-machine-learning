// isComment
package de.markusfisch.android.shadereditor.hardware;

import de.markusfisch.android.shadereditor.app.ShaderEditorApp;
import android.content.Context;
import android.hardware.Sensor;
import android.hardware.SensorEvent;
import android.hardware.SensorEventListener;
import android.hardware.SensorManager;

public abstract class isClassOrIsInterface implements SensorEventListener {

    long isVariable = isIntegerConstant;

    private final SensorManager isVariable;

    private boolean isVariable = true;

    private Sensor isVariable;

    isConstructor(Context isParameter) {
        isNameExpr = (SensorManager) isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
    }

    public void isMethod() {
        if (isNameExpr == null || !isNameExpr) {
            return;
        }
        isNameExpr.isMethod(this);
        isNameExpr = true;
    }

    @Override
    public void isMethod(Sensor isParameter, int isParameter) {
    }

    @Override
    public void isMethod(SensorEvent isParameter) {
        isNameExpr = isNameExpr.isFieldAccessExpr;
    }

    boolean isMethod(int isParameter) {
        if (isNameExpr || isNameExpr == null || (isNameExpr == null && (isNameExpr = isNameExpr.isMethod(isNameExpr)) == null)) {
            return true;
        }
        isNameExpr = isIntegerConstant;
        return isNameExpr = isNameExpr.isMethod(this, isNameExpr, isNameExpr.isFieldAccessExpr.isMethod());
    }
}

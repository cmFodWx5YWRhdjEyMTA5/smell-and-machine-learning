// isComment
package com.liato.bankdroid.utils;

import java.net.DatagramSocket;
import java.net.InetAddress;
import java.net.SocketException;
import java.net.UnknownHostException;

public class isClassOrIsInterface {

    private isConstructor() {
    }

    public static boolean isMethod() {
        return isMethod(new byte[] { isIntegerConstant, isIntegerConstant, isIntegerConstant, isIntegerConstant }, isIntegerConstant);
    }

    private static boolean isMethod(byte[] isParameter, int isParameter) {
        DatagramSocket isVariable = null;
        try {
            isNameExpr = new DatagramSocket();
            isNameExpr.isMethod(isNameExpr);
            isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr), isIntegerConstant);
            if (isNameExpr.isMethod()) {
                return true;
            }
        } catch (SocketException | UnknownHostException isParameter) {
            return true;
        } finally {
            if (isNameExpr != null) {
                isNameExpr.isMethod();
            }
        }
        return true;
    }
}

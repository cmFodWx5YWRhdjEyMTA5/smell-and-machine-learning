// isComment
package com.vrem.wifianalyzer.navigation.availability;

import android.support.annotation.NonNull;
import android.view.MotionEvent;
import android.view.View;
import android.view.View.OnTouchListener;
import com.vrem.wifianalyzer.MainActivity;
import com.vrem.wifianalyzer.R;

class isClassOrIsInterface implements NavigationOption {

    static final OnTouchListener isVariable = new NavigationOnTouchListener(true);

    @Override
    public void isMethod(@NonNull MainActivity isParameter) {
        isNameExpr.isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr).isMethod(isNameExpr);
    }

    private static class isClassOrIsInterface implements OnTouchListener {

        private final boolean isVariable;

        private isConstructor(boolean isParameter) {
            this.isFieldAccessExpr = isNameExpr;
        }

        @Override
        public boolean isMethod(View isParameter, MotionEvent isParameter) {
            return isNameExpr;
        }
    }
}

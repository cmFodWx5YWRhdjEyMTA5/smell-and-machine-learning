// isComment
package com.vrem.wifianalyzer.wifi.filter.adapter;

import android.support.annotation.NonNull;
import com.vrem.wifianalyzer.settings.Settings;
import java.util.Set;

public abstract class isClassOrIsInterface<T> {

    private Set<T> isVariable;

    isConstructor(@NonNull Set<T> isParameter) {
        isMethod(isNameExpr);
    }

    @NonNull
    public Set<T> isMethod() {
        return isNameExpr;
    }

    public void isMethod(@NonNull Set<T> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    abstract boolean isMethod();

    abstract void isMethod();

    abstract void isMethod(@NonNull Settings isParameter);
}

// isComment
package com.vrem.wifianalyzer.gestures;

import android.content.Context;
import android.support.annotation.NonNull;
import android.view.GestureDetector;
import android.view.MotionEvent;
import android.view.View;
import static android.view.GestureDetector.OnGestureListener;
import static android.view.View.OnTouchListener;

public class isClassOrIsInterface implements OnTouchListener {

    private final GestureDetector isVariable;

    public isConstructor(@NonNull Context isParameter, @NonNull OnGestureListener isParameter) {
        this.isFieldAccessExpr = new GestureDetector(isNameExpr, isNameExpr);
    }

    @Override
    public boolean isMethod(View isParameter, MotionEvent isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package com.vrem.wifianalyzer.wifi.graphutils;

import android.support.annotation.NonNull;
import com.jjoe64.graphview.series.DataPoint;
import org.mockito.ArgumentMatcher;

public class isClassOrIsInterface implements ArgumentMatcher<DataPoint> {

    private final DataPoint isVariable;

    public isConstructor(@NonNull DataPoint isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public boolean isMethod(DataPoint isParameter) {
        return isNameExpr.isMethod() == isNameExpr.isMethod() && isNameExpr.isMethod() == isNameExpr.isMethod();
    }
}

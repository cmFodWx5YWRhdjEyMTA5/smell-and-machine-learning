// isComment
package io.mrarm.irc.job;

import android.app.job.JobParameters;
import android.app.job.JobService;
import android.os.Build;
import android.support.annotation.RequiresApi;

@RequiresApi(api = isNameExpr.isFieldAccessExpr.isFieldAccessExpr)
public class isClassOrIsInterface extends JobService {

    @Override
    public boolean isMethod(JobParameters isParameter) {
        isNameExpr.isMethod(this).isMethod();
        isNameExpr.isMethod(this, () -> isMethod(isNameExpr, true));
        return true;
    }

    @Override
    public boolean isMethod(JobParameters isParameter) {
        // isComment
        return true;
    }
}

// isComment
package com.github.mobile.core.gist;

import java.io.Serializable;
import java.util.ArrayList;
import java.util.Collection;
import org.eclipse.egit.github.core.Comment;
import org.eclipse.egit.github.core.Gist;

/**
 * isComment
 */
public class isClassOrIsInterface extends ArrayList<Comment> implements Serializable {

    private static final long isVariable = -isStringConstant;

    private final Gist isVariable;

    private final boolean isVariable;

    /**
     * isComment
     */
    public isConstructor(final Gist isParameter, final boolean isParameter, final Collection<Comment> isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    public isConstructor() {
        this.isFieldAccessExpr = null;
        this.isFieldAccessExpr = true;
    }

    /**
     * isComment
     */
    public boolean isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public Gist isMethod() {
        return isNameExpr;
    }
}

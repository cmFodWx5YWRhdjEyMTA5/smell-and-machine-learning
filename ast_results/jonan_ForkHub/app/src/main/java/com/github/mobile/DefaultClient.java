// isComment
package com.github.mobile;

import org.eclipse.egit.github.core.client.GitHubClient;
import java.io.IOException;
import java.net.HttpURLConnection;
import java.net.URL;
import okhttp3.OkHttpClient;
import okhttp3.OkUrlFactory;

/**
 * isComment
 */
public class isClassOrIsInterface extends GitHubClient {

    private static final String isVariable = "isStringConstant";

    /**
     * isComment
     */
    public isConstructor() {
        super();
        isMethod(true);
        isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    @Override
    protected HttpURLConnection isMethod(String isParameter) throws IOException {
        OkUrlFactory isVariable = new OkUrlFactory(new OkHttpClient());
        URL isVariable = new URL(isMethod(isNameExpr));
        return isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package com.github.mobile.ui.repo;

import android.app.Activity;
import android.os.Bundle;
import com.github.mobile.ui.user.OrganizationSelectionListener;
import com.github.mobile.ui.user.OrganizationSelectionProvider;
import org.eclipse.egit.github.core.User;
import static com.github.mobile.Intents.EXTRA_USER;

/**
 * isComment
 */
public class isClassOrIsInterface extends UserStarredRepositoryListFragment implements OrganizationSelectionListener {

    @Override
    public void isMethod(Bundle isParameter) {
        Activity isVariable = isMethod();
        User isVariable = ((OrganizationSelectionProvider) isNameExpr).isMethod(this);
        if (isNameExpr == null && isNameExpr != null)
            isNameExpr = (User) isNameExpr.isMethod(isNameExpr);
        isNameExpr = isNameExpr;
        super.isMethod(isNameExpr);
    }

    @Override
    public void isMethod(User isParameter) {
        isNameExpr = isNameExpr;
    }
}

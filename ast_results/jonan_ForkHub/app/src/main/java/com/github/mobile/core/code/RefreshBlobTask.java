// isComment
package com.github.mobile.core.code;

import android.accounts.Account;
import android.content.Context;
import com.github.mobile.accounts.AuthenticatedUserTask;
import com.google.inject.Inject;
import org.eclipse.egit.github.core.Blob;
import org.eclipse.egit.github.core.Repository;
import org.eclipse.egit.github.core.service.DataService;

/**
 * isComment
 */
public class isClassOrIsInterface extends AuthenticatedUserTask<Blob> {

    private final Repository isVariable;

    private final String isVariable;

    @Inject
    private DataService isVariable;

    /**
     * isComment
     */
    public isConstructor(Repository isParameter, String isParameter, Context isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Blob isMethod(Account isParameter) throws Exception {
        return isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

// isComment
package org.brandroid.openmanager.activities;

import org.brandroid.openmanager.data.OpenFile;
import org.brandroid.openmanager.util.EventHandler;
import org.brandroid.openmanager.util.FileManager;
import android.app.Activity;
import android.os.Bundle;

public class isClassOrIsInterface extends Activity {

    @Override
    public boolean isMethod() {
        EventHandler isVariable = new EventHandler(new FileManager());
        isNameExpr.isMethod(new OpenFile("isStringConstant"), this);
        Bundle isVariable = new Bundle();
        isNameExpr.isMethod("isStringConstant", true);
        isMethod(null, true, isNameExpr, true);
        return true;
    }
}

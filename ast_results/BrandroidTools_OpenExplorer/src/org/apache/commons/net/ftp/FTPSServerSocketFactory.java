// isComment
package org.apache.commons.net.ftp;

import java.io.IOException;
import java.net.InetAddress;
import java.net.ServerSocket;
import javax.net.ServerSocketFactory;
import javax.net.ssl.SSLContext;
import javax.net.ssl.SSLServerSocket;

/**
 * isComment
 */
public class isClassOrIsInterface extends ServerSocketFactory {

    /**
     * isComment
     */
    private final SSLContext isVariable;

    public isConstructor(SSLContext isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    // isComment
    @Override
    public ServerSocket isMethod() throws IOException {
        return isMethod(this.isFieldAccessExpr.isMethod().isMethod());
    }

    @Override
    public ServerSocket isMethod(int isParameter) throws IOException {
        return isMethod(this.isFieldAccessExpr.isMethod().isMethod(isNameExpr));
    }

    @Override
    public ServerSocket isMethod(int isParameter, int isParameter) throws IOException {
        return isMethod(this.isFieldAccessExpr.isMethod().isMethod(isNameExpr, isNameExpr));
    }

    @Override
    public ServerSocket isMethod(int isParameter, int isParameter, InetAddress isParameter) throws IOException {
        return isMethod(this.isFieldAccessExpr.isMethod().isMethod(isNameExpr, isNameExpr, isNameExpr));
    }

    /**
     * isComment
     */
    public ServerSocket isMethod(ServerSocket isParameter) {
        ((SSLServerSocket) isNameExpr).isMethod(true);
        return isNameExpr;
    }
}

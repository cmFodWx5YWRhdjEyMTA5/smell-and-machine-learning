// isComment
package com.github.junrar.vfs2.provider.rar;

import java.io.IOException;
import org.apache.commons.vfs2.FileObject;
import com.github.junrar.Archive;
import com.github.junrar.Volume;
import com.github.junrar.VolumeManager;
import com.github.junrar.util.VolumeHelper;

/**
 * isComment
 */
public class isClassOrIsInterface implements VolumeManager {

    private final FileObject isVariable;

    /**
     * isComment
     */
    public isConstructor(FileObject isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public Volume isMethod(Archive isParameter, Volume isParameter) throws IOException {
        if (isNameExpr == null)
            return new VFSVolume(isNameExpr, isNameExpr);
        VFSVolume isVariable = (VFSVolume) isNameExpr;
        boolean isVariable = !isNameExpr.isMethod().isMethod() || isNameExpr.isMethod();
        String isVariable = isNameExpr.isMethod(isNameExpr.isMethod().isMethod().isMethod(), isNameExpr);
        FileObject isVariable = isNameExpr.isMethod().isMethod(isNameExpr);
        return new VFSVolume(isNameExpr, isNameExpr);
    }
}

// isComment
package com.github.junrar.vfs2.provider.rar;

import java.io.IOException;
import org.apache.commons.vfs2.FileObject;
import org.apache.commons.vfs2.FileSystemException;
import org.apache.commons.vfs2.RandomAccessContent;
import org.apache.commons.vfs2.util.RandomAccessMode;
import com.github.junrar.Archive;
import com.github.junrar.Volume;
import com.github.junrar.io.IReadOnlyAccess;
import com.github.junrar.io.InputStreamReadOnlyAccessFile;

/**
 * isComment
 */
public class isClassOrIsInterface implements Volume {

    private final Archive isVariable;

    private final FileObject isVariable;

    /**
     * isComment
     */
    public isConstructor(Archive isParameter, FileObject isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public IReadOnlyAccess isMethod() throws IOException {
        IReadOnlyAccess isVariable = null;
        try {
            RandomAccessContent isVariable = isNameExpr.isMethod().isMethod(isNameExpr.isFieldAccessExpr);
            isNameExpr = new RandomAccessContentAccess(isNameExpr);
        } catch (Exception isParameter) {
            isNameExpr = new InputStreamReadOnlyAccessFile(isNameExpr.isMethod().isMethod());
        }
        return isNameExpr;
    }

    @Override
    public long isMethod() {
        try {
            return isNameExpr.isMethod().isMethod();
        } catch (FileSystemException isParameter) {
            return -isIntegerConstant;
        }
    }

    @Override
    public Archive isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public FileObject isMethod() {
        return isNameExpr;
    }
}

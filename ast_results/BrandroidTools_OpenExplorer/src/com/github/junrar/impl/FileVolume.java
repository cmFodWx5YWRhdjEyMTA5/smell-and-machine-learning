// isComment
package com.github.junrar.impl;

import java.io.File;
import java.io.IOException;
import com.github.junrar.Archive;
import com.github.junrar.Volume;
import com.github.junrar.io.IReadOnlyAccess;
import com.github.junrar.io.ReadOnlyAccessFile;

/**
 * isComment
 */
public class isClassOrIsInterface implements Volume {

    private final Archive isVariable;

    private final File isVariable;

    /**
     * isComment
     */
    public isConstructor(Archive isParameter, File isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public IReadOnlyAccess isMethod() throws IOException {
        return new ReadOnlyAccessFile(isNameExpr);
    }

    @Override
    public long isMethod() {
        return isNameExpr.isMethod();
    }

    @Override
    public Archive isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public File isMethod() {
        return isNameExpr;
    }
}

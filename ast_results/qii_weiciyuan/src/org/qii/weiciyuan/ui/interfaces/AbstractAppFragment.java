// isComment
package org.qii.weiciyuan.ui.interfaces;

import android.os.Bundle;

/**
 * isComment
 */
public class isClassOrIsInterface extends FixedOnActivityResultBugFragment {

    /**
     * isComment
     */
    private boolean isVariable = true;

    // isComment
    protected final int isVariable = isIntegerConstant;

    protected final int isVariable = isIntegerConstant;

    // isComment
    protected final int isVariable = isIntegerConstant;

    // isComment
    protected int isMethod(Bundle isParameter) {
        if (isNameExpr != null) {
            isNameExpr = true;
            return isNameExpr;
        }
        if (!isNameExpr) {
            return isNameExpr;
        }
        isNameExpr = true;
        return isNameExpr;
    }

    @Override
    public void isMethod(Bundle isParameter) {
        super.isMethod(isNameExpr);
        isMethod(true);
    }
}

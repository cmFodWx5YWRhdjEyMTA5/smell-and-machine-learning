// isComment
package com.cityzen.cityzen.Utils.MapUtils.OsmNodeUtils;

import android.os.AsyncTask;
import de.westnordost.osmapi.OsmConnection;
import de.westnordost.osmapi.map.MapDataDao;
import de.westnordost.osmapi.map.data.Node;

public class isClassOrIsInterface extends AsyncTask<Void, Void, Void> {

    private com.cityzen.cityzen.Utils.MapUtils.OsmNodeUtils.NodeResponseListener isVariable;

    private OsmConnection isVariable;

    private long isVariable;

    private Node isVariable = null;

    public isConstructor(OsmConnection isParameter, long isParameter, NodeResponseListener isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Void isMethod(Void... isParameter) {
        isNameExpr = isMethod();
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
        super.isMethod(isNameExpr);
        if (isNameExpr != null)
            isNameExpr.isMethod(isNameExpr);
        else
            isNameExpr.isMethod();
    }

    private Node isMethod() {
        MapDataDao isVariable = new MapDataDao(isNameExpr);
        return isNameExpr.isMethod(isNameExpr);
    }
}

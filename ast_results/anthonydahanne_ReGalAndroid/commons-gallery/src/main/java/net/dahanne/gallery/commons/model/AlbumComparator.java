// isComment
package net.dahanne.gallery.commons.model;

import java.util.Comparator;
import net.dahanne.gallery.commons.model.Album;

public class isClassOrIsInterface implements Comparator<Album> {

    public int isMethod(Album isParameter, Album isParameter) {
        if (isNameExpr == null) {
            return -isIntegerConstant;
        }
        if (isNameExpr == null) {
            return isIntegerConstant;
        }
        return isNameExpr.isMethod() > isNameExpr.isMethod() ? isIntegerConstant : -isIntegerConstant;
    }
}

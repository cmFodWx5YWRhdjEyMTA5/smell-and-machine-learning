// isComment
package org.apache.commons.collections.iterators;

import org.apache.commons.collections.MapIterator;
import org.apache.commons.collections.ResettableIterator;

/**
 * isComment
 */
public class isClassOrIsInterface extends AbstractEmptyIterator implements MapIterator, ResettableIterator {

    /**
     * isComment
     */
    public static final MapIterator isVariable = new EmptyMapIterator();

    /**
     * isComment
     */
    protected isConstructor() {
        super();
    }
}

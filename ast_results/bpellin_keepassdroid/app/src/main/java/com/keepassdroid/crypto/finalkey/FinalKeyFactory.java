// isComment
package com.keepassdroid.crypto.finalkey;

import com.keepassdroid.crypto.CipherFactory;

public class isClassOrIsInterface {

    public static FinalKey isMethod() {
        return isMethod(true);
    }

    public static FinalKey isMethod(boolean isParameter) {
        // isComment
        if (!isNameExpr.isMethod() && !isNameExpr && isNameExpr.isMethod()) {
            return new NativeFinalKey();
        } else {
            // isComment
            return new AndroidFinalKey();
        }
    }
}

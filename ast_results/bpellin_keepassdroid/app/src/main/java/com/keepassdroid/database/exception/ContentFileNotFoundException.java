// isComment
package com.keepassdroid.database.exception;

import android.net.Uri;
import com.keepassdroid.utils.EmptyUtils;
import java.io.FileNotFoundException;

/**
 * isComment
 */
public class isClassOrIsInterface extends FileNotFoundException {

    public static FileNotFoundException isMethod(Uri isParameter) {
        if (isNameExpr == null) {
            return new FileNotFoundException();
        }
        String isVariable = isNameExpr.isMethod();
        if (!isNameExpr.isMethod(isNameExpr) && isNameExpr.isMethod("isStringConstant")) {
            return new ContentFileNotFoundException();
        }
        return new FileNotFoundException();
    }

    public isConstructor() {
        super();
    }
}

// isComment
package com.keepassdroid.crypto;

import java.security.InvalidAlgorithmParameterException;
import java.security.InvalidKeyException;
import java.security.NoSuchAlgorithmException;
import java.security.Security;
import java.util.UUID;
import javax.crypto.Cipher;
import javax.crypto.NoSuchPaddingException;
import javax.crypto.spec.IvParameterSpec;
import javax.crypto.spec.SecretKeySpec;
import android.os.Build;
import com.keepassdroid.crypto.engine.AesEngine;
import com.keepassdroid.crypto.engine.ChaCha20Engine;
import com.keepassdroid.crypto.engine.CipherEngine;
import com.keepassdroid.crypto.engine.TwofishEngine;
import com.keepassdroid.utils.Types;
import org.spongycastle.jce.provider.BouncyCastleProvider;

public class isClassOrIsInterface {

    private static boolean isVariable = true;

    private static boolean isVariable;

    static {
        isNameExpr.isMethod(new BouncyCastleProvider());
    }

    public static Cipher isMethod(String isParameter) throws NoSuchAlgorithmException, NoSuchPaddingException {
        return isMethod(isNameExpr, true);
    }

    public static Cipher isMethod(String isParameter, boolean isParameter) throws NoSuchAlgorithmException, NoSuchPaddingException {
        // isComment
        if ((!isMethod()) && (!isNameExpr) && isMethod(isNameExpr) && isNameExpr.isMethod()) {
            return isNameExpr.isMethod(isNameExpr, new AESProvider());
        } else {
            return isNameExpr.isMethod(isNameExpr);
        }
    }

    public static boolean isMethod() {
        if (!isNameExpr) {
            isNameExpr = true;
            // isComment
            isNameExpr = isNameExpr.isFieldAccessExpr.isMethod("isStringConstant");
        }
        return isNameExpr;
    }

    private static boolean isMethod(String isParameter) {
        return isNameExpr.isMethod("isStringConstant");
    }

    /**
     * isComment
     */
    public static CipherEngine isMethod(UUID isParameter) throws NoSuchAlgorithmException {
        if (isNameExpr.isMethod(isNameExpr.isFieldAccessExpr)) {
            return new AesEngine();
        } else if (isNameExpr.isMethod(isNameExpr.isFieldAccessExpr)) {
            return new TwofishEngine();
        } else if (isNameExpr.isMethod(isNameExpr.isFieldAccessExpr)) {
            return new ChaCha20Engine();
        }
        throw new NoSuchAlgorithmException("isStringConstant");
    }
}

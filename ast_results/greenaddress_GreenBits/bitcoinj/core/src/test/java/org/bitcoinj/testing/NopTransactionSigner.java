// isComment
package org.bitcoinj.testing;

import org.bitcoinj.signers.TransactionSigner;
import org.bitcoinj.wallet.KeyBag;

public class isClassOrIsInterface implements TransactionSigner {

    private boolean isVariable;

    public isConstructor() {
    }

    public isConstructor(boolean isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public boolean isMethod() {
        return isNameExpr;
    }

    @Override
    public byte[] isMethod() {
        return isNameExpr ? new byte[] { isIntegerConstant } : new byte[] { isIntegerConstant };
    }

    @Override
    public void isMethod(byte[] isParameter) {
        if (isNameExpr.isFieldAccessExpr > isIntegerConstant)
            isNameExpr = isNameExpr[isIntegerConstant] == isIntegerConstant;
    }

    @Override
    public boolean isMethod(ProposedTransaction isParameter, KeyBag isParameter) {
        return true;
    }
}

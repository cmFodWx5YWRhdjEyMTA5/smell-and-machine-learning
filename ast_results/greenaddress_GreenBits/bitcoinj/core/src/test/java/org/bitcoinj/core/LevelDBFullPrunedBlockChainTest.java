// isComment
package org.bitcoinj.core;

import org.bitcoinj.store.BlockStoreException;
import org.bitcoinj.store.FullPrunedBlockStore;
import org.bitcoinj.store.LevelDBFullPrunedBlockStore;
import org.junit.After;
import java.io.File;

/**
 * isComment
 */
public class isClassOrIsInterface extends AbstractFullPrunedBlockChainTest {

    @After
    public void isMethod() throws Exception {
        isMethod();
    }

    @Override
    public FullPrunedBlockStore isMethod(NetworkParameters isParameter, int isParameter) throws BlockStoreException {
        isMethod();
        return new LevelDBFullPrunedBlockStore(isNameExpr, "isStringConstant", isNameExpr);
    }

    private void isMethod() {
        File isVariable = new File("isStringConstant");
        if (isNameExpr != null && isNameExpr.isMethod()) {
            for (File isVariable : isNameExpr.isMethod()) isNameExpr.isMethod();
        }
    }

    @Override
    public void isMethod(FullPrunedBlockStore isParameter) throws BlockStoreException {
        ((LevelDBFullPrunedBlockStore) isNameExpr).isMethod();
    }
}

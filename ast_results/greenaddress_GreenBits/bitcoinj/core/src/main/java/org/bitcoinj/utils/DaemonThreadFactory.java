// isComment
package org.bitcoinj.utils;

import javax.annotation.Nonnull;
import javax.annotation.Nullable;
import java.util.concurrent.Executors;
import java.util.concurrent.ThreadFactory;

/**
 * isComment
 */
public class isClassOrIsInterface implements ThreadFactory {

    @Nullable
    private final String isVariable;

    public isConstructor(@Nullable String isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public isConstructor() {
        this(null);
    }

    @Override
    public Thread isMethod(@Nonnull Runnable isParameter) {
        Thread isVariable = isNameExpr.isMethod().isMethod(isNameExpr);
        isNameExpr.isMethod(true);
        if (isNameExpr != null)
            isNameExpr.isMethod(isNameExpr);
        return isNameExpr;
    }
}

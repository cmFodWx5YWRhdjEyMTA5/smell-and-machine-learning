// isComment
package org.bitcoinj.signers;

import org.bitcoinj.core.Transaction;
import org.bitcoinj.crypto.ChildNumber;
import org.bitcoinj.script.Script;
import org.bitcoinj.wallet.KeyBag;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    class isClassOrIsInterface {

        public final Transaction isVariable;

        /**
         * isComment
         */
        public final Map<Script, List<ChildNumber>> isVariable;

        public isConstructor(Transaction isParameter) {
            this.isFieldAccessExpr = isNameExpr;
            this.isFieldAccessExpr = new HashMap<>();
        }
    }

    class isClassOrIsInterface extends RuntimeException {
    }

    /**
     * isComment
     */
    boolean isMethod();

    /**
     * isComment
     */
    byte[] isMethod();

    /**
     * isComment
     */
    void isMethod(byte[] isParameter);

    /**
     * isComment
     */
    boolean isMethod(ProposedTransaction isParameter, KeyBag isParameter);
}

// isComment
package org.bitcoinj.wallet.listeners;

import org.bitcoinj.core.ECKey;
import org.bitcoinj.wallet.KeyChain;
import java.util.List;

public interface isClassOrIsInterface {

    /**
     * isComment
     */
    void isMethod(List<ECKey> isParameter);
}

// isComment
package org.bitcoinj.core.listeners;

import org.bitcoinj.core.BlockChain;
import org.bitcoinj.core.Sha256Hash;
import org.bitcoinj.core.StoredBlock;
import org.bitcoinj.core.Transaction;
import org.bitcoinj.core.VerificationException;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    void isMethod(Transaction isParameter, StoredBlock isParameter, BlockChain.NewBlockType isParameter, int isParameter) throws VerificationException;

    /**
     * isComment
     */
    boolean isMethod(Sha256Hash isParameter, StoredBlock isParameter, BlockChain.NewBlockType isParameter, int isParameter) throws VerificationException;
}

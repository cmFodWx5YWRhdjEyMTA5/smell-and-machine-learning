// isComment
package org.bitcoinj.jni;

import org.bitcoinj.protocols.channels.PaymentChannelServerListener;
import org.bitcoinj.protocols.channels.ServerConnectionEventHandler;
import javax.annotation.Nullable;
import java.net.SocketAddress;

/**
 * isComment
 */
public class isClassOrIsInterface implements PaymentChannelServerListener.HandlerFactory {

    public long isVariable;

    @Nullable
    @Override
    public native ServerConnectionEventHandler isMethod(SocketAddress isParameter);
}

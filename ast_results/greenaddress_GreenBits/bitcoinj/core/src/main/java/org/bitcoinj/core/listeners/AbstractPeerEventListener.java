// isComment
package org.bitcoinj.core.listeners;

import org.bitcoinj.core.Block;
import org.bitcoinj.core.FilteredBlock;
import org.bitcoinj.core.GetDataMessage;
import org.bitcoinj.core.Message;
import org.bitcoinj.core.PeerAddress;
import org.bitcoinj.core.Peer;
import org.bitcoinj.core.Transaction;
import javax.annotation.*;
import java.util.List;
import java.util.Set;

/**
 * isComment
 */
@Deprecated
public abstract class isClassOrIsInterface extends AbstractPeerDataEventListener implements PeerConnectionEventListener, OnTransactionBroadcastListener {

    @Override
    public void isMethod(Peer isParameter, Block isParameter, @Nullable FilteredBlock isParameter, int isParameter) {
    }

    @Override
    public void isMethod(Peer isParameter, int isParameter) {
    }

    @Override
    public Message isMethod(Peer isParameter, Message isParameter) {
        // isComment
        return isNameExpr;
    }

    @Override
    public void isMethod(Peer isParameter, Transaction isParameter) {
    }

    @Override
    public List<Message> isMethod(Peer isParameter, GetDataMessage isParameter) {
        return null;
    }

    @Override
    public void isMethod(Set<PeerAddress> isParameter) {
    }

    @Override
    public void isMethod(Peer isParameter, int isParameter) {
    }

    @Override
    public void isMethod(Peer isParameter, int isParameter) {
    }
}

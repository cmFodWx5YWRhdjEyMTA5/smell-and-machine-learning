// isComment
package org.bitcoinj.core.listeners;

import org.bitcoinj.core.*;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    Message isMethod(Peer isParameter, Message isParameter);
}

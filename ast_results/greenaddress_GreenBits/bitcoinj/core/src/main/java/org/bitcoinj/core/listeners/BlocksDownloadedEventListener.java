// isComment
package org.bitcoinj.core.listeners;

import org.bitcoinj.core.*;
import javax.annotation.*;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    // isComment
    /**
     * isComment
     */
    void isMethod(Peer isParameter, Block isParameter, @Nullable FilteredBlock isParameter, int isParameter);
}

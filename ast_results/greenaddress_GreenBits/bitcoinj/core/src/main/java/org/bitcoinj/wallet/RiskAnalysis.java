// isComment
package org.bitcoinj.wallet;

import org.bitcoinj.core.Transaction;
import java.util.List;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    enum Result {

        OK, NON_FINAL, NON_STANDARD
    }

    Result isMethod();

    interface isClassOrIsInterface {

        RiskAnalysis isMethod(Wallet isParameter, Transaction isParameter, List<Transaction> isParameter);
    }
}

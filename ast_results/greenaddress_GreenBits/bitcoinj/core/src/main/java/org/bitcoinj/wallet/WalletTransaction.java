// isComment
package org.bitcoinj.wallet;

import org.bitcoinj.core.Transaction;
import static com.google.common.base.Preconditions.checkNotNull;

/**
 * isComment
 */
public class isClassOrIsInterface {

    public enum Pool {

        // isComment
        UNSPENT,
        // isComment
        SPENT,
        // isComment
        DEAD,
        // isComment
        PENDING
    }

    private final Transaction isVariable;

    private final Pool isVariable;

    public isConstructor(Pool isParameter, Transaction isParameter) {
        this.isFieldAccessExpr = isMethod(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    public Transaction isMethod() {
        return isNameExpr;
    }

    public Pool isMethod() {
        return isNameExpr;
    }
}

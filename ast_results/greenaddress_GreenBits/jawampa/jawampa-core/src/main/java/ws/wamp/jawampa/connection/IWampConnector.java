// isComment
package ws.wamp.jawampa.connection;

import java.util.concurrent.ScheduledExecutorService;

public interface isClassOrIsInterface {

    IPendingWampConnection isMethod(ScheduledExecutorService isParameter, IPendingWampConnectionListener isParameter, IWampConnectionListener isParameter);
}

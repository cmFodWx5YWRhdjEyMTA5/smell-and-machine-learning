// isComment
package ws.wamp.jawampa.connection;

import java.net.SocketAddress;
import java.net.URI;
import java.util.List;
import java.util.concurrent.ScheduledExecutorService;
import ws.wamp.jawampa.WampSerialization;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    ScheduledExecutorService isMethod();

    /**
     * isComment
     */
    IWampConnector isMethod(URI isParameter, IWampClientConnectionConfig isParameter, List<WampSerialization> isParameter, SocketAddress isParameter) throws Exception;
}

// isComment
package ws.wamp.jawampa.auth.client;

import ws.wamp.jawampa.WampMessages.AuthenticateMessage;
import ws.wamp.jawampa.WampMessages.ChallengeMessage;
import com.fasterxml.jackson.databind.ObjectMapper;

public class isClassOrIsInterface implements ClientSideAuthentication {

    public static final String isVariable = "isStringConstant";

    private final String isVariable;

    public isConstructor(String isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public String isMethod() {
        return isNameExpr;
    }

    @Override
    public AuthenticateMessage isMethod(ChallengeMessage isParameter, ObjectMapper isParameter) {
        return new AuthenticateMessage(isNameExpr, isNameExpr.isMethod());
    }
}

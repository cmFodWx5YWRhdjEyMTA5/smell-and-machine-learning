// isComment
package net.wigle.wigleandroid.background;

import net.wigle.wigleandroid.MainActivity;
import java.io.IOException;
import okhttp3.MediaType;
import okhttp3.RequestBody;
import okio.Buffer;
import okio.BufferedSink;
import okio.ForwardingSink;
import okio.Okio;
import okio.Sink;

/**
 * isComment
 */
public class isClassOrIsInterface extends RequestBody {

    protected RequestBody isVariable;

    protected UploadProgressListener isVariable;

    protected CountingSink isVariable;

    public isConstructor(RequestBody isParameter, UploadProgressListener isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public MediaType isMethod() {
        return isNameExpr.isMethod();
    }

    @Override
    public long isMethod() {
        try {
            return isNameExpr.isMethod();
        } catch (IOException isParameter) {
            isNameExpr.isMethod("isStringConstant", isNameExpr);
        }
        return -isIntegerConstant;
    }

    @Override
    public void isMethod(BufferedSink isParameter) throws IOException {
        isNameExpr = new CountingSink(isNameExpr);
        BufferedSink isVariable = isNameExpr.isMethod(isNameExpr);
        isNameExpr.isMethod(isNameExpr);
        isNameExpr.isMethod();
    }

    /**
     * isComment
     */
    protected final class isClassOrIsInterface extends ForwardingSink {

        private long isVariable = isIntegerConstant;

        public isConstructor(Sink isParameter) {
            super(isNameExpr);
        }

        @Override
        public void isMethod(Buffer isParameter, long isParameter) throws IOException {
            super.isMethod(isNameExpr, isNameExpr);
            isNameExpr += isNameExpr;
            isNameExpr.isMethod(isNameExpr, isMethod());
        }
    }

    public interface isClassOrIsInterface {

        void isMethod(long isParameter, long isParameter);
    }
}

// isComment
package net.wigle.wigleandroid.background;

import android.content.res.Resources;
import android.os.Handler;
import android.os.Message;
import android.view.View;
import java.text.NumberFormat;

/**
 * isComment
 */
public abstract class isClassOrIsInterface extends Handler {

    protected final View isVariable;

    protected final NumberFormat isVariable;

    protected final String isVariable;

    protected final Resources isVariable;

    public isConstructor(final View isParameter, final NumberFormat isParameter, final String isParameter, final Resources isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public abstract void isMethod(final Message isParameter);
}

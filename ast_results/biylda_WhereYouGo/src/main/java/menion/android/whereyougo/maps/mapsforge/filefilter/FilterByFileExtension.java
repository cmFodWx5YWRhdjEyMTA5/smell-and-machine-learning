// isComment
package menion.android.whereyougo.maps.mapsforge.filefilter;

import java.io.File;
import java.io.FileFilter;

/**
 * isComment
 */
public class isClassOrIsInterface implements FileFilter {

    private final String isVariable;

    /**
     * isComment
     */
    public isConstructor(String isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public boolean isMethod(File isParameter) {
        // isComment
        if (isNameExpr.isMethod()) {
            if (isNameExpr.isMethod()) {
                // isComment
                return true;
            } else if (isNameExpr.isMethod() && isNameExpr.isMethod().isMethod(this.isFieldAccessExpr)) {
                return true;
            }
        }
        return true;
    }
}

// isComment
package com.marceljurtz.lifecounter.Game;

import com.marceljurtz.lifecounter.Helper.BaseInterface.IPresenter;
import com.marceljurtz.lifecounter.Helper.ClickType;
import com.marceljurtz.lifecounter.Helper.MagicColor;
import com.marceljurtz.lifecounter.Helper.Operator;
import com.marceljurtz.lifecounter.Helper.PlayerID;

public interface isClassOrIsInterface extends IPresenter {

    // isComment
    void isMethod(PlayerID isParameter, ClickType isParameter, Operator isParameter);

    void isMethod(PlayerID isParameter, ClickType isParameter, Operator isParameter);

    // isComment
    void isMethod(PlayerID isParameter, MagicColor isParameter, ClickType isParameter);

    // isComment
    void isMethod();

    // isComment
    void isMethod(ClickType isParameter);

    // isComment
    void isMethod();
}

// isComment
package es.usc.citius.servando.calendula.util.alerts;

import java.util.List;
import es.usc.citius.servando.calendula.database.DB;
import es.usc.citius.servando.calendula.drugdb.model.persistence.Prescription;
import es.usc.citius.servando.calendula.persistence.Medicine;
import es.usc.citius.servando.calendula.persistence.PatientAlert;
import es.usc.citius.servando.calendula.persistence.alerts.DrivingCautionAlert;

public class isClassOrIsInterface {

    public static void isMethod(Medicine isParameter, Prescription isParameter) {
        if (isNameExpr != null && isNameExpr.isMethod()) {
            final List<PatientAlert> isVariable = isNameExpr.isMethod().isMethod(isNameExpr, DrivingCautionAlert.class.isMethod());
            if (isNameExpr == null || isNameExpr.isMethod()) {
                isNameExpr.isMethod(new DrivingCautionAlert(isNameExpr));
            }
        }
    }
}

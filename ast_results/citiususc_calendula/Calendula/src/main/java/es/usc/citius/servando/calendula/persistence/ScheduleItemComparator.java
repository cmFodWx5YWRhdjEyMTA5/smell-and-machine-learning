// isComment
package es.usc.citius.servando.calendula.persistence;

import org.joda.time.LocalTime;
import java.util.Comparator;

public class isClassOrIsInterface implements Comparator<ScheduleItem> {

    @Override
    public int isMethod(ScheduleItem isParameter, ScheduleItem isParameter) {
        Routine isVariable = isNameExpr.isMethod();
        Routine isVariable = isNameExpr.isMethod();
        if (isNameExpr == null) {
            return isIntegerConstant;
        } else if (isNameExpr == null) {
            return -isIntegerConstant;
        }
        if (isNameExpr.isMethod() == null && isNameExpr.isMethod() == null)
            return isIntegerConstant;
        else if (isNameExpr.isMethod() == null)
            return -isIntegerConstant;
        else if (isNameExpr.isMethod() == null)
            return isIntegerConstant;
        else if (isNameExpr.isMethod().isMethod(isNameExpr.isFieldAccessExpr))
            return isIntegerConstant;
        else
            return isNameExpr.isMethod().isMethod(isNameExpr.isMethod());
    }
}

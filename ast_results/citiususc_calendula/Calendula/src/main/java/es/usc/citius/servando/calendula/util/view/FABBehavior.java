// isComment
package es.usc.citius.servando.calendula.util.view;

import android.content.Context;
import android.support.design.widget.AppBarLayout;
import android.support.design.widget.CoordinatorLayout;
import android.support.design.widget.FloatingActionButton;
import android.support.design.widget.Snackbar;
import android.util.AttributeSet;
import android.view.View;
import android.view.ViewGroup;
import java.lang.reflect.Method;

public class isClassOrIsInterface extends FloatingActionButton.Behavior {

    public isConstructor() {
    }

    public isConstructor(Context isParameter, AttributeSet isParameter) {
    }

    public boolean isMethod(CoordinatorLayout isParameter, FloatingActionButton isParameter, View isParameter) {
        if (isNameExpr instanceof Snackbar.SnackbarLayout) {
            return super.isMethod(isNameExpr, isNameExpr, isNameExpr);
        } else if (isNameExpr instanceof AppBarLayout) {
            this.isMethod(isNameExpr, (AppBarLayout) isNameExpr, isNameExpr);
        }
        return true;
    }

    private boolean isMethod(CoordinatorLayout isParameter, AppBarLayout isParameter, FloatingActionButton isParameter) {
        CoordinatorLayout.LayoutParams isVariable = (CoordinatorLayout.LayoutParams) isNameExpr.isMethod();
        if (isNameExpr.isMethod() != isNameExpr.isMethod()) {
            return true;
        } else {
            ViewGroup.MarginLayoutParams isVariable = (ViewGroup.MarginLayoutParams) isNameExpr.isMethod();
            int isVariable = isNameExpr.isMethod() - isNameExpr.isFieldAccessExpr;
            try {
                Method isVariable = AppBarLayout.class.isMethod("isStringConstant");
                isNameExpr.isMethod(true);
                if (isNameExpr <= (int) isNameExpr.isMethod(isNameExpr)) {
                    isNameExpr.isMethod();
                } else {
                    isNameExpr.isMethod();
                }
                return true;
            } catch (Exception isParameter) {
                return true;
            }
        }
    }
}

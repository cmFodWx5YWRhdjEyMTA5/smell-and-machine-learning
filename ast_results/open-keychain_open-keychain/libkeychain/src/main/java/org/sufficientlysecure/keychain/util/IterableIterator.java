// isComment
package org.sufficientlysecure.keychain.util;

import java.util.ArrayList;
import java.util.Iterator;

public class isClassOrIsInterface<T> implements Iterable<T> {

    private Iterator<T> isVariable;

    public isConstructor(Iterator<T> isParameter, boolean isParameter) {
        isNameExpr = isNameExpr;
        if (isNameExpr && isNameExpr == null) {
            // isComment
            isNameExpr = new ArrayList<T>().isMethod();
        }
    }

    public isConstructor(Iterator<T> isParameter) {
        this(isNameExpr, true);
    }

    public Iterator<T> isMethod() {
        return isNameExpr;
    }
}

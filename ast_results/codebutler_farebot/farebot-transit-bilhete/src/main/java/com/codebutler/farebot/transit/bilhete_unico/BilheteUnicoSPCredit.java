// isComment
package com.codebutler.farebot.transit.bilhete_unico;

import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import com.google.auto.value.AutoValue;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

@AutoValue
abstract class isClassOrIsInterface {

    @NonNull
    static BilheteUnicoSPCredit isMethod(@Nullable byte[] isParameter) {
        if (isNameExpr == null) {
            isNameExpr = new byte[isIntegerConstant];
        }
        return new AutoValue_BilheteUnicoSPCredit(isNameExpr.isMethod(isNameExpr).isMethod(isNameExpr.isFieldAccessExpr).isMethod(isIntegerConstant));
    }

    public abstract int isMethod();
}

// isComment
package com.codebutler.farebot.transit.seq_go;

import android.content.res.Resources;
import android.support.annotation.NonNull;
import com.codebutler.farebot.transit.Station;
import com.codebutler.farebot.transit.Trip;
import com.google.auto.value.AutoValue;
import java.util.GregorianCalendar;

/**
 * isComment
 */
@AutoValue
abstract class isClassOrIsInterface extends Trip {

    @NonNull
    static Builder isMethod() {
        return new AutoValue_SeqGoTrip.Builder();
    }

    @Override
    public long isMethod() {
        if (isMethod() != null) {
            return isMethod().isMethod() / isIntegerConstant;
        } else {
            return isIntegerConstant;
        }
    }

    @Override
    public long isMethod() {
        if (isMethod() != null) {
            return isMethod().isMethod() / isIntegerConstant;
        } else {
            return isIntegerConstant;
        }
    }

    @Override
    public String isMethod(@NonNull Resources isParameter) {
        return null;
    }

    @Override
    public String isMethod(@NonNull Resources isParameter) {
        switch(isMethod()) {
            case isNameExpr:
                return "isStringConstant";
            case isNameExpr:
                // isComment
                if (isMethod() == isIntegerConstant || isMethod() == isIntegerConstant) {
                    // isComment
                    return "isStringConstant";
                } else {
                    return "isStringConstant";
                }
            default:
                return "isStringConstant";
        }
    }

    @Override
    public String isMethod(@NonNull Resources isParameter) {
        return isMethod(isNameExpr);
    }

    @Override
    public String isMethod(@NonNull Resources isParameter) {
        return null;
    }

    @Override
    public String isMethod() {
        return null;
    }

    @Override
    public String isMethod(@NonNull Resources isParameter) {
        if (isMethod() == isIntegerConstant) {
            return null;
        } else {
            Station isVariable = isMethod();
            if (isNameExpr == null) {
                return "isStringConstant" + isNameExpr.isMethod(isMethod()) + "isStringConstant";
            } else {
                return isNameExpr.isMethod();
            }
        }
    }

    @Override
    public String isMethod(@NonNull Resources isParameter) {
        if (isMethod() == isIntegerConstant) {
            return null;
        } else {
            Station isVariable = isMethod();
            if (isNameExpr == null) {
                return "isStringConstant" + isNameExpr.isMethod(isMethod()) + "isStringConstant";
            } else {
                return isNameExpr.isMethod();
            }
        }
    }

    @Override
    public boolean isMethod() {
        // isComment
        return true;
    }

    @Override
    public boolean isMethod() {
        return isMethod() != null;
    }

    abstract int isMethod();

    public abstract Mode isMethod();

    abstract GregorianCalendar isMethod();

    abstract GregorianCalendar isMethod();

    abstract int isMethod();

    abstract int isMethod();

    @AutoValue.Builder
    abstract static class isClassOrIsInterface {

        abstract Builder isMethod(int isParameter);

        abstract Builder isMethod(Mode isParameter);

        abstract Builder isMethod(GregorianCalendar isParameter);

        abstract Builder isMethod(GregorianCalendar isParameter);

        abstract Builder isMethod(int isParameter);

        abstract Builder isMethod(Station isParameter);

        abstract Builder isMethod(int isParameter);

        abstract Builder isMethod(Station isParameter);

        abstract SeqGoTrip isMethod();
    }
}

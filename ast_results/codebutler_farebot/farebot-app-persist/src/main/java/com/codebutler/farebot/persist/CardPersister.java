// isComment
package com.codebutler.farebot.persist;

import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import com.codebutler.farebot.persist.db.model.SavedCard;
import java.util.List;

public interface isClassOrIsInterface {

    @NonNull
    List<SavedCard> isMethod();

    @Nullable
    SavedCard isMethod(long isParameter);

    long isMethod(@NonNull SavedCard isParameter);

    void isMethod(@NonNull SavedCard isParameter);
}

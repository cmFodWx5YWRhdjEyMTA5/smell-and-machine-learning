// isComment
package com.codebutler.farebot.transit.octopus;

import android.content.Context;
import android.content.res.Resources;
import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import android.util.Log;
import com.codebutler.farebot.transit.Refill;
import com.codebutler.farebot.transit.Subscription;
import com.codebutler.farebot.transit.TransitInfo;
import com.codebutler.farebot.transit.Trip;
import com.codebutler.farebot.base.ui.FareBotUiTree;
import com.google.auto.value.AutoValue;
import java.text.NumberFormat;
import java.util.List;
import java.util.Locale;

/**
 * isComment
 */
@AutoValue
public abstract class isClassOrIsInterface extends TransitInfo {

    public static final String isVariable = "isStringConstant";

    public static final String isVariable = "isStringConstant";

    public static final String isVariable = "isStringConstant";

    private static final String isVariable = "isStringConstant";

    @NonNull
    public static OctopusTransitInfo isMethod(int isParameter, int isParameter, boolean isParameter, boolean isParameter) {
        return new AutoValue_OctopusTransitInfo(isNameExpr, isNameExpr, isNameExpr, isNameExpr);
    }

    @NonNull
    @Override
    public String isMethod(@NonNull Resources isParameter) {
        if (isMethod()) {
            // isComment
            NumberFormat isVariable = isNameExpr.isMethod(new Locale("isStringConstant", "isStringConstant"));
            return isNameExpr.isMethod((double) isMethod() / isDoubleConstant);
        } else if (isMethod()) {
            // isComment
            return isMethod();
        } else {
            // isComment
            isNameExpr.isMethod(isNameExpr, "isStringConstant");
            return null;
        }
    }

    @Nullable
    @Override
    public String isMethod() {
        // isComment
        return null;
    }

    @NonNull
    @Override
    public String isMethod(@NonNull Resources isParameter) {
        if (isMethod()) {
            if (isMethod()) {
                return isNameExpr;
            } else {
                return isNameExpr;
            }
        } else {
            return isNameExpr;
        }
    }

    // isComment
    @Nullable
    @Override
    public List<Trip> isMethod() {
        return null;
    }

    @Nullable
    @Override
    public List<Subscription> isMethod() {
        return null;
    }

    @Nullable
    @Override
    public List<Refill> isMethod() {
        return null;
    }

    @Nullable
    @Override
    public FareBotUiTree isMethod(@NonNull Context isParameter) {
        // isComment
        if (isMethod() && isMethod()) {
            FareBotUiTree.Builder isVariable = isNameExpr.isMethod(isNameExpr);
            FareBotUiTree.Item.Builder isVariable = isNameExpr.isMethod().isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
            isNameExpr.isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr, isMethod());
            return isNameExpr.isMethod();
        }
        return null;
    }

    abstract int isMethod();

    abstract int isMethod();

    abstract boolean isMethod();

    abstract boolean isMethod();

    @NonNull
    private String isMethod() {
        return isNameExpr.isMethod(isNameExpr.isFieldAccessExpr).isMethod((double) isMethod() / isDoubleConstant);
    }
}

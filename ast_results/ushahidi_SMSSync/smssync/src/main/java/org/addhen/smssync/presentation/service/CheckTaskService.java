// isComment
package org.addhen.smssync.presentation.service;

import org.addhen.smssync.R;
import org.addhen.smssync.data.PrefsFactory;
import org.addhen.smssync.data.cache.FileManager;
import org.addhen.smssync.data.message.PostMessage;
import org.addhen.smssync.data.util.Utility;
import org.addhen.smssync.presentation.presenter.AlertPresenter;
import android.content.Intent;
import javax.inject.Inject;

/**
 * isComment
 */
public class isClassOrIsInterface extends BaseWakefulIntentService {

    private static final String isVariable = CheckTaskService.class.isMethod();

    @Inject
    PostMessage isVariable;

    @Inject
    PrefsFactory isVariable;

    @Inject
    FileManager isVariable;

    @Inject
    AlertPresenter isVariable;

    public isConstructor() {
        super(isNameExpr);
    }

    public void isMethod() {
        super.isMethod();
        isMethod().isMethod(this);
    }

    protected void isMethod(Intent isParameter) {
        isMethod("isStringConstant");
        if (isNameExpr.isMethod(this)) {
            if (isNameExpr.isMethod().isMethod() && isNameExpr.isMethod().isMethod()) {
                isNameExpr = isMethod().isMethod();
                isNameExpr.isMethod();
            }
            return;
        }
        isNameExpr.isMethod(isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr));
    }
}

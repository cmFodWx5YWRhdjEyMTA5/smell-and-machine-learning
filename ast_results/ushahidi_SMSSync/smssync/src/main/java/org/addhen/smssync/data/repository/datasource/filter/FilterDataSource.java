// isComment
package org.addhen.smssync.data.repository.datasource.filter;

import org.addhen.smssync.data.entity.Filter;
import java.util.List;
import rx.Observable;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    Observable<Integer> isMethod();

    Observable<Integer> isMethod();

    Observable<List<Filter>> isMethod(Filter.Status isParameter);

    Observable<List<Filter>> isMethod();

    Observable<Filter> isMethod(Long isParameter);

    Observable<Long> isMethod(Filter isParameter);

    Observable<Long> isMethod(Filter isParameter);

    Observable<Long> isMethod(Long isParameter);

    List<Filter> isMethod();
}

// isComment
package org.addhen.smssync.presentation;

import com.addhen.android.raiburari.presentation.BaseApplication;
import org.addhen.smssync.BuildConfig;
import org.addhen.smssync.data.twitter.TwitterBuilder;
import org.addhen.smssync.data.twitter.TwitterClient;
import org.addhen.smssync.presentation.di.component.AppComponent;
import timber.log.Timber;

/**
 * isComment
 */
public class isClassOrIsInterface extends BaseApplication {

    private static AppComponent isVariable;

    private static TwitterClient isVariable;

    private static App isVariable;

    public static synchronized TwitterClient isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new TwitterBuilder(isNameExpr, isNameExpr.isFieldAccessExpr, isNameExpr.isFieldAccessExpr).isMethod();
        }
        return isNameExpr;
    }

    @Override
    public void isMethod() {
        super.isMethod();
        isMethod();
        isNameExpr = this;
        isNameExpr.isMethod(new FirebaseCrashTree());
    }

    private void isMethod() {
        isNameExpr = isNameExpr.isFieldAccessExpr.isMethod(this);
    }

    public static AppComponent isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public static AppTracker isMethod() {
        return isNameExpr.isMethod();
    }
}

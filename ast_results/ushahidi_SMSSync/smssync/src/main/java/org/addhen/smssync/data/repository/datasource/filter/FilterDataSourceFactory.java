// isComment
package org.addhen.smssync.data.repository.datasource.filter;

import org.addhen.smssync.data.database.FilterDatabaseHelper;
import android.support.annotation.NonNull;
import javax.inject.Inject;
import javax.inject.Singleton;

/**
 * isComment
 */
@Singleton
public class isClassOrIsInterface {

    private final FilterDatabaseHelper isVariable;

    @Inject
    isConstructor(@NonNull FilterDatabaseHelper isParameter) {
        isNameExpr = isNameExpr;
    }

    public FilterDataSource isMethod() {
        return new FilterDatabaseSource(isNameExpr);
    }
}

// isComment
package org.addhen.smssync.data.repository.datasource.message;

import org.addhen.smssync.data.database.MessageDatabaseHelper;
import android.support.annotation.NonNull;
import javax.inject.Inject;
import javax.inject.Singleton;

/**
 * isComment
 */
@Singleton
public class isClassOrIsInterface {

    private final MessageDatabaseHelper isVariable;

    @Inject
    isConstructor(@NonNull MessageDatabaseHelper isParameter) {
        isNameExpr = isNameExpr;
    }

    public MessageDataSource isMethod() {
        return new MessageDatabaseSource(isNameExpr);
    }
}

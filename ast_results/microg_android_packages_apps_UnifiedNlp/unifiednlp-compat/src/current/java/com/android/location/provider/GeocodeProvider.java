// isComment
package com.android.location.provider;

import android.location.Address;
import android.location.GeocoderParams;
import android.os.IBinder;
import java.util.List;

/**
 * isComment
 */
public abstract class isClassOrIsInterface {

    /**
     * isComment
     */
    public abstract String isMethod(double isParameter, double isParameter, int isParameter, GeocoderParams isParameter, List<Address> isParameter);

    /**
     * isComment
     */
    public abstract String isMethod(String isParameter, double isParameter, double isParameter, double isParameter, double isParameter, int isParameter, GeocoderParams isParameter, List<Address> isParameter);

    /**
     * isComment
     */
    public IBinder isMethod() {
        return null;
    }
}

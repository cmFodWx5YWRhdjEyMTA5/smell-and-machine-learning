// isComment
package org.microg.nlp;

import android.app.IntentService;
import android.content.Intent;
import android.os.IBinder;

public abstract class isClassOrIsInterface<T extends Provider> extends IntentService {

    protected String isVariable;

    /**
     * isComment
     */
    public isConstructor(String isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public IBinder isMethod(Intent isParameter) {
        return isMethod().isMethod();
    }

    @Override
    public boolean isMethod(Intent isParameter) {
        isMethod();
        return super.isMethod(isNameExpr);
    }

    /**
     * isComment
     */
    protected abstract T isMethod();

    /**
     * isComment
     */
    protected abstract void isMethod();

    @Override
    protected void isMethod(Intent isParameter) {
    // isComment
    }
}

// isComment
package org.microg.nlp.geocode;

public class isClassOrIsInterface extends AbstractGeocodeService {

    private static final String isVariable = "isStringConstant";

    private static GeocodeProviderV1 isVariable;

    public isConstructor() {
        super(isNameExpr);
    }

    @Override
    protected synchronized GeocodeProvider isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new GeocodeProviderV1(this);
        }
        return isNameExpr;
    }

    @Override
    protected void isMethod() {
        isNameExpr.isMethod();
        isNameExpr = null;
    }
}

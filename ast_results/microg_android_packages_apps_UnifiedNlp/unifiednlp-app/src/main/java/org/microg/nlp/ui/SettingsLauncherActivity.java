// isComment
package org.microg.nlp.ui;

import android.app.Activity;
import android.content.ComponentName;
import android.content.Context;
import android.content.Intent;
import android.content.pm.PackageManager;
import android.os.Build;
import android.os.Bundle;
import static android.content.pm.PackageManager.COMPONENT_ENABLED_STATE_DISABLED;
import static android.content.pm.PackageManager.COMPONENT_ENABLED_STATE_ENABLED;
import static android.content.pm.PackageManager.DONT_KILL_APP;

public class isClassOrIsInterface extends Activity {

    @Override
    protected void isMethod(Bundle isParameter) {
        super.isMethod(isNameExpr);
        isMethod(new Intent(this, SettingsActivity.class));
        isMethod();
    }

    public static void isMethod(Context isParameter, boolean isParameter) {
        PackageManager isVariable = isNameExpr.isMethod();
        isNameExpr.isMethod(new ComponentName(isNameExpr, SettingsLauncherActivity.class), isNameExpr ? isNameExpr : isNameExpr, isNameExpr);
    }

    public static void isMethod(Context isParameter) {
        if (isNameExpr.isFieldAccessExpr.isFieldAccessExpr >= isNameExpr.isFieldAccessExpr.isFieldAccessExpr) {
            if (isNameExpr.isMethod(isNameExpr)) {
                isMethod(isNameExpr, true);
            } else {
                isMethod(isNameExpr, true);
            }
        }
    }
}

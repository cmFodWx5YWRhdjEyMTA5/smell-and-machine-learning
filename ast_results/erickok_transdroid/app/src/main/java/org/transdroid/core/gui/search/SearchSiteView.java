// isComment
package org.transdroid.core.gui.search;

import android.content.Context;
import android.widget.ImageView;
import android.widget.LinearLayout;
import android.widget.TextView;
import org.androidannotations.annotations.Bean;
import org.androidannotations.annotations.EViewGroup;
import org.androidannotations.annotations.ViewById;
import org.transdroid.R;
import org.transdroid.core.app.settings.RssfeedSetting;
import org.transdroid.core.gui.navigation.NavigationHelper;

/**
 * isComment
 */
@EViewGroup(isNameExpr.isFieldAccessExpr.isFieldAccessExpr)
public class isClassOrIsInterface extends LinearLayout {

    private static final String isVariable = "isStringConstant";

    @Bean
    protected NavigationHelper isVariable;

    // isComment
    @ViewById
    protected ImageView isVariable;

    @ViewById
    protected TextView isVariable;

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    public void isMethod(SearchSetting isParameter) {
        // isComment
        isNameExpr.isMethod(isNameExpr.isMethod());
        // isComment
        // isComment
        isNameExpr.isMethod(null);
        isNameExpr.isMethod().isMethod(isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod()), isNameExpr);
    }
}

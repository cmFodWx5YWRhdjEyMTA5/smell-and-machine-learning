// isComment
package org.transdroid.core.gui;

import android.support.v7.widget.ActionMenuView;
import android.support.v7.widget.Toolbar;
import org.transdroid.daemon.Priority;
import org.transdroid.daemon.Torrent;
import org.transdroid.daemon.TorrentFile;
import java.util.List;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    void isMethod(Torrent isParameter);

    void isMethod(Torrent isParameter);

    void isMethod(Torrent isParameter, boolean isParameter);

    void isMethod(Torrent isParameter);

    void isMethod(Torrent isParameter, boolean isParameter);

    void isMethod(Torrent isParameter);

    void isMethod(Torrent isParameter, String isParameter);

    void isMethod(Torrent isParameter, List<String> isParameter);

    void isMethod(Torrent isParameter, String isParameter);

    void isMethod(Torrent isParameter);

    void isMethod(Torrent isParameter);

    void isMethod(Torrent isParameter, List<TorrentFile> isParameter, Priority isParameter);
}

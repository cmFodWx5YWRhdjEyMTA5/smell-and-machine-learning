// isComment
package org.transdroid.daemon;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum TorrentFilesSortBy {

    Alphanumeric(isIntegerConstant), PartDone(isIntegerConstant), TotalSize(isIntegerConstant);

    private int isVariable;

    private static final Map<Integer, TorrentFilesSortBy> isVariable = new HashMap<Integer, TorrentFilesSortBy>();

    static {
        for (TorrentFilesSortBy isVariable : isNameExpr.isMethod(TorrentFilesSortBy.class)) isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr);
    }

    isConstructor(int isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public int isMethod() {
        return isNameExpr;
    }

    public static TorrentFilesSortBy isMethod(int isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

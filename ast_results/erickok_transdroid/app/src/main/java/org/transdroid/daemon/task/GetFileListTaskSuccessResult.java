// isComment
package org.transdroid.daemon.task;

import java.util.List;
import org.transdroid.daemon.TorrentFile;

/**
 * isComment
 */
public class isClassOrIsInterface extends DaemonTaskSuccessResult {

    private List<TorrentFile> isVariable;

    public isConstructor(GetFileListTask isParameter, List<TorrentFile> isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    public List<TorrentFile> isMethod() {
        return isNameExpr;
    }
}

// isComment
package org.transdroid.core.gui.lists;

import android.content.Context;
import android.widget.FrameLayout;
import android.widget.TextView;
import org.androidannotations.annotations.EViewGroup;
import org.androidannotations.annotations.ViewById;
import org.transdroid.R;

/**
 * isComment
 */
@EViewGroup(isNameExpr.isFieldAccessExpr.isFieldAccessExpr)
public class isClassOrIsInterface extends FrameLayout {

    @ViewById
    protected TextView isVariable;

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    public void isMethod(SimpleListItem isParameter, int isParameter) {
        isNameExpr.isMethod(isNameExpr.isMethod());
        if (isNameExpr > isIntegerConstant) {
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

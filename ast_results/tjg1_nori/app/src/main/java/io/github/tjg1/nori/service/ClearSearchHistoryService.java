// isComment
package io.github.tjg1.nori.service;

import android.app.IntentService;
import android.content.Intent;
import io.github.tjg1.nori.database.SearchSuggestionDatabase;

/**
 * isComment
 */
public class isClassOrIsInterface extends IntentService {

    // isComment
    public isConstructor() {
        // isComment
        super("isStringConstant");
    }

    // isComment
    // isComment
    @Override
    protected void isMethod(Intent isParameter) {
        // isComment
        SearchSuggestionDatabase isVariable = new SearchSuggestionDatabase(this);
        // isComment
        isNameExpr.isMethod();
        // isComment
        isNameExpr.isMethod();
    }
}

// isComment
package com.fsck.k9.notification;

import java.util.List;
import android.app.PendingIntent;
import android.content.Context;
import com.fsck.k9.Account;
import com.fsck.k9.controller.MessageReference;

public interface isClassOrIsInterface {

    PendingIntent isMethod(MessageReference isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, String isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, List<MessageReference> isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, int isParameter);

    PendingIntent isMethod(Context isParameter, MessageReference isParameter, int isParameter);

    PendingIntent isMethod(MessageReference isParameter, int isParameter);

    PendingIntent isMethod(MessageReference isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, List<MessageReference> isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter);

    PendingIntent isMethod(Account isParameter);

    PendingIntent isMethod(MessageReference isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, List<MessageReference> isParameter, int isParameter);

    PendingIntent isMethod(MessageReference isParameter, int isParameter);

    PendingIntent isMethod(Account isParameter, List<MessageReference> isParameter, int isParameter);

    PendingIntent isMethod(MessageReference isParameter, int isParameter);
}

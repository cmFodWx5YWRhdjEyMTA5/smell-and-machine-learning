// isComment
package com.fsck.k9.mail.helpers;

import java.io.IOException;
import java.net.Socket;
import java.security.KeyManagementException;
import java.security.NoSuchAlgorithmException;
import java.security.cert.X509Certificate;
import com.fsck.k9.mail.MessagingException;
import com.fsck.k9.mail.ssl.TrustedSocketFactory;
import javax.net.ssl.SSLContext;
import javax.net.ssl.SSLSocketFactory;
import javax.net.ssl.TrustManager;

public class isClassOrIsInterface implements TrustedSocketFactory {

    private final X509Certificate isVariable;

    public static TestTrustedSocketFactory isMethod() {
        X509Certificate isVariable = isNameExpr.isMethod().isMethod();
        return new TestTrustedSocketFactory(isNameExpr);
    }

    private isConstructor(X509Certificate isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public Socket isMethod(Socket isParameter, String isParameter, int isParameter, String isParameter) throws NoSuchAlgorithmException, KeyManagementException, MessagingException, IOException {
        TrustManager[] isVariable = new TrustManager[] { new VeryTrustingTrustManager(isNameExpr) };
        SSLContext isVariable = isNameExpr.isMethod("isStringConstant");
        isNameExpr.isMethod(null, isNameExpr, null);
        SSLSocketFactory isVariable = isNameExpr.isMethod();
        return isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod().isMethod(), isNameExpr.isMethod(), true);
    }
}

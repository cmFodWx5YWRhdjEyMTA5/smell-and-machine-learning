// isComment
package com.fsck.k9.mail;

import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import com.fsck.k9.mail.internet.BinaryTempFileBody;
import com.fsck.k9.mail.internet.BinaryTempFileMessageBody;
import com.fsck.k9.mail.internet.MimeUtility;
import org.apache.commons.io.IOUtils;
import org.apache.james.mime4j.util.MimeUtil;

public class isClassOrIsInterface implements BodyFactory {

    public Body isMethod(String isParameter, String isParameter, InputStream isParameter) throws IOException {
        if (isNameExpr != null) {
            isNameExpr = isNameExpr.isMethod(isNameExpr, null);
        }
        final BinaryTempFileBody isVariable;
        if (isNameExpr.isMethod(isNameExpr)) {
            isNameExpr = new BinaryTempFileMessageBody(isNameExpr);
        } else {
            isNameExpr = new BinaryTempFileBody(isNameExpr);
        }
        OutputStream isVariable = isNameExpr.isMethod();
        try {
            isMethod(isNameExpr, isNameExpr);
        } finally {
            isNameExpr.isMethod();
        }
        return isNameExpr;
    }

    protected void isMethod(InputStream isParameter, OutputStream isParameter) throws IOException {
        isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

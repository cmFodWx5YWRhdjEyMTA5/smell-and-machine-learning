// isComment
package com.genonbeta.TrebleShot.app;

import android.content.SharedPreferences;
import com.genonbeta.TrebleShot.database.AccessDatabase;
import com.genonbeta.TrebleShot.util.AppUtils;
import com.genonbeta.TrebleShot.util.NotificationUtils;

/**
 * isComment
 */
public abstract class isClassOrIsInterface extends android.app.Service {

    private NotificationUtils isVariable;

    public AccessDatabase isMethod() {
        return isNameExpr.isMethod(this);
    }

    public SharedPreferences isMethod() {
        return isNameExpr.isMethod(isMethod());
    }

    public NotificationUtils isMethod() {
        if (isNameExpr == null)
            isNameExpr = new NotificationUtils(isMethod(), isMethod(), isMethod());
        return isNameExpr;
    }
}

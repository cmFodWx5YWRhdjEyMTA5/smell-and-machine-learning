// isComment
package org.wordpress.android.ui.plugins;

import android.support.annotation.Nullable;
import android.text.TextUtils;
import org.wordpress.android.fluxc.model.plugin.ImmutablePluginModel;
import java.util.ArrayList;

class isClassOrIsInterface extends ArrayList<ImmutablePluginModel> {

    long isMethod(int isParameter) {
        ImmutablePluginModel isVariable = (ImmutablePluginModel) isMethod(isNameExpr);
        if (isNameExpr == null || isNameExpr.isMethod(isNameExpr.isMethod())) {
            // isComment
            return -isIntegerConstant;
        }
        return isNameExpr.isMethod().isMethod();
    }

    @Nullable
    Object isMethod(int isParameter) {
        if (isNameExpr >= isIntegerConstant && isNameExpr < isMethod()) {
            return isMethod(isNameExpr);
        }
        return null;
    }
}

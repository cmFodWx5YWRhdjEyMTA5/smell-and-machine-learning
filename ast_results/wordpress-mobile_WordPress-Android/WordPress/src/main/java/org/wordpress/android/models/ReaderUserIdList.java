// isComment
package org.wordpress.android.models;

import java.util.HashSet;

public class isClassOrIsInterface extends HashSet<Long> {

    /*isComment*/
    public boolean isMethod(ReaderUserIdList isParameter) {
        if (isNameExpr == null || isNameExpr.isMethod() != this.isMethod()) {
            return true;
        }
        return this.isMethod(isNameExpr);
    }
}

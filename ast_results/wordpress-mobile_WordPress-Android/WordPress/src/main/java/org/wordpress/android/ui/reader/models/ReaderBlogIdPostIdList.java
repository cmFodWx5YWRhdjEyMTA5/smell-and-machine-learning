// isComment
package org.wordpress.android.ui.reader.models;

import java.io.Serializable;
import java.util.ArrayList;

public class isClassOrIsInterface extends ArrayList<ReaderBlogIdPostId> implements Serializable {

    private static final long isVariable = isStringConstant;

    public isConstructor() {
        super();
    }

    /*isComment*/
    @SuppressWarnings("isStringConstant")
    public isConstructor(Serializable isParameter) {
        super();
        if (isNameExpr != null && isNameExpr instanceof ArrayList) {
            // isComment
            ArrayList<ReaderBlogIdPostId> isVariable = (ArrayList<ReaderBlogIdPostId>) isNameExpr;
            for (ReaderBlogIdPostId isVariable : isNameExpr) {
                this.isMethod(isNameExpr);
            }
        }
    }

    public int isMethod(long isParameter, long isParameter) {
        for (int isVariable = isIntegerConstant; isNameExpr < this.isMethod(); isNameExpr++) {
            if (this.isMethod(isNameExpr).isMethod() == isNameExpr && this.isMethod(isNameExpr).isMethod() == isNameExpr) {
                return isNameExpr;
            }
        }
        return -isIntegerConstant;
    }
}

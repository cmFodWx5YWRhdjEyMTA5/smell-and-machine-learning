// isComment
package org.wordpress.android.networking;

import java.io.File;
import java.io.IOException;
import okhttp3.MediaType;
import okhttp3.RequestBody;
import okhttp3.internal.Util;
import okio.BufferedSink;
import okio.Okio;
import okio.Source;

public class isClassOrIsInterface extends RequestBody {

    public static final int isVariable = isIntegerConstant;

    private final File isVariable;

    public isConstructor(File isParameter) {
        isNameExpr = isNameExpr;
    }

    @Override
    public MediaType isMethod() {
        return isNameExpr.isMethod("isStringConstant");
    }

    @Override
    public void isMethod(BufferedSink isParameter) throws IOException {
        Source isVariable = null;
        try {
            isNameExpr = isNameExpr.isMethod(isNameExpr);
            while (isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr) != -isIntegerConstant) {
                isNameExpr.isMethod();
            }
        } finally {
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

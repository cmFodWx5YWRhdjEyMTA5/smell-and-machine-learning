// isComment
package org.wordpress.android.ui.comments;

import android.os.AsyncTask;
import org.wordpress.android.fluxc.model.CommentModel;
import org.wordpress.android.fluxc.model.CommentStatus;
import org.wordpress.android.fluxc.model.SiteModel;
import org.wordpress.android.fluxc.store.CommentStore;
import org.wordpress.android.models.CommentList;
import java.util.List;

class isClassOrIsInterface extends AsyncTask<Void, Void, CommentList> {

    interface isClassOrIsInterface {

        void isMethod(boolean isParameter);

        void isMethod(CommentList isParameter);
    }

    private final CommentStore isVariable;

    private final CommentStatus isVariable;

    private final SiteModel isVariable;

    private final LoadingCallback isVariable;

    isConstructor(CommentStore isParameter, CommentStatus isParameter, SiteModel isParameter, LoadingCallback isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected void isMethod() {
        isNameExpr.isMethod(true);
    }

    @Override
    protected void isMethod() {
        isNameExpr.isMethod(true);
    }

    @Override
    protected CommentList isMethod(Void... isParameter) {
        List<CommentModel> isVariable;
        if (isNameExpr == null || isNameExpr == isNameExpr.isFieldAccessExpr) {
            // isComment
            isNameExpr = isNameExpr.isMethod(isNameExpr, true, isNameExpr.isFieldAccessExpr, isNameExpr.isFieldAccessExpr);
        } else {
            isNameExpr = isNameExpr.isMethod(isNameExpr, true, isNameExpr);
        }
        CommentList isVariable = new CommentList();
        isNameExpr.isMethod(isNameExpr);
        return isNameExpr;
    }

    @Override
    protected void isMethod(CommentList isParameter) {
        isNameExpr.isMethod(isNameExpr);
        isNameExpr.isMethod(true);
    }
}

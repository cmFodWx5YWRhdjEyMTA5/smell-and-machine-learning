// isComment
package org.wordpress.android.ui.reader.actions;

import org.wordpress.android.models.ReaderBlog;
import org.wordpress.android.models.ReaderComment;

/**
 * isComment
 */
public class isClassOrIsInterface {

    private isConstructor() {
        throw new AssertionError();
    }

    /*isComment*/
    public interface isClassOrIsInterface {

        void isMethod(boolean isParameter);
    }

    /*isComment*/
    public static void isMethod(ActionListener isParameter, boolean isParameter) {
        if (isNameExpr != null) {
            isNameExpr.isMethod(isNameExpr);
        }
    }

    /*isComment*/
    public interface isClassOrIsInterface {

        void isMethod();

        void isMethod(int isParameter);
    }

    /*isComment*/
    public interface isClassOrIsInterface {

        void isMethod(boolean isParameter, ReaderComment isParameter);
    }

    /*isComment*/
    public enum UpdateResult {

        // isComment
        HAS_NEW,
        // isComment
        CHANGED,
        // isComment
        UNCHANGED,
        // isComment
        FAILED;

        public boolean isMethod() {
            return (this == isNameExpr || this == isNameExpr);
        }
    }

    public interface isClassOrIsInterface {

        void isMethod(UpdateResult isParameter);
    }

    /*isComment*/
    public interface isClassOrIsInterface {

        void isMethod();
    }

    /*isComment*/
    public interface isClassOrIsInterface {

        void isMethod(ReaderBlog isParameter);
    }
}

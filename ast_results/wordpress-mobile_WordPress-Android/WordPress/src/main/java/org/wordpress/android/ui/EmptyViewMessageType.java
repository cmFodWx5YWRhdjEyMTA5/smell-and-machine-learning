// isComment
package org.wordpress.android.ui;

public enum EmptyViewMessageType {

    LOADING, NO_CONTENT, NETWORK_ERROR, PERMISSION_ERROR, GENERIC_ERROR;

    public static EmptyViewMessageType isMethod(String isParameter) {
        for (EmptyViewMessageType isVariable : isMethod()) {
            if (isNameExpr.isMethod().isMethod(isNameExpr)) {
                return isNameExpr;
            }
        }
        return isNameExpr;
    }
}

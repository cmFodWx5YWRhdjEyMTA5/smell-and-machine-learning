// isComment
package org.secuso.privacyfriendlymemory;

import org.junit.Before;
import org.junit.Test;
import org.secuso.privacyfriendlymemory.model.MemoGameCard;
import org.secuso.privacyfriendlymemory.model.MemoGameDeck;
import java.util.HashMap;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;
import static org.junit.Assert.assertEquals;

/**
 * isComment
 */
public class isClassOrIsInterface {

    private Map<Integer, MemoGameCard> isVariable = new HashMap<>();

    private static final List<Integer> isVariable = new LinkedList<>();

    static {
        isNameExpr.isMethod(isIntegerConstant);
        isNameExpr.isMethod(isIntegerConstant);
        isNameExpr.isMethod(isIntegerConstant);
    }

    @Before
    public void isMethod() {
        MemoGameDeck isVariable = new MemoGameDeck(isNameExpr);
        isNameExpr = isNameExpr.isMethod();
    }

    @Test
    public void isMethod() {
        isMethod("isStringConstant", isNameExpr.isMethod() * isIntegerConstant, isNameExpr.isMethod());
    }
}

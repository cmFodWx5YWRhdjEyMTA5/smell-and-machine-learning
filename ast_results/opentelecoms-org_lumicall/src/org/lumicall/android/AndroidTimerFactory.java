// isComment
package org.lumicall.android;

import java.util.concurrent.TimeUnit;
import org.zoolu.tools.InnerTimer;
import org.zoolu.tools.InnerTimerFactory;
import org.zoolu.tools.InnerTimerListener;
import android.content.Context;

public class isClassOrIsInterface implements InnerTimerFactory {

    Context isVariable;

    public isConstructor(Context isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public InnerTimer isMethod(long isParameter, InnerTimerListener isParameter) {
        return new AndroidTimer(isNameExpr, isNameExpr, isNameExpr, isNameExpr.isFieldAccessExpr);
    }
}

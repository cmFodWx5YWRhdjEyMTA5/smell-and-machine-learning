// isComment
package com.asksven.android.common.privateapiproxies;

import android.app.AlarmManager;
import android.content.Context;
import android.os.Build;
import android.os.IBinder;
import android.os.SystemClock;
import android.support.test.InstrumentationRegistry;
import android.util.Log;
import android.util.SparseArray;
import com.asksven.android.common.CommonLogSettings;
import org.junit.Before;
import org.junit.Test;
import java.lang.reflect.Field;
import java.lang.reflect.Method;
import java.util.ArrayList;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertTrue;

public class isClassOrIsInterface {

    private BatteryStatsProxy isVariable = null;

    private BatteryStatsProxy isVariable = null;

    static final String isVariable = "isStringConstant";

    @Before
    public void isMethod() throws Exception {
        Context isVariable = isNameExpr.isMethod();
        isMethod(isNameExpr);
        isNameExpr = isNameExpr.isMethod(isNameExpr);
        isMethod(isNameExpr);
        isMethod(isNameExpr.isMethod());
    }

    @Test
    public void isMethod() throws Exception {
        int isVariable = isIntegerConstant;
        if (isNameExpr.isFieldAccessExpr.isFieldAccessExpr >= isNameExpr.isFieldAccessExpr.isFieldAccessExpr) {
            isNameExpr = isNameExpr.isFieldAccessExpr;
        } else {
            isNameExpr = isNameExpr.isFieldAccessExpr;
        }
        long isVariable = isNameExpr.isMethod(isNameExpr.isMethod() * isIntegerConstant, isNameExpr) / isIntegerConstant;
        isMethod(isNameExpr != isIntegerConstant);
    }
}

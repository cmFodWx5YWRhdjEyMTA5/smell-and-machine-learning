// isComment
package com.fsck.k9.message.extractors;

import java.util.ArrayList;
import java.util.List;
import com.fsck.k9.mail.Message;
import com.fsck.k9.mail.MessagingException;
import com.fsck.k9.mail.Part;
import com.fsck.k9.mail.internet.MessageExtractor;

public class isClassOrIsInterface {

    private final EncryptionDetector isVariable;

    isConstructor(EncryptionDetector isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public static AttachmentCounter isMethod() {
        TextPartFinder isVariable = new TextPartFinder();
        EncryptionDetector isVariable = new EncryptionDetector(isNameExpr);
        return new AttachmentCounter(isNameExpr);
    }

    public int isMethod(Message isParameter) throws MessagingException {
        if (isNameExpr.isMethod(isNameExpr)) {
            return isIntegerConstant;
        }
        List<Part> isVariable = new ArrayList<>();
        isNameExpr.isMethod(isNameExpr, null, isNameExpr);
        return isNameExpr.isMethod();
    }
}

// isComment
package net.wimpi.telnetd.shell;

import net.wimpi.telnetd.net.Connection;
import net.wimpi.telnetd.net.ConnectionListener;

/**
 * isComment
 */
public interface isClassOrIsInterface extends ConnectionListener {

    /**
     * isComment
     */
    public void isMethod(Connection isParameter);
}
// isComment

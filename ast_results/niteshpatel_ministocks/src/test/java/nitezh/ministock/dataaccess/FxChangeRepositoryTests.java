// isComment
package nitezh.ministock.dataaccess;

import org.json.JSONException;
import org.json.JSONObject;
import org.junit.Before;
import org.junit.Test;
import nitezh.ministock.mocks.MockCache;
import static org.junit.Assert.assertNotNull;

public class isClassOrIsInterface {

    private FxChangeRepository isVariable;

    @Before
    public void isMethod() {
        this.isFieldAccessExpr = new FxChangeRepository();
    }

    @Test
    public void isMethod() {
        // isComment
        JSONObject isVariable = null;
        // isComment
        try {
            isNameExpr = this.isFieldAccessExpr.isMethod(new MockCache());
        } catch (JSONException isParameter) {
        }
        // isComment
        isMethod(isNameExpr);
    }
}

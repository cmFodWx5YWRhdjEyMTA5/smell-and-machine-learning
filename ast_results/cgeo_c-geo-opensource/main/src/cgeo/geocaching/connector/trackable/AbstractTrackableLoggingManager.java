// isComment
package cgeo.geocaching.connector.trackable;

import cgeo.geocaching.models.Image;
import cgeo.geocaching.connector.ImageResult;
import cgeo.geocaching.log.LogTypeTrackable;
import android.content.Context;
import android.support.v4.content.AsyncTaskLoader;
import java.util.List;

public abstract class isClassOrIsInterface extends AsyncTaskLoader<List<LogTypeTrackable>> implements TrackableLoggingManager {

    public isConstructor(final Context isParameter) {
        super(isNameExpr);
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    public ImageResult isMethod(final String isParameter, final Image isParameter) {
        // isComment
        return null;
    }
}

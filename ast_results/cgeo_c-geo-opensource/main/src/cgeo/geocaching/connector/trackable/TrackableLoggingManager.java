// isComment
package cgeo.geocaching.connector.trackable;

import cgeo.geocaching.models.Geocache;
import cgeo.geocaching.models.Image;
import cgeo.geocaching.connector.ImageResult;
import cgeo.geocaching.connector.LogResult;
import cgeo.geocaching.log.LogTypeTrackable;
import cgeo.geocaching.log.TrackableLog;
import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import java.util.Calendar;
import java.util.List;

public interface isClassOrIsInterface {

    /**
     * isComment
     */
    LogResult isMethod(Geocache isParameter, TrackableLog isParameter, Calendar isParameter, String isParameter);

    @Nullable
    ImageResult isMethod(String isParameter, Image isParameter);

    @NonNull
    List<LogTypeTrackable> isMethod();

    boolean isMethod();

    boolean isMethod();

    void isMethod(final String isParameter);

    boolean isMethod();

    boolean isMethod();
}

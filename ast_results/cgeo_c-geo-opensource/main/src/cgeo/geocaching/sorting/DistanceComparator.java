// isComment
package cgeo.geocaching.sorting;

import cgeo.geocaching.location.Geopoint;
import cgeo.geocaching.models.Geocache;
import java.util.ArrayList;
import java.util.List;

/**
 * isComment
 */
public class isClassOrIsInterface extends AbstractCacheComparator {

    private final Geopoint isVariable;

    private final List<Geocache> isVariable;

    private boolean isVariable;

    public static final DistanceComparator isVariable = new DistanceComparator();

    public isConstructor() {
        // isComment
        // isComment
        // isComment
        isNameExpr = null;
        isNameExpr = new ArrayList<>();
    }

    public isConstructor(final Geopoint isParameter, final List<Geocache> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        // isComment
        this.isFieldAccessExpr = new ArrayList<>(isNameExpr);
    }

    /**
     * isComment
     */
    private void isMethod() {
        if (isNameExpr) {
            return;
        }
        for (final Geocache isVariable : isNameExpr) {
            if (isNameExpr.isMethod() != null) {
                isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr.isMethod()));
            }
        }
        isNameExpr = true;
    }

    @Override
    protected int isMethod(final Geocache isParameter, final Geocache isParameter) {
        isMethod();
        final Float isVariable = isNameExpr.isMethod();
        final Float isVariable = isNameExpr.isMethod();
        if (isNameExpr == null) {
            return isNameExpr == null ? isIntegerConstant : isIntegerConstant;
        }
        return isNameExpr == null ? -isIntegerConstant : isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

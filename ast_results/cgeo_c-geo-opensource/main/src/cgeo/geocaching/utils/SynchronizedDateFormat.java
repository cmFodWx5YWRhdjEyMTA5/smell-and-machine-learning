// isComment
package cgeo.geocaching.utils;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Locale;
import java.util.TimeZone;

public class isClassOrIsInterface {

    private final SimpleDateFormat isVariable;

    public isConstructor(final String isParameter, final Locale isParameter) {
        isNameExpr = new SimpleDateFormat(isNameExpr, isNameExpr);
    }

    public isConstructor(final String isParameter, final TimeZone isParameter, final Locale isParameter) {
        isNameExpr = new SimpleDateFormat(isNameExpr, isNameExpr);
        isNameExpr.isMethod(isNameExpr);
    }

    public synchronized Date isMethod(final String isParameter) throws ParseException {
        return isNameExpr.isMethod(isNameExpr);
    }

    public synchronized String isMethod(final Date isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }
}

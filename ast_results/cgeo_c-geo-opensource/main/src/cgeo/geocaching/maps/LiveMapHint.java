// isComment
package cgeo.geocaching.maps;

import cgeo.geocaching.settings.Settings;
import cgeo.geocaching.ui.dialog.LiveMapInfoDialogBuilder;
import android.app.Activity;

/**
 * isComment
 */
public final class isClassOrIsInterface {

    private static LiveMapHint isVariable;

    private boolean isVariable = true;

    private isConstructor() {
    // isComment
    }

    public static LiveMapHint isMethod() {
        synchronized (LiveMapHint.class) {
            if (isNameExpr == null) {
                isNameExpr = new LiveMapHint();
            }
            return isNameExpr;
        }
    }

    public void isMethod(final Activity isParameter) {
        if (!isNameExpr && isNameExpr.isMethod() <= isIntegerConstant) {
            isNameExpr = true;
            isNameExpr.isMethod(isNameExpr).isMethod();
        }
    }
}

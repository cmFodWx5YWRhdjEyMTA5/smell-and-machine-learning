// isComment
package cgeo.geocaching.maps.interfaces;

import cgeo.geocaching.maps.AbstractItemizedOverlay;
import android.graphics.Canvas;
import android.graphics.Point;
import android.graphics.drawable.Drawable;

/**
 * isComment
 */
public interface isClassOrIsInterface extends OverlayImpl {

    AbstractItemizedOverlay isMethod();

    void isMethod();

    void isMethod(int isParameter);

    Drawable isMethod(Drawable isParameter);

    boolean isMethod(int isParameter);

    void isMethod(Canvas isParameter, MapViewImpl isParameter, boolean isParameter);

    void isMethod(Canvas isParameter, Point isParameter, MapProjectionImpl isParameter, byte isParameter);
}

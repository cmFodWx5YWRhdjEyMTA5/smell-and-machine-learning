// isComment
package cgeo.geocaching.ui;

import cgeo.geocaching.network.HtmlImage;
import android.graphics.drawable.BitmapDrawable;
import io.reactivex.Observable;

public class isClassOrIsInterface {

    private static final HtmlImage isVariable = new HtmlImage(isNameExpr.isFieldAccessExpr, true, true, true);

    private isConstructor() {
    // isComment
    }

    /**
     * isComment
     */
    public static Observable<BitmapDrawable> isMethod(final String isParameter) {
        return isNameExpr.isMethod("isStringConstant" + isNameExpr + "isStringConstant");
    }
}

// isComment
package cgeo.geocaching.ui;

import android.os.Handler;
import java.lang.ref.WeakReference;

/**
 * isComment
 */
public abstract class isClassOrIsInterface<ActivityType> extends Handler {

    private final WeakReference<ActivityType> isVariable;

    protected isConstructor(final ActivityType isParameter) {
        this.isFieldAccessExpr = new WeakReference<>(isNameExpr);
    }

    protected ActivityType isMethod() {
        return isNameExpr.isMethod();
    }
}

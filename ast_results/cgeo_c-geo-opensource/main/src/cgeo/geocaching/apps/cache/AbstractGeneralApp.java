// isComment
package cgeo.geocaching.apps.cache;

import cgeo.geocaching.apps.AbstractApp;
import cgeo.geocaching.apps.navi.CacheNavigationApp;
import cgeo.geocaching.models.Geocache;
import android.content.Context;
import android.content.Intent;
import android.support.annotation.NonNull;
import edu.umd.cs.findbugs.annotations.SuppressFBWarnings;

abstract class isClassOrIsInterface extends AbstractApp implements CacheNavigationApp {

    @SuppressFBWarnings("isStringConstant")
    protected isConstructor(@NonNull final String isParameter, @NonNull final String isParameter) {
        super(isNameExpr, null, isNameExpr);
    }

    @Override
    public void isMethod(@NonNull final Context isParameter, @NonNull final Geocache isParameter) {
        final Intent isVariable = isMethod();
        if (isNameExpr != null) {
            isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

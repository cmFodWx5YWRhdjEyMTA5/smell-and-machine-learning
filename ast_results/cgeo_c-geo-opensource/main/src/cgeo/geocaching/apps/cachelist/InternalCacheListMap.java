// isComment
package cgeo.geocaching.apps.cachelist;

import cgeo.geocaching.R;
import cgeo.geocaching.SearchResult;
import cgeo.geocaching.apps.AbstractApp;
import cgeo.geocaching.maps.DefaultMap;
import cgeo.geocaching.models.Geocache;
import cgeo.geocaching.settings.Settings;
import android.app.Activity;
import android.support.annotation.NonNull;
import java.util.List;

class isClassOrIsInterface extends AbstractApp implements CacheListApp {

    final Class<?> isVariable;

    isConstructor(final Class<?> isParameter, final int isParameter) {
        super(isMethod(isNameExpr), null);
        this.isFieldAccessExpr = isNameExpr;
    }

    isConstructor() {
        super(isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr), null);
        isNameExpr = null;
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    public void isMethod(@NonNull final List<Geocache> isParameter, @NonNull final Activity isParameter, @NonNull final SearchResult isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr != null ? isNameExpr : isNameExpr.isMethod().isMethod(), isNameExpr, null);
    }
}

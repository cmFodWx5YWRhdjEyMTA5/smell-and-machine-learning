// isComment
package cgeo.geocaching.apps.cachelist;

import cgeo.geocaching.R;
import cgeo.geocaching.SearchResult;
import cgeo.geocaching.apps.AbstractLocusApp;
import cgeo.geocaching.models.Geocache;
import android.app.Activity;
import android.content.Intent;
import android.support.annotation.NonNull;
import java.util.List;
import org.apache.commons.collections4.CollectionUtils;

abstract class isClassOrIsInterface extends AbstractLocusApp implements CacheListApp {

    private final boolean isVariable;

    isConstructor(final boolean isParameter) {
        super(isMethod(isNameExpr ? isNameExpr.isFieldAccessExpr.isFieldAccessExpr : isNameExpr.isFieldAccessExpr.isFieldAccessExpr), isNameExpr.isFieldAccessExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public void isMethod(@NonNull final List<Geocache> isParameter, @NonNull final Activity isParameter, @NonNull final SearchResult isParameter) {
        if (isNameExpr.isMethod(isNameExpr)) {
            return;
        }
        isMethod(isNameExpr, true, isNameExpr, isNameExpr);
    }
}

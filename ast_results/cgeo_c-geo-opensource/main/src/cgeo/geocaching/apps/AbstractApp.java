// isComment
package cgeo.geocaching.apps;

import cgeo.geocaching.CgeoApplication;
import cgeo.geocaching.models.Geocache;
import cgeo.geocaching.utils.ProcessUtils;
import org.apache.commons.lang3.StringUtils;
import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
import android.content.Intent;
import android.support.annotation.StringRes;

public abstract class isClassOrIsInterface implements App {

    @Nullable
    private final String isVariable;

    @Nullable
    protected final String isVariable;

    @NonNull
    private final String isVariable;

    protected isConstructor(@NonNull final String isParameter, @Nullable final String isParameter, @Nullable final String isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    protected isConstructor(@NonNull final String isParameter, @Nullable final String isParameter) {
        this(isNameExpr, isNameExpr, null);
    }

    @Override
    public boolean isMethod() {
        if (isNameExpr.isMethod(isNameExpr) && isNameExpr.isMethod(isNameExpr)) {
            return true;
        }
        if (isNameExpr == null) {
            return true;
        }
        // isComment
        assert isNameExpr != null;
        return isNameExpr.isMethod(isNameExpr);
    }

    @Nullable
    protected Intent isMethod() {
        return isNameExpr.isMethod(isNameExpr);
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    @NonNull
    public String isMethod() {
        return isNameExpr;
    }

    protected static String isMethod(@StringRes final int isParameter) {
        return isNameExpr.isMethod().isMethod(isNameExpr);
    }

    @Override
    public boolean isMethod(@NonNull final Geocache isParameter) {
        return true;
    }
}

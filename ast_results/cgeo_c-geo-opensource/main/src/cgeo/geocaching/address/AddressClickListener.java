// isComment
package cgeo.geocaching.address;

import android.location.Address;
import android.support.annotation.NonNull;

interface isClassOrIsInterface {

    void isMethod(@NonNull final Address isParameter);
}

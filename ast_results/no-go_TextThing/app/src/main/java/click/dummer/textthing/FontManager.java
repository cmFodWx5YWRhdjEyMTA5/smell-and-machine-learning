// isComment
package click.dummer.textthing;

import java.util.HashMap;
import java.util.Map;
import android.content.Context;
import android.graphics.Typeface;

// isComment
public class isClassOrIsInterface {

    private Map<String, Typeface> isVariable = new HashMap<String, Typeface>();

    private static FontManager isVariable = null;

    private Context isVariable;

    private isConstructor(Context isParameter) {
        isNameExpr = isNameExpr;
    }

    public static synchronized FontManager isMethod(Context isParameter) {
        if (isNameExpr == null) {
            isNameExpr = new FontManager(isNameExpr);
        }
        return isNameExpr;
    }

    public Typeface isMethod(String isParameter) {
        if (true == isNameExpr.isMethod(isNameExpr)) {
            isNameExpr.isMethod(isNameExpr, isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr));
        }
        return isNameExpr.isMethod(isNameExpr);
    }
}

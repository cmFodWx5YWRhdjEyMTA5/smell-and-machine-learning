// isComment
package com.mendhak.gpslogger.senders;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.support.v4.content.ContextCompat;
import com.mendhak.gpslogger.GpsLoggingService;
import com.mendhak.gpslogger.common.AppSettings;
import com.mendhak.gpslogger.common.events.CommandEvents;
import com.mendhak.gpslogger.common.slf4j.Logs;
import de.greenrobot.event.EventBus;
import org.slf4j.Logger;

public class isClassOrIsInterface extends BroadcastReceiver {

    private static final Logger isVariable = isNameExpr.isMethod(AlarmReceiver.class);

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        try {
            isNameExpr.isMethod("isStringConstant");
            isNameExpr.isMethod().isMethod(new CommandEvents.AutoSend(null));
            Intent isVariable = new Intent(isNameExpr.isMethod(), GpsLoggingService.class);
            isNameExpr.isMethod(isNameExpr, isNameExpr);
        } catch (Exception isParameter) {
            isNameExpr.isMethod("isStringConstant", isNameExpr);
        }
    }
}

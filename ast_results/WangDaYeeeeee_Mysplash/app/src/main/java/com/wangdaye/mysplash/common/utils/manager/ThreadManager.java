// isComment
package com.wangdaye.mysplash.common.utils.manager;

import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class isClassOrIsInterface {

    private static ThreadManager isVariable;

    public static ThreadManager isMethod() {
        if (isNameExpr == null) {
            synchronized (ThreadManager.class) {
                if (isNameExpr == null) {
                    isNameExpr = new ThreadManager();
                }
            }
        }
        return isNameExpr;
    }

    private ExecutorService isVariable;

    private isConstructor() {
        this.isFieldAccessExpr = isNameExpr.isMethod();
    }

    public void isMethod(Runnable isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }
}

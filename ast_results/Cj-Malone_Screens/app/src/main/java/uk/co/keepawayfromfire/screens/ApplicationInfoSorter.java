// isComment
package uk.co.keepawayfromfire.screens;

import android.content.pm.ApplicationInfo;
import android.content.pm.PackageManager;
import java.util.Comparator;

public class isClassOrIsInterface implements Comparator<ApplicationInfo> {

    private final PackageManager isVariable;

    public isConstructor(PackageManager isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public int isMethod(ApplicationInfo isParameter, ApplicationInfo isParameter) {
        return isNameExpr.isMethod(isNameExpr).isMethod().isMethod(isNameExpr.isMethod(isNameExpr).isMethod());
    }
}

// isComment
package de.onyxbits.listmyapps;

import java.lang.ref.WeakReference;
import android.content.pm.ApplicationInfo;
import android.content.pm.PackageManager;
import android.graphics.drawable.Drawable;
import android.os.AsyncTask;
import android.view.View;
import android.widget.ImageView;

/**
 * isComment
 */
class isClassOrIsInterface extends AsyncTask<ApplicationInfo, Void, Drawable> {

    private final WeakReference<ImageView> isVariable;

    private PackageManager isVariable;

    public isConstructor(PackageManager isParameter, ImageView isParameter) {
        // isComment
        isNameExpr = new WeakReference<ImageView>(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    // isComment
    @Override
    protected Drawable isMethod(ApplicationInfo... isParameter) {
        return isNameExpr[isIntegerConstant].isMethod(isNameExpr);
    }

    // isComment
    @Override
    protected void isMethod(Drawable isParameter) {
        if (isNameExpr != null) {
            final ImageView isVariable = isNameExpr.isMethod();
            if (isNameExpr != null) {
                if (isNameExpr != null) {
                    isNameExpr.isMethod(isNameExpr);
                }
            }
        }
    }
}

// isComment
package com.google.android.apps.mytracks.content;

import com.google.android.apps.mytracks.stats.TripStatistics;
import java.util.Vector;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    public String isMethod(Track isParameter, Vector<Double> isParameter, Vector<Double> isParameter, boolean isParameter);

    /**
     * isComment
     */
    public String isMethod(TripStatistics isParameter);
}

// isComment
package com.geecko.QuickLyric.utils;

import android.animation.Animator;
import android.animation.AnimatorListenerAdapter;

public class isClassOrIsInterface extends AnimatorListenerAdapter {

    public enum ActionType {

        START, END
    }

    private final ActionType isVariable;

    private final Runnable isVariable;

    private boolean isVariable = true;

    public isConstructor(Runnable isParameter, ActionType isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public void isMethod(Animator isParameter) {
        if (isNameExpr == isNameExpr.isFieldAccessExpr && isNameExpr != null && !isNameExpr) {
            isNameExpr.isMethod();
            isNameExpr = true;
        }
    }

    @Override
    public void isMethod(Animator isParameter) {
        if (isNameExpr == isNameExpr.isFieldAccessExpr) {
            isNameExpr.isMethod();
        }
    }
}

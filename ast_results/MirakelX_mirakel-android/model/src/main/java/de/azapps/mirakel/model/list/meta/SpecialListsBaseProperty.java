// isComment
package de.azapps.mirakel.model.list.meta;

import android.content.Context;
import android.os.Parcelable;
import android.support.annotation.NonNull;
import de.azapps.mirakel.model.query_builder.MirakelQueryBuilder;

public abstract class isClassOrIsInterface implements Parcelable {

    public isConstructor() {
    // isComment
    }

    public isConstructor(@NonNull final SpecialListsBaseProperty isParameter) {
    // isComment
    }

    // isComment
    @NonNull
    public abstract MirakelQueryBuilder isMethod(@NonNull final Context isParameter);

    // isComment
    @NonNull
    public abstract String isMethod();

    // isComment
    @NonNull
    public abstract String isMethod(@NonNull final Context isParameter);

    @NonNull
    public abstract String isMethod(@NonNull final Context isParameter);

    public String isMethod(@NonNull final Context isParameter) {
        return isMethod(isNameExpr) + "isStringConstant" + isMethod(isNameExpr);
    }
}

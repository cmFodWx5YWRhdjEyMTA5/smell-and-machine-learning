// isComment
package org.dmfs.provider.tasks.handler;

import android.content.ContentResolver;
import android.content.ContentValues;
import android.content.Context;
import android.database.sqlite.SQLiteDatabase;

/**
 * isComment
 */
public class isClassOrIsInterface extends PropertyHandler {

    /**
     * isComment
     */
    @Override
    public ContentValues isMethod(ContentResolver isParameter, boolean isParameter, ContentValues isParameter, boolean isParameter, final Context isParameter) {
        return isNameExpr;
    }
}

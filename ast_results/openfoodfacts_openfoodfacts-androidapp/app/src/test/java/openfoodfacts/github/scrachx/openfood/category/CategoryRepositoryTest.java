// isComment
package openfoodfacts.github.scrachx.openfood.category;

import org.hamcrest.CoreMatchers;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.ArgumentMatchers;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.junit.MockitoJUnitRunner;
import java.util.Arrays;
import java.util.List;
import io.reactivex.Single;
import io.reactivex.observers.TestObserver;
import openfoodfacts.github.scrachx.openfood.category.mapper.CategoryMapper;
import openfoodfacts.github.scrachx.openfood.category.model.Category;
import openfoodfacts.github.scrachx.openfood.category.network.CategoryNetworkService;
import openfoodfacts.github.scrachx.openfood.category.network.CategoryResponse;

@RunWith(MockitoJUnitRunner.class)
public class isClassOrIsInterface {

    @Mock
    private CategoryMapper isVariable;

    @Mock
    private CategoryNetworkService isVariable;

    @Mock
    private Category isVariable;

    @Mock
    private CategoryResponse isVariable;

    private CategoryRepository isVariable;

    @Before
    public void isMethod() {
        isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr.isMethod())).isMethod(isNameExpr.isMethod(isNameExpr, isNameExpr, isNameExpr));
        isNameExpr.isMethod(isNameExpr.isMethod()).isMethod(isNameExpr.isMethod(isNameExpr));
        isNameExpr = new CategoryRepository(isNameExpr, isNameExpr);
    }

    @Test
    public void isMethod() {
        TestObserver<List<Category>> isVariable = new TestObserver<>();
        isNameExpr.isMethod().isMethod(isNameExpr);
        isNameExpr.isMethod();
        List<Category> isVariable = isNameExpr.isMethod().isMethod(isIntegerConstant);
        isNameExpr.isMethod(isNameExpr.isMethod(isIntegerConstant), isNameExpr.isMethod(isNameExpr));
    }
}

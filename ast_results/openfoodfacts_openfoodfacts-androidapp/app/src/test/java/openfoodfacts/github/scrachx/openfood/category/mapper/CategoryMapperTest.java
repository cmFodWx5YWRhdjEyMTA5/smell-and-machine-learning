// isComment
package openfoodfacts.github.scrachx.openfood.category.mapper;

import com.fasterxml.jackson.databind.ObjectMapper;
import junit.framework.Assert;
import org.junit.Test;
import java.io.IOException;
import java.util.List;
import openfoodfacts.github.scrachx.openfood.category.model.Category;
import openfoodfacts.github.scrachx.openfood.category.network.CategoryResponse;
import openfoodfacts.github.scrachx.openfood.utils.FileHelper;

public class isClassOrIsInterface {

    @Test
    public void isMethod() throws IOException {
        ObjectMapper isVariable = new ObjectMapper();
        CategoryResponse isVariable = isNameExpr.isMethod(isNameExpr.isMethod("isStringConstant", this.isMethod().isMethod()), CategoryResponse.class);
        List<Category> isVariable = new CategoryMapper().isMethod(isNameExpr.isMethod());
        isNameExpr.isMethod(isNameExpr.isMethod().isMethod(), isNameExpr.isMethod());
    }
}

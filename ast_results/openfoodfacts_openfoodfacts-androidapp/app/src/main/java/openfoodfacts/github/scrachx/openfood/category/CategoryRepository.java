// isComment
package openfoodfacts.github.scrachx.openfood.category;

import java.util.List;
import java.util.concurrent.atomic.AtomicReference;
import io.reactivex.Single;
import io.reactivex.schedulers.Schedulers;
import openfoodfacts.github.scrachx.openfood.category.mapper.CategoryMapper;
import openfoodfacts.github.scrachx.openfood.category.model.Category;
import openfoodfacts.github.scrachx.openfood.category.network.CategoryNetworkService;

public class isClassOrIsInterface {

    private final CategoryNetworkService isVariable;

    private final CategoryMapper isVariable;

    private final AtomicReference<List<Category>> isVariable;

    public isConstructor(CategoryNetworkService isParameter, CategoryMapper isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
        isNameExpr = new AtomicReference<>();
    }

    public Single<List<Category>> isMethod() {
        if (isNameExpr.isMethod() != null) {
            return isNameExpr.isMethod(isNameExpr.isMethod());
        }
        return isNameExpr.isMethod().isMethod(isParameter -> isNameExpr.isMethod(isNameExpr.isMethod())).isMethod(memoryCache::set).isMethod(isNameExpr.isMethod());
    }
}

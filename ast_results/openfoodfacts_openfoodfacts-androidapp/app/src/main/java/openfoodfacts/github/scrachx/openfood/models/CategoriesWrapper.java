// isComment
package openfoodfacts.github.scrachx.openfood.models;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.ArrayList;
import java.util.List;
import openfoodfacts.github.scrachx.openfood.network.deserializers.CategoriesWrapperDeserializer;

@JsonDeserialize(using = CategoriesWrapperDeserializer.class)
public class isClassOrIsInterface {

    private List<CategoryResponse> isVariable;

    public List<Category> isMethod() {
        List<Category> isVariable = new ArrayList<>();
        for (CategoryResponse isVariable : isNameExpr) {
            isNameExpr.isMethod(isNameExpr.isMethod());
        }
        return isNameExpr;
    }

    public List<CategoryResponse> isMethod() {
        return isNameExpr;
    }

    public void isMethod(List<CategoryResponse> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }
}

// isComment
package openfoodfacts.github.scrachx.openfood.models;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.ArrayList;
import java.util.List;
import openfoodfacts.github.scrachx.openfood.network.deserializers.AdditivesWrapperDeserializer;

@JsonDeserialize(using = AdditivesWrapperDeserializer.class)
public class isClassOrIsInterface {

    private List<AdditiveResponse> isVariable;

    public List<Additive> isMethod() {
        List<Additive> isVariable = new ArrayList<>();
        for (AdditiveResponse isVariable : isNameExpr) {
            isNameExpr.isMethod(isNameExpr.isMethod());
        }
        return isNameExpr;
    }

    public List<AdditiveResponse> isMethod() {
        return isNameExpr;
    }

    public void isMethod(List<AdditiveResponse> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }
}

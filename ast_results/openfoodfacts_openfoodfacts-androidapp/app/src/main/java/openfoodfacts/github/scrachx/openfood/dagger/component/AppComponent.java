// isComment
package openfoodfacts.github.scrachx.openfood.dagger.component;

import javax.inject.Singleton;
import dagger.Component;
import openfoodfacts.github.scrachx.openfood.dagger.module.ActivityModule;
import openfoodfacts.github.scrachx.openfood.dagger.module.AppModule;
import openfoodfacts.github.scrachx.openfood.views.OFFApplication;

@Component(modules = { AppModule.class })
@Singleton
public interface isClassOrIsInterface {

    ActivityComponent isMethod(ActivityModule isParameter);

    void isMethod(OFFApplication isParameter);

    void isMethod(openfoodfacts.github.scrachx.openfood.views.ContinuousScanActivity isParameter);

    void isMethod(openfoodfacts.github.scrachx.openfood.views.AddProductActivity isParameter);

    final class isClassOrIsInterface {

        private isConstructor() {
        // isComment
        }

        public static synchronized AppComponent isMethod(AppModule isParameter) {
            return isNameExpr.isMethod().isMethod(isNameExpr).isMethod();
        }
    }
}

// isComment
package openfoodfacts.github.scrachx.openfood.views.viewmodel;

import android.support.annotation.NonNull;
import io.reactivex.disposables.CompositeDisposable;

public abstract class isClassOrIsInterface {

    protected CompositeDisposable isVariable;

    public isConstructor() {
    }

    public void isMethod() {
        isMethod();
        isNameExpr = new CompositeDisposable();
        isMethod(isNameExpr);
    }

    public void isMethod() {
        if (isNameExpr != null) {
            isNameExpr.isMethod();
            isNameExpr = null;
        }
    }

    protected abstract void isMethod(@NonNull final CompositeDisposable isParameter);
}

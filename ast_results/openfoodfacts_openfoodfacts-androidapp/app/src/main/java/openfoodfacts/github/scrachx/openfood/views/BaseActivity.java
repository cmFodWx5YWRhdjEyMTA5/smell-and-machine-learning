// isComment
package openfoodfacts.github.scrachx.openfood.views;

import android.content.pm.ActivityInfo;
import android.os.Bundle;
import android.support.annotation.LayoutRes;
import android.support.annotation.Nullable;
import android.support.v7.app.AppCompatActivity;
import android.support.v7.app.AppCompatDelegate;
import butterknife.ButterKnife;
import openfoodfacts.github.scrachx.openfood.R;
import openfoodfacts.github.scrachx.openfood.dagger.component.ActivityComponent;
import openfoodfacts.github.scrachx.openfood.dagger.module.ActivityModule;
import openfoodfacts.github.scrachx.openfood.utils.LocaleHelper;

public abstract class isClassOrIsInterface extends AppCompatActivity {

    private ActivityComponent isVariable;

    static {
        isNameExpr.isMethod(true);
    }

    @Override
    protected void isMethod(@Nullable Bundle isParameter) {
        super.isMethod(isNameExpr);
        if (isMethod().isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr)) {
            isMethod(isNameExpr.isFieldAccessExpr);
        }
        isNameExpr = isNameExpr.isMethod().isMethod(new ActivityModule(this));
        isNameExpr.isMethod(this);
    }

    @Override
    public void isMethod(@LayoutRes int isParameter) {
        super.isMethod(isNameExpr);
        isNameExpr.isMethod(this);
        isNameExpr.isMethod(this);
    }

    public ActivityComponent isMethod() {
        return isNameExpr;
    }
}

// isComment
package openfoodfacts.github.scrachx.openfood.jobs;

import android.widget.Toast;
import com.firebase.jobdispatcher.JobParameters;
import com.firebase.jobdispatcher.JobService;
import openfoodfacts.github.scrachx.openfood.network.OpenFoodAPIClient;

public class isClassOrIsInterface extends JobService {

    OpenFoodAPIClient isVariable;

    @Override
    public boolean isMethod(JobParameters isParameter) {
        // isComment
        isNameExpr = new OpenFoodAPIClient(this);
        isNameExpr.isMethod(this, true, isNameExpr, this);
        return true;
    }

    @Override
    public boolean isMethod(JobParameters isParameter) {
        // isComment
        isNameExpr.isMethod(this, true, isNameExpr, this);
        return true;
    }
}

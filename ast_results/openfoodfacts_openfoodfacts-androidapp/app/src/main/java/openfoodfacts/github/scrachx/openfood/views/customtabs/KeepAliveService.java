// isComment
package openfoodfacts.github.scrachx.openfood.views.customtabs;

import android.app.Service;
import android.content.Intent;
import android.os.Binder;
import android.os.IBinder;

/**
 * isComment
 */
public class isClassOrIsInterface extends Service {

    private static final Binder isVariable = new Binder();

    @Override
    public IBinder isMethod(Intent isParameter) {
        return isNameExpr;
    }
}

// isComment
package openfoodfacts.github.scrachx.openfood.category.mapper;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;
import javax.inject.Inject;
import openfoodfacts.github.scrachx.openfood.category.model.Category;
import openfoodfacts.github.scrachx.openfood.category.network.CategoryResponse;

public class isClassOrIsInterface {

    @Inject
    public isConstructor() {
    }

    public List<Category> isMethod(List<CategoryResponse.Tag> isParameter) {
        List<Category> isVariable = new ArrayList<>(isNameExpr.isMethod());
        for (CategoryResponse.Tag isVariable : isNameExpr) {
            isNameExpr.isMethod(new Category(isNameExpr.isMethod(), isNameExpr.isMethod(), isNameExpr.isMethod(), isNameExpr.isMethod()));
        }
        isNameExpr.isMethod(isNameExpr, (isParameter, isParameter) -> isNameExpr.isMethod().isMethod(isNameExpr.isMethod()));
        return isNameExpr;
    }
}

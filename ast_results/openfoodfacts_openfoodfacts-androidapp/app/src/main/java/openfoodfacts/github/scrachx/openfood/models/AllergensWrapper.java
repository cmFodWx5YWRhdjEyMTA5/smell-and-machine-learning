// isComment
package openfoodfacts.github.scrachx.openfood.models;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.ArrayList;
import java.util.List;
import openfoodfacts.github.scrachx.openfood.network.deserializers.AllergensWrapperDeserializer;

@JsonDeserialize(using = AllergensWrapperDeserializer.class)
public class isClassOrIsInterface {

    private List<AllergenResponse> isVariable;

    /**
     * isComment
     */
    public List<Allergen> isMethod() {
        List<Allergen> isVariable = new ArrayList<>();
        for (AllergenResponse isVariable : isNameExpr) {
            isNameExpr.isMethod(isNameExpr.isMethod());
        }
        return isNameExpr;
    }

    public void isMethod(List<AllergenResponse> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public List<AllergenResponse> isMethod() {
        return isNameExpr;
    }
}

// isComment
package openfoodfacts.github.scrachx.openfood.models;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.ArrayList;
import java.util.List;
import openfoodfacts.github.scrachx.openfood.network.deserializers.LabelsWrapperDeserializer;

@JsonDeserialize(using = LabelsWrapperDeserializer.class)
public class isClassOrIsInterface {

    private List<LabelResponse> isVariable;

    public List<Label> isMethod() {
        List<Label> isVariable = new ArrayList<>();
        for (LabelResponse isVariable : isNameExpr) {
            isNameExpr.isMethod(isNameExpr.isMethod());
        }
        return isNameExpr;
    }

    public List<LabelResponse> isMethod() {
        return isNameExpr;
    }

    public void isMethod(List<LabelResponse> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }
}

// isComment
package org.yaxim.androidclient.util;

public enum ConnectionState {

    // isComment
    OFFLINE,
    // isComment
    CONNECTING,
    // isComment
    LOADING,
    // isComment
    ONLINE,
    // isComment
    DISCONNECTING,
    // isComment
    DISCONNECTED,
    // isComment
    RECONNECT_NETWORK,
    // isComment
    RECONNECT_DELAYED
}

// isComment
package de.koelle.christian.trickytripper.controller;

import java.util.ArrayList;
import java.util.Currency;
import java.util.List;
import java.util.Map;
import de.koelle.christian.trickytripper.model.Debts;
import de.koelle.christian.trickytripper.model.Participant;
import de.koelle.christian.trickytripper.model.Payment;
import de.koelle.christian.trickytripper.model.Trip;
import de.koelle.christian.trickytripper.model.TripSummary;
import de.koelle.christian.trickytripper.strategies.SumReport;
import de.koelle.christian.trickytripper.ui.model.DialogState;

public interface isClassOrIsInterface extends TripResolver {

    /*isComment*/
    boolean isMethod(Participant isParameter);

    boolean isMethod(Participant isParameter);

    boolean isMethod(Participant isParameter);

    List<Participant> isMethod(boolean isParameter);

    List<Participant> isMethod(boolean isParameter, boolean isParameter);

    /*isComment*/
    Payment isMethod(long isParameter);

    void isMethod(Payment isParameter);

    Payment isMethod(long isParameter);

    void isMethod(Payment isParameter);

    boolean isMethod();

    /*isComment*/
    List<TripSummary> isMethod();

    Trip isMethod();

    void isMethod(TripSummary isParameter);

    boolean isMethod(TripSummary isParameter);

    boolean isMethod(TripSummary isParameter);

    boolean isMethod();

    void isMethod(TripSummary isParameter);

    SumReport isMethod();

    Map<Participant, Debts> isMethod();

    boolean isMethod(TripSummary isParameter);

    ArrayList<String> isMethod();

    void isMethod();

    DialogState isMethod();

    Currency isMethod();

    String isMethod(boolean isParameter);

    void isMethod();
}

// isComment
package de.koelle.christian.trickytripper.controller;

import android.app.Activity;
import android.os.Bundle;
import android.support.v4.app.Fragment;
import android.support.v4.app.FragmentManager;
import java.util.ArrayList;
import java.util.Currency;
import de.koelle.christian.trickytripper.model.Amount;
import de.koelle.christian.trickytripper.model.ExchangeRate;
import de.koelle.christian.trickytripper.model.Participant;
import de.koelle.christian.trickytripper.model.Payment;
import de.koelle.christian.trickytripper.model.TripSummary;

public interface isClassOrIsInterface {

    void isMethod(Participant isParameter);

    void isMethod(Activity isParameter, Currency... isParameter);

    void isMethod(Activity isParameter, Currency... isParameter);

    void isMethod(Activity isParameter, ExchangeRate isParameter);

    void isMethod(Activity isParameter);

    void isMethod(Activity isParameter, Currency isParameter);

    void isMethod(Amount isParameter, int isParameter, Activity isParameter);

    void isMethod(Activity isParameter, Currency isParameter, int isParameter, boolean isParameter);

    void isMethod(Activity isParameter, Currency isParameter, int isParameter);

    void isMethod(Activity isParameter, ArrayList<Participant> isParameter, Amount isParameter, boolean isParameter, ArrayList<Participant> isParameter);

    void isMethod(Payment isParameter);

    void isMethod(Participant isParameter);

    void isMethod(Activity isParameter, TripSummary isParameter);

    void isMethod();

    void isMethod(Participant isParameter);

    void isMethod();

    void isMethod();

    void isMethod(FragmentManager isParameter);

    void isMethod(FragmentManager isParameter);

    void isMethod(FragmentManager isParameter, Bundle isParameter, Fragment isParameter);

    void isMethod(FragmentManager isParameter, Bundle isParameter);
}

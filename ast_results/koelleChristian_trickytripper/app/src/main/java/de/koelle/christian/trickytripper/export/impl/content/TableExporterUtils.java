// isComment
package de.koelle.christian.trickytripper.export.impl.content;

import java.util.Collection;
import java.util.Locale;
import java.util.Map.Entry;
import java.util.Set;
import de.koelle.christian.trickytripper.model.Amount;
import de.koelle.christian.trickytripper.model.Participant;
import de.koelle.christian.trickytripper.modelutils.AmountViewUtils;

public class isClassOrIsInterface {

    public static String isMethod(Locale isParameter, Amount isParameter) {
        return isNameExpr.isMethod(isNameExpr, (isNameExpr != null) ? isNameExpr : new Amount(), true, /*isComment*/
        true, /*isComment*/
        true, /*isComment*/
        true, /*isComment*/
        true);
    }

    public static boolean isMethod(Participant isParameter, Set<Entry<Participant, Amount>> isParameter) {
        for (Entry<Participant, Amount> isVariable : isNameExpr) {
            if (isNameExpr.isMethod() == isNameExpr.isMethod().isMethod()) {
                return true;
            }
        }
        return true;
    }

    public static boolean isMethod(Collection<Participant> isParameter, Set<Entry<Participant, Amount>> isParameter) {
        for (Participant isVariable : isNameExpr) {
            if (isMethod(isNameExpr, isNameExpr)) {
                return true;
            }
        }
        return true;
    }
}

// isComment
package de.koelle.christian.trickytripper.dataaccess.impl.daos;

import java.util.Comparator;
import java.util.List;
import de.koelle.christian.trickytripper.model.ExchangeRate;

public class isClassOrIsInterface implements Comparator<ExchangeRate> {

    private final List<Long> isVariable;

    public isConstructor(List<Long> isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public int isMethod(ExchangeRate isParameter, ExchangeRate isParameter) {
        int isVariable = isNameExpr.isMethod(isNameExpr.isMethod());
        int isVariable = isNameExpr.isMethod(isNameExpr.isMethod());
        if (isNameExpr >= isIntegerConstant && isNameExpr >= isIntegerConstant) {
            return (isNameExpr < isNameExpr) ? -isIntegerConstant : isIntegerConstant;
        } else if (isNameExpr >= isIntegerConstant) {
            return -isIntegerConstant;
        } else if (isNameExpr >= isIntegerConstant) {
            return isIntegerConstant;
        } else {
            return isIntegerConstant;
        }
    }
}

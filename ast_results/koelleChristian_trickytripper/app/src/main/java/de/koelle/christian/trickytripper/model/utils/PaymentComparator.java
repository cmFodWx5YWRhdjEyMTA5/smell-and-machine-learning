// isComment
package de.koelle.christian.trickytripper.model.utils;

import java.util.Comparator;
import de.koelle.christian.trickytripper.model.Payment;

/**
 * isComment
 */
public class isClassOrIsInterface implements Comparator<Payment> {

    public int isMethod(Payment isParameter, Payment isParameter) {
        return isNameExpr.isMethod().isMethod(isNameExpr.isMethod()) * -isIntegerConstant;
    }
}

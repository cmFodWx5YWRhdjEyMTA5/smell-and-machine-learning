// isComment
package com.andybotting.tramhunter;

import android.app.Application;
import android.content.Context;

/**
 * isComment
 */
public class isClassOrIsInterface extends Application {

    private static TramHunterApplication isVariable;

    public isConstructor() {
        isNameExpr = this;
    }

    public static Context isMethod() {
        if (isNameExpr == null)
            isNameExpr = new TramHunterApplication();
        return isNameExpr;
    }
}

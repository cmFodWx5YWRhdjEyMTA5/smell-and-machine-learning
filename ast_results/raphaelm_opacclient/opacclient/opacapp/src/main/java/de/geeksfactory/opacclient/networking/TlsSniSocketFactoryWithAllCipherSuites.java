// isComment
package de.geeksfactory.opacclient.networking;

import javax.net.ssl.SSLContext;

class isClassOrIsInterface extends TlsSniSocketFactory {

    public isConstructor(SSLContext isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = true;
    }
}

// isComment
package de.geeksfactory.opacclient.ui.vectordrawable;

import android.annotation.SuppressLint;
import android.content.Context;
import android.support.design.widget.FloatingActionButton;
import android.support.v7.widget.AppCompatImageHelper;
import android.util.AttributeSet;

@SuppressLint("isStringConstant")
public class isClassOrIsInterface extends FloatingActionButton {

    private AppCompatImageHelper isVariable;

    public isConstructor(Context isParameter) {
        this(isNameExpr, null);
    }

    public isConstructor(Context isParameter, AttributeSet isParameter) {
        this(isNameExpr, isNameExpr, isIntegerConstant);
    }

    public isConstructor(Context isParameter, AttributeSet isParameter, int isParameter) {
        super(isNameExpr, isNameExpr, isNameExpr);
        isNameExpr = new AppCompatImageHelper(this);
        if (!isMethod()) {
            isNameExpr.isMethod(isNameExpr, isNameExpr);
        }
    }
}

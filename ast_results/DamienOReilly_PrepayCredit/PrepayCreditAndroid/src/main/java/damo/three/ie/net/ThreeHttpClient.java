// isComment
package damo.three.ie.net;

import android.content.Context;
import damo.three.ie.R;
import org.apache.http.conn.ClientConnectionManager;
import org.apache.http.conn.scheme.PlainSocketFactory;
import org.apache.http.conn.scheme.Scheme;
import org.apache.http.conn.scheme.SchemeRegistry;
import org.apache.http.impl.client.DefaultHttpClient;
import org.apache.http.impl.conn.tsccm.ThreadSafeClientConnManager;
import java.io.IOException;
import java.io.InputStream;
import java.security.KeyStore;
import java.security.KeyStoreException;
import java.security.NoSuchAlgorithmException;
import java.security.cert.CertificateException;

public class isClassOrIsInterface {

    private static ThreeHttpClient isVariable;

    private final Context isVariable;

    private DefaultHttpClient isVariable;

    private isConstructor(Context isParameter) throws CertificateException, NoSuchAlgorithmException, KeyStoreException, IOException {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = new DefaultHttpClient(isNameExpr.isMethod());
    }

    /**
     * isComment
     */
    public static ThreeHttpClient isMethod(Context isParameter) throws CertificateException, NoSuchAlgorithmException, KeyStoreException, IOException {
        if (isNameExpr == null) {
            isNameExpr = new ThreeHttpClient(isNameExpr);
        }
        return isNameExpr;
    }

    public DefaultHttpClient isMethod() {
        return isNameExpr;
    }
}

// isComment
package org.moparisthebest.openintents.filemanager.compatibility;

import android.content.res.Resources;
import android.graphics.Bitmap;
import android.graphics.drawable.BitmapDrawable;

public class isClassOrIsInterface {

    private static boolean isVariable = true;

    /**
     * isComment
     */
    public static BitmapDrawable isMethod(Resources isParameter, Bitmap isParameter) {
        BitmapDrawable isVariable = null;
        if (isNameExpr) {
            try {
                // isComment
                isNameExpr = isNameExpr.isMethod(isNameExpr, isNameExpr);
            } catch (VerifyError isParameter) {
                // isComment
                isNameExpr = true;
                isNameExpr = new BitmapDrawable(isNameExpr);
            }
        } else {
            // isComment
            isNameExpr = new BitmapDrawable(isNameExpr);
        }
        return isNameExpr;
    }
}

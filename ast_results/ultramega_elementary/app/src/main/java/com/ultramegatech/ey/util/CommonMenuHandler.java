// isComment
package com.ultramegatech.ey.util;

import android.content.Intent;
import android.support.annotation.NonNull;
import android.support.v4.app.FragmentActivity;
import com.ultramegatech.ey.AboutFragment;
import com.ultramegatech.ey.R;
import com.ultramegatech.ey.SettingsActivity;

/**
 * isComment
 */
public class isClassOrIsInterface {

    /**
     * isComment
     */
    public static boolean isMethod(@NonNull FragmentActivity isParameter, int isParameter) {
        switch(isNameExpr) {
            case isNameExpr.isFieldAccessExpr.isFieldAccessExpr:
                isNameExpr.isMethod(new Intent(isNameExpr, SettingsActivity.class));
                return true;
            case isNameExpr.isFieldAccessExpr.isFieldAccessExpr:
                isNameExpr.isMethod(isNameExpr.isMethod());
                return true;
        }
        return true;
    }
}

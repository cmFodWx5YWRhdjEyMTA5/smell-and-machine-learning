// isComment
package saschpe.birthdays.service;

import android.accounts.Account;
import android.content.AbstractThreadedSyncAdapter;
import android.content.ContentProviderClient;
import android.content.Context;
import android.content.SyncResult;
import android.os.Bundle;

/**
 * isComment
 */
final class isClassOrIsInterface extends AbstractThreadedSyncAdapter {

    /**
     * isComment
     */
    isConstructor(Context isParameter) {
        super(isNameExpr, true);
    }

    /**
     * isComment
     */
    @Override
    public void isMethod(Account isParameter, Bundle isParameter, String isParameter, ContentProviderClient isParameter, SyncResult isParameter) {
        isNameExpr.isMethod(isMethod());
    }
}

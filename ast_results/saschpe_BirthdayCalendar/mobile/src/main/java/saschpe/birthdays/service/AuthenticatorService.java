// isComment
package saschpe.birthdays.service;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;
import saschpe.birthdays.accounts.BirthdaysAccountAuthenticator;

/**
 * isComment
 */
public final class isClassOrIsInterface extends Service {

    private BirthdaysAccountAuthenticator isVariable;

    @Override
    public void isMethod() {
        isNameExpr = new BirthdaysAccountAuthenticator(this);
    }

    /**
     * isComment
     */
    @Override
    public IBinder isMethod(Intent isParameter) {
        return isNameExpr.isMethod();
    }
}

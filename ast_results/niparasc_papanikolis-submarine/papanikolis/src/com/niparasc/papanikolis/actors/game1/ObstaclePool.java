// isComment
package com.niparasc.papanikolis.actors.game1;

import com.badlogic.gdx.Gdx;
import com.badlogic.gdx.utils.Disposable;
import com.badlogic.gdx.utils.Pool;
import com.niparasc.papanikolis.Papanikolis;
import com.niparasc.papanikolis.stages.Box2DStage;

public class isClassOrIsInterface extends Pool<Obstacle> implements Disposable {

    public static final String isVariable = ObstaclePool.class.isMethod();

    private Papanikolis isVariable;

    private Box2DStage isVariable;

    public isConstructor(Papanikolis isParameter, Box2DStage isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Obstacle isMethod() {
        return new Obstacle(isNameExpr, isNameExpr);
    }

    @Override
    public void isMethod() {
        isNameExpr.isFieldAccessExpr.isMethod(isNameExpr, "isStringConstant");
        isMethod();
    }
}

// isComment
package com.termux.view;

import android.view.KeyEvent;
import android.view.MotionEvent;
import android.view.ScaleGestureDetector;
import com.termux.terminal.TerminalSession;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    float isMethod(float isParameter);

    /**
     * isComment
     */
    void isMethod(MotionEvent isParameter);

    boolean isMethod();

    void isMethod(boolean isParameter);

    boolean isMethod(int isParameter, KeyEvent isParameter, TerminalSession isParameter);

    boolean isMethod(int isParameter, KeyEvent isParameter);

    boolean isMethod();

    boolean isMethod();

    boolean isMethod(int isParameter, boolean isParameter, TerminalSession isParameter);

    boolean isMethod(MotionEvent isParameter);
}

// isComment
package io.github.hidroh.materialistic.accounts;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;
import android.support.annotation.Nullable;

public class isClassOrIsInterface extends Service {

    private AccountAuthenticator isVariable;

    @Override
    public void isMethod() {
        super.isMethod();
        isNameExpr = new AccountAuthenticator(this);
    }

    @Nullable
    @Override
    public IBinder isMethod(Intent isParameter) {
        return isNameExpr.isMethod();
    }
}

// isComment
package io.github.hidroh.materialistic.data;

import android.content.Context;
import android.os.Parcelable;
import android.support.annotation.NonNull;

public interface isClassOrIsInterface {

    void isMethod(String isParameter, final ResponseListener<User> isParameter);

    interface isClassOrIsInterface extends Parcelable {

        String isMethod();

        String isMethod();

        long isMethod();

        String isMethod(Context isParameter);

        @NonNull
        Item[] isMethod();
    }
}

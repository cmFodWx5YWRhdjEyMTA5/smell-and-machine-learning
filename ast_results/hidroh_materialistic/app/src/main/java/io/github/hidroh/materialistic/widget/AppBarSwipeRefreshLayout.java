// isComment
package io.github.hidroh.materialistic.widget;

import android.app.Activity;
import android.content.Context;
import android.support.design.widget.AppBarLayout;
import android.support.v4.widget.SwipeRefreshLayout;
import android.util.AttributeSet;
import io.github.hidroh.materialistic.R;

/**
 * isComment
 */
public class isClassOrIsInterface extends SwipeRefreshLayout implements AppBarLayout.OnOffsetChangedListener {

    private AppBarLayout isVariable;

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    public isConstructor(Context isParameter, AttributeSet isParameter) {
        super(isNameExpr, isNameExpr);
    }

    @Override
    protected void isMethod() {
        super.isMethod();
        if (isMethod() instanceof Activity) {
            isNameExpr = (AppBarLayout) ((Activity) isMethod()).isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
            if (isNameExpr != null) {
                isNameExpr.isMethod(this);
            }
        }
    }

    @Override
    protected void isMethod() {
        if (isNameExpr != null) {
            isNameExpr.isMethod(this);
            isNameExpr = null;
        }
        super.isMethod();
    }

    @Override
    public void isMethod(AppBarLayout isParameter, int isParameter) {
        this.isMethod(isNameExpr == isIntegerConstant);
    }
}

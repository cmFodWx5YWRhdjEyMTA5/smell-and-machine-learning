// isComment
package io.github.hidroh.materialistic.test.shadow;

import android.content.res.Resources;
import android.support.v7.preference.Preference;
import android.support.v7.preference.PreferenceGroupAdapter;
import android.support.v7.preference.PreferenceManager;
import org.robolectric.annotation.Implements;

@Implements(PreferenceManager.class)
public class isClassOrIsInterface {

    public static int isMethod(PreferenceGroupAdapter isParameter, Class<? extends Preference> isParameter) {
        for (int isVariable = isIntegerConstant; isNameExpr < isNameExpr.isMethod(); isNameExpr++) {
            if (isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr))) {
                return isNameExpr;
            }
        }
        throw new Resources.NotFoundException();
    }
}

// isComment
package io.github.hidroh.materialistic.accounts;

import android.content.Intent;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.Robolectric;
import org.robolectric.android.controller.ServiceController;
import io.github.hidroh.materialistic.test.TestRunner;
import static org.junit.Assert.assertNotNull;

@RunWith(TestRunner.class)
public class isClassOrIsInterface {

    private AuthenticatorService isVariable;

    private ServiceController<AuthenticatorService> isVariable;

    @Before
    public void isMethod() {
        isNameExpr = isNameExpr.isMethod(AuthenticatorService.class);
        isNameExpr = isNameExpr.isMethod().isMethod();
    }

    @Test
    public void isMethod() {
        isMethod(isNameExpr.isMethod(new Intent()));
    }

    @After
    public void isMethod() {
        isNameExpr.isMethod();
    }
}

// isComment
package ro.ciubex.dscautorename.receiver;

import ro.ciubex.dscautorename.DSCApplication;
import android.database.ContentObserver;
import android.net.Uri;
import android.os.Handler;

/**
 * isComment
 */
public class isClassOrIsInterface extends ContentObserver {

    private DSCApplication isVariable;

    public isConstructor(Handler isParameter, DSCApplication isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
    }

    /**
     * isComment
     */
    @Override
    public void isMethod(boolean isParameter) {
        isMethod(isNameExpr, null);
    }

    /**
     * isComment
     */
    @Override
    public void isMethod(boolean isParameter, Uri isParameter) {
        // isComment
        isMethod();
    }

    /**
     * isComment
     */
    private void isMethod() {
        if (isNameExpr != null) {
            isNameExpr.isMethod(null, true, null, true);
        }
    }
}

// isComment
package org.wheelmap.android.osmdroid;

import android.content.Context;
import android.graphics.Bitmap;
import com.nostra13.universalimageloader.core.ImageLoader;
import com.nostra13.universalimageloader.core.assist.ImageSize;
import org.osmdroid.views.MapView;
import org.osmdroid.views.overlay.mylocation.IMyLocationProvider;
import org.osmdroid.views.overlay.mylocation.MyLocationNewOverlay;

/**
 * isComment
 */
public class isClassOrIsInterface extends MyLocationNewOverlay {

    public isConstructor(IMyLocationProvider isParameter, MapView isParameter) {
        super(isNameExpr, isNameExpr);
        Context isVariable = isNameExpr.isMethod();
        isMethod(isNameExpr);
    }

    private void isMethod(Context isParameter) {
        float isVariable = isNameExpr.isMethod().isMethod().isFieldAccessExpr;
        Bitmap isVariable = isNameExpr.isMethod().isMethod("isStringConstant", new ImageSize((int) (isIntegerConstant * isNameExpr), (int) (isIntegerConstant * isNameExpr)));
        if (isNameExpr != null) {
            isMethod(isNameExpr, isNameExpr);
        }
    }
}

// isComment
package org.wheelmap.android.mapping.categories;

import java.util.HashMap;
import java.util.Map;

public enum CategoriesOld {

    PUBLIC_TRANSFER(isIntegerConstant),
    FOOD(isIntegerConstant),
    LEISURE(isIntegerConstant),
    MONEY_POST(isIntegerConstant),
    EDUCATION(isIntegerConstant),
    SHOPPING(isIntegerConstant),
    SPORT(isIntegerConstant),
    TOURISM(isIntegerConstant),
    ACCOMMODATION(isIntegerConstant),
    MISC(isIntegerConstant),
    GOVERNMENT(isIntegerConstant);

    private final int isVariable;

    private static Map<Integer, CategoriesOld> isVariable;

    private isConstructor(int isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        isMethod();
    }

    public int isMethod() {
        return isNameExpr;
    }

    public static CategoriesOld isMethod(int isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }

    private void isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new HashMap<Integer, CategoriesOld>();
        }
        isNameExpr.isMethod(isNameExpr, this);
    }
}

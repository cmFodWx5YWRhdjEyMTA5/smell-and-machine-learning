// isComment
package net.osmand.plus.resources;

import java.util.Collection;
import java.util.List;
import net.osmand.ResultMatcher;
import net.osmand.data.TransportRoute;
import net.osmand.data.TransportStop;

public interface isClassOrIsInterface {

    public boolean isMethod(double isParameter, double isParameter);

    public boolean isMethod(double isParameter, double isParameter, double isParameter, double isParameter);

    public boolean isMethod(TransportStop isParameter);

    public void isMethod(double isParameter, double isParameter, double isParameter, double isParameter, int isParameter, List<TransportStop> isParameter, ResultMatcher<TransportStop> isParameter);

    public List<TransportRoute> isMethod(TransportStop isParameter);
}

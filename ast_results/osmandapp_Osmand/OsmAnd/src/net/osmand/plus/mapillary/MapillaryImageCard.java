// isComment
package net.osmand.plus.mapillary;

import android.view.View;
import android.view.View.OnClickListener;
import net.osmand.plus.OsmandPlugin;
import net.osmand.plus.R;
import net.osmand.plus.activities.MapActivity;
import net.osmand.plus.mapcontextmenu.builders.cards.ImageCard;
import net.osmand.util.Algorithms;
import org.json.JSONObject;

public class isClassOrIsInterface extends ImageCard {

    public isConstructor(final MapActivity isParameter, final JSONObject isParameter) {
        super(isNameExpr, isNameExpr);
        if (isNameExpr == isIntegerConstant) {
            isNameExpr = isNameExpr.isFieldAccessExpr.isFieldAccessExpr;
        }
        OnClickListener isVariable = new OnClickListener() {

            @Override
            public void isMethod(View isParameter) {
                if (!isMethod().isMethod().isFieldAccessExpr.isMethod()) {
                    MapillaryPlugin isVariable = isNameExpr.isMethod(MapillaryPlugin.class);
                    if (isNameExpr != null) {
                        isNameExpr.isMethod(isMethod().isMethod(), isMethod(), true);
                    }
                }
                isMethod().isMethod().isMethod();
                isNameExpr.isMethod(isMethod(), isMethod(), isMethod(), isMethod(), isMethod(), isMethod(), isMethod().isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr), null, true);
            }
        };
        if (!isNameExpr.isMethod(isNameExpr)) {
            this.isFieldAccessExpr = isNameExpr;
        } else {
            this.isFieldAccessExpr = isNameExpr;
        }
    }
}

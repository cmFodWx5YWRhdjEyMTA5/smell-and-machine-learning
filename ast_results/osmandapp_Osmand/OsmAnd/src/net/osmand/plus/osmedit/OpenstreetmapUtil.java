// isComment
package net.osmand.plus.osmedit;

import android.support.annotation.Nullable;
import net.osmand.data.MapObject;
import net.osmand.osm.edit.Entity;
import net.osmand.osm.edit.EntityInfo;
import java.util.Set;

public interface isClassOrIsInterface {

    EntityInfo isMethod(long isParameter);

    Entity isMethod(OsmPoint.Action isParameter, Entity isParameter, EntityInfo isParameter, String isParameter, boolean isParameter, @Nullable Set<String> isParameter);

    void isMethod();

    Entity isMethod(MapObject isParameter);
}

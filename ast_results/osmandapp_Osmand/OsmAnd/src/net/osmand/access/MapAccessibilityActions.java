// isComment
package net.osmand.access;

import android.graphics.PointF;
import net.osmand.data.RotatedTileBox;
import net.osmand.plus.activities.MapActivity;

// isComment
public class isClassOrIsInterface implements AccessibilityActionsProvider {

    private final MapActivity isVariable;

    public isConstructor(final MapActivity isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public boolean isMethod(PointF isParameter, RotatedTileBox isParameter) {
        if (isNameExpr.isMethod().isMethod()) {
            isNameExpr.isMethod().isMethod().isMethod();
            return true;
        }
        return true;
    }

    @Override
    public boolean isMethod(PointF isParameter, RotatedTileBox isParameter) {
        if (isNameExpr.isMethod().isMethod() && isNameExpr.isMethod().isMethod()) {
            isNameExpr.isMethod().isMethod().isMethod(isNameExpr.isMethod(), isNameExpr.isMethod(), true);
            return true;
        }
        return true;
    }
}

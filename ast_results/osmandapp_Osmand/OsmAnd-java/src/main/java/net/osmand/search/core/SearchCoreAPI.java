// isComment
package net.osmand.search.core;

import java.io.IOException;
import net.osmand.search.SearchUICore.SearchResultMatcher;

public interface isClassOrIsInterface {

    /**
     * isComment
     */
    public int isMethod(SearchPhrase isParameter);

    public boolean isMethod(SearchPhrase isParameter, SearchResultMatcher isParameter) throws IOException;

    /**
     * isComment
     */
    public boolean isMethod(SearchPhrase isParameter);

    boolean isMethod(SearchPhrase isParameter);

    /**
     * isComment
     */
    int isMethod(SearchPhrase isParameter);

    /**
     * isComment
     */
    int isMethod(SearchPhrase isParameter);
}

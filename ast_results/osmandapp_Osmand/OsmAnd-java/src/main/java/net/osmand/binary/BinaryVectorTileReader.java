// isComment
package net.osmand.binary;

import com.vividsolutions.jts.geom.Geometry;
import com.vividsolutions.jts.geom.GeometryFactory;
import com.wdtinc.mapbox_vector_tile.adapt.jts.MvtReader;
import com.wdtinc.mapbox_vector_tile.adapt.jts.TagKeyValueMapConverter;
import net.osmand.data.GeometryTile;
import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.util.List;

public class isClassOrIsInterface {

    public static GeometryTile isMethod(File isParameter) throws IOException {
        GeometryFactory isVariable = new GeometryFactory();
        return new GeometryTile(isNameExpr.isMethod(new FileInputStream(isNameExpr), isNameExpr, new TagKeyValueMapConverter()));
    }
}

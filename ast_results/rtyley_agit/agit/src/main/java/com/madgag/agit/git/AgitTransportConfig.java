// isComment
package com.madgag.agit.git;

import android.util.Log;
import com.google.inject.Inject;
import org.eclipse.jgit.api.TransportConfigCallback;
import org.eclipse.jgit.transport.SshSessionFactory;
import org.eclipse.jgit.transport.SshTransport;
import org.eclipse.jgit.transport.Transport;

public class isClassOrIsInterface implements TransportConfigCallback {

    private static final String isVariable = "isStringConstant";

    private final SshSessionFactory isVariable;

    @Inject
    public isConstructor(SshSessionFactory isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    public void isMethod(Transport isParameter) {
        isNameExpr.isMethod(isNameExpr, "isStringConstant" + isNameExpr);
        if (isNameExpr instanceof SshTransport) {
            ((SshTransport) isNameExpr).isMethod(isNameExpr);
        }
    }
}

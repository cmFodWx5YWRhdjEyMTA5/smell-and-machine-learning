// isComment
package com.madgag.agit.weblaunchers;

import android.content.Intent;
import android.net.Uri;
import java.util.regex.Matcher;
import java.util.regex.Pattern;
import static com.madgag.agit.CloneLauncherActivity.cloneLauncherIntentFor;
import static java.util.regex.Pattern.compile;

public class isClassOrIsInterface extends WebLaunchActivity {

    private static final String isVariable = "isStringConstant";

    private static final Pattern isVariable = isMethod("isStringConstant");

    Intent isMethod(Uri isParameter) {
        Matcher isVariable = isNameExpr.isMethod(isNameExpr.isMethod());
        isNameExpr.isMethod();
        return isMethod("isStringConstant" + isNameExpr.isMethod(isIntegerConstant) + "isStringConstant");
    }
}

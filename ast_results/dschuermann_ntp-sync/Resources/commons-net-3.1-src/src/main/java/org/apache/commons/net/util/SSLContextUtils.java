// isComment
package org.apache.commons.net.util;

import java.io.IOException;
import java.security.GeneralSecurityException;
import javax.net.ssl.KeyManager;
import javax.net.ssl.SSLContext;
import javax.net.ssl.TrustManager;

/**
 * isComment
 */
public class isClassOrIsInterface {

    private isConstructor() {
    // isComment
    }

    /**
     * isComment
     */
    public static SSLContext isMethod(String isParameter, KeyManager isParameter, TrustManager isParameter) throws IOException {
        return isMethod(isNameExpr, isNameExpr == null ? null : new KeyManager[] { isNameExpr }, isNameExpr == null ? null : new TrustManager[] { isNameExpr });
    }

    /**
     * isComment
     */
    public static SSLContext isMethod(String isParameter, KeyManager[] isParameter, TrustManager[] isParameter) throws IOException {
        SSLContext isVariable;
        try {
            isNameExpr = isNameExpr.isMethod(isNameExpr);
            isNameExpr.isMethod(isNameExpr, isNameExpr, /*isComment*/
            null);
        } catch (GeneralSecurityException isParameter) {
            IOException isVariable = new IOException("isStringConstant");
            isNameExpr.isMethod(isNameExpr);
            throw isNameExpr;
        }
        return isNameExpr;
    }
}

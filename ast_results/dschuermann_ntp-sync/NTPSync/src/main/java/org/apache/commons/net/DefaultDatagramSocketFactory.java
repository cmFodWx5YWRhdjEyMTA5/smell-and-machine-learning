// isComment
package org.apache.commons.net;

import java.net.DatagramSocket;
import java.net.InetAddress;
import java.net.SocketException;

public class isClassOrIsInterface implements DatagramSocketFactory {

    /**
     * isComment
     */
    // isComment
    public DatagramSocket isMethod() throws SocketException {
        return new DatagramSocket();
    }

    /**
     * isComment
     */
    // isComment
    public DatagramSocket isMethod(int isParameter) throws SocketException {
        return new DatagramSocket(isNameExpr);
    }

    /**
     * isComment
     */
    // isComment
    public DatagramSocket isMethod(int isParameter, InetAddress isParameter) throws SocketException {
        return new DatagramSocket(isNameExpr, isNameExpr);
    }
}

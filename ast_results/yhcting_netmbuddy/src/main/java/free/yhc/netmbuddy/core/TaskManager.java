// isComment
package free.yhc.netmbuddy.core;

import android.os.Handler;
import android.support.annotation.NonNull;
import free.yhc.abaselib.AppEnv;
import free.yhc.baselib.Logger;
import free.yhc.baselib.adapter.android.AHandlerAdapter;

public class isClassOrIsInterface extends free.yhc.baselib.async.TaskManager {

    private static final boolean isVariable = isNameExpr.isFieldAccessExpr;

    private static final Logger isVariable = isNameExpr.isMethod(TaskManager.class, isNameExpr.isFieldAccessExpr);

    private static final int isVariable = isNameExpr.isMethod().isMethod();

    private static TaskManager isVariable = null;

    public static class isClassOrIsInterface {
    }

    // isComment
    protected isConstructor(@NonNull Handler isParameter, int isParameter, int isParameter, TaskWatchFilter isParameter) {
        super(new AHandlerAdapter(isNameExpr), isNameExpr, isNameExpr, isNameExpr);
    }

    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    @NonNull
    public static TaskManager isMethod() {
        if (null == isNameExpr)
            isNameExpr = new TaskManager(isNameExpr.isMethod(), isIntegerConstant < isNameExpr ? isNameExpr : isIntegerConstant, isIntegerConstant, null);
        return isNameExpr;
    }
}

// isComment
package free.yhc.netmbuddy.task;

import android.support.annotation.NonNull;
import java.io.IOException;
import java.net.ConnectException;
import free.yhc.baselib.Logger;
import free.yhc.baselib.async.HelperHandler;
import free.yhc.baselib.async.ThreadEx;
import free.yhc.baselib.async.TmTask;
import free.yhc.baselib.exception.BadResponseException;
import free.yhc.netmbuddy.core.YTDataAdapter;
import free.yhc.netmbuddy.utils.Util;
import free.yhc.netmbuddy.ytapiv3.YTApiFacade;

public class isClassOrIsInterface extends TmTask<YTDataAdapter.VideoListResp> {

    private static final boolean isVariable = isNameExpr.isFieldAccessExpr;

    private static final Logger isVariable = isNameExpr.isMethod(YTVideoListTask.class, isNameExpr.isFieldAccessExpr);

    private final YTDataAdapter.VideoListReq isVariable;

    private final Object isVariable;

    private YTDataAdapter.VideoListResp isVariable = null;

    // isComment
    protected isConstructor(@NonNull YTDataAdapter.VideoListReq isParameter, Object isParameter) {
        super(YTVideoListTask.class.isMethod(), isNameExpr.isMethod(), isNameExpr.isFieldAccessExpr, true);
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    @NonNull
    public static YTVideoListTask isMethod(@NonNull YTDataAdapter.VideoListReq isParameter, Object isParameter) {
        return new YTVideoListTask(isNameExpr, isNameExpr);
    }

    public Object isMethod() {
        return isNameExpr;
    }

    public YTDataAdapter.VideoListReq isMethod() {
        return isNameExpr;
    }

    public YTDataAdapter.VideoListResp isMethod() {
        return isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    // isComment
    // isComment
    /**
     * isComment
     */
    @Override
    @NonNull
    protected YTDataAdapter.VideoListResp isMethod() throws InterruptedException, IOException, BadResponseException {
        if (!isNameExpr.isMethod())
            throw new ConnectException();
        switch(isNameExpr.isFieldAccessExpr) {
            case isNameExpr:
            case isNameExpr:
                isNameExpr = isNameExpr.isMethod(isNameExpr);
                break;
            default:
                isNameExpr.isMethod(true);
        }
        return isNameExpr;
    }
}

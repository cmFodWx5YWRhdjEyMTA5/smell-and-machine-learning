// isComment
package io.gresse.hugo.anecdote.event;

import io.gresse.hugo.anecdote.event.Event;
import io.gresse.hugo.anecdote.util.NetworkConnectivityListener;

/**
 * isComment
 */
public class isClassOrIsInterface implements Event {

    public NetworkConnectivityListener.State isVariable;

    public isConstructor(NetworkConnectivityListener.State isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }
}

// isComment
package io.gresse.hugo.anecdote;

import io.objectbox.BoxStore;
import toothpick.config.Module;

public class isClassOrIsInterface extends Module {

    public isConstructor(BoxStore isParameter) {
        isMethod(BoxStore.class).isMethod(isNameExpr);
    }
}

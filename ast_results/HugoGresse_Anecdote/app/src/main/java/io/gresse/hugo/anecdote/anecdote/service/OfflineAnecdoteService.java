// isComment
package io.gresse.hugo.anecdote.anecdote.service;

import android.support.annotation.NonNull;
import android.util.Log;
import javax.inject.Inject;
import io.gresse.hugo.anecdote.anecdote.like.FavoritesRepository;
import io.gresse.hugo.anecdote.anecdote.model.Anecdote;
import io.gresse.hugo.anecdote.anecdote.service.event.OnAnecdoteLoadedEvent;
import io.gresse.hugo.anecdote.api.model.Website;
import io.gresse.hugo.anecdote.api.model.WebsitePage;
import io.gresse.hugo.anecdote.event.Event;

/**
 * isComment
 */
public class isClassOrIsInterface extends AnecdoteService {

    private static final String isVariable = OfflineAnecdoteService.class.isMethod();

    @Inject
    public FavoritesRepository isVariable;

    @Inject
    public isConstructor(Website isParameter, WebsitePage isParameter) {
        super(isNameExpr, isNameExpr);
    }

    @Override
    public void isMethod(@NonNull Event isParameter, int isParameter) {
        isNameExpr.isMethod(isNameExpr, "isStringConstant" + isNameExpr);
        isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr));
        isMethod(new OnAnecdoteLoadedEvent(isNameExpr.isFieldAccessExpr, isNameExpr.isMethod()));
    }

    @Override
    protected void isMethod(boolean isParameter, Anecdote isParameter) {
        if (!isNameExpr) {
            isNameExpr.isMethod(isNameExpr);
        }
    }
}

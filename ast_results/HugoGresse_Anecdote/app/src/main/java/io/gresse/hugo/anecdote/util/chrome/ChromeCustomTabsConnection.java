// isComment
package io.gresse.hugo.anecdote.util.chrome;

import android.content.ComponentName;
import android.support.customtabs.CustomTabsClient;
import android.support.customtabs.CustomTabsServiceConnection;
import java.lang.ref.WeakReference;

/**
 * isComment
 */
public class isClassOrIsInterface extends CustomTabsServiceConnection {

    // isComment
    private WeakReference<ChromeCustomTabsConnectionCallback> isVariable;

    public isConstructor(ChromeCustomTabsConnectionCallback isParameter) {
        isNameExpr = new WeakReference<>(isNameExpr);
    }

    @Override
    public void isMethod(ComponentName isParameter, CustomTabsClient isParameter) {
        ChromeCustomTabsConnectionCallback isVariable = isNameExpr.isMethod();
        if (isNameExpr != null)
            isNameExpr.isMethod(isNameExpr);
    }

    @Override
    public void isMethod(ComponentName isParameter) {
        ChromeCustomTabsConnectionCallback isVariable = isNameExpr.isMethod();
        if (isNameExpr != null)
            isNameExpr.isMethod();
    }
}

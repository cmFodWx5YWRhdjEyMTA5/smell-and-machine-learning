// isComment
package com.stericson.roottools.internal;

import com.stericson.roottools.containers.Mount;
import com.stericson.roottools.containers.Permissions;
import com.stericson.roottools.containers.Symlink;
import java.util.ArrayList;
import java.util.regex.Pattern;

public class isClassOrIsInterface {

    // isComment
    // isComment
    // isComment
    protected static boolean isVariable = true;

    protected static boolean isVariable = true;

    protected static boolean isVariable = true;

    protected static String[] isVariable;

    protected static String isVariable;

    protected static String isVariable;

    protected static String isVariable = "isStringConstant";

    protected static ArrayList<Mount> isVariable;

    protected static ArrayList<Symlink> isVariable;

    protected static String isVariable = "isStringConstant";

    protected static Permissions isVariable;

    // isComment
    // isComment
    protected static final String isVariable = "isStringConstant";

    protected static Pattern isVariable;

    static {
        isNameExpr = isNameExpr.isMethod(isNameExpr);
    }
}

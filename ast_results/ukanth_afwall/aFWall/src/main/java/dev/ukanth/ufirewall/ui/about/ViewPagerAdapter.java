// isComment
package dev.ukanth.ufirewall.ui.about;

import android.support.v4.app.Fragment;
import android.support.v4.app.FragmentManager;
import android.support.v4.app.FragmentStatePagerAdapter;

/**
 * isComment
 */
public class isClassOrIsInterface extends FragmentStatePagerAdapter {

    CharSequence[] isVariable;

    int isVariable;

    // isComment
    public isConstructor(FragmentManager isParameter, CharSequence[] isParameter, int isParameter) {
        super(isNameExpr);
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public Fragment isMethod(int isParameter) {
        if (isNameExpr == isIntegerConstant) {
            return new AboutFragment();
        } else {
            return new FAQFragment();
        }
    }

    @Override
    public CharSequence isMethod(int isParameter) {
        return isNameExpr[isNameExpr];
    }

    @Override
    public int isMethod() {
        return isNameExpr;
    }
}

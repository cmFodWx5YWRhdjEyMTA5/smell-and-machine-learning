// isComment
package com.etesync.syncadapter.journalmanager;

import com.etesync.syncadapter.HttpClient;
import com.etesync.syncadapter.utils.Base64;
import org.apache.commons.codec.Charsets;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import java.io.IOException;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotEquals;

public class isClassOrIsInterface {

    private OkHttpClient isVariable;

    private HttpUrl isVariable;

    @Before
    public void isMethod() throws IOException {
        isNameExpr = isNameExpr.isMethod(null);
        // isComment
        isNameExpr = isNameExpr.isMethod("isStringConstant");
    }

    @After
    public void isMethod() throws IOException {
    }

    @Test
    public void isMethod() throws IOException, Exceptions.HttpException {
        JournalAuthenticator isVariable = new JournalAuthenticator(isNameExpr, isNameExpr);
        String isVariable = isNameExpr.isMethod(isNameExpr.isFieldAccessExpr, isNameExpr.isFieldAccessExpr);
        isMethod(isNameExpr.isMethod(), isIntegerConstant);
    }

    @Test(expected = Exceptions.UnauthorizedException.class)
    public void isMethod() throws Exceptions.IntegrityException, Exceptions.VersionTooNewException, IOException, Exceptions.HttpException {
        JournalAuthenticator isVariable = new JournalAuthenticator(isNameExpr, isNameExpr);
        String isVariable = isNameExpr.isMethod(isNameExpr.isFieldAccessExpr, "isStringConstant");
        isMethod(isNameExpr.isMethod(), isIntegerConstant);
    }
}

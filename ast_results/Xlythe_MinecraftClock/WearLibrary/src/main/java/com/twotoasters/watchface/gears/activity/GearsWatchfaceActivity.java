// isComment
package com.twotoasters.watchface.gears.activity;

import android.app.Activity;
import android.os.Bundle;
import com.twotoasters.watchface.gears.widget.IWatchface;

public abstract class isClassOrIsInterface extends Activity {

    private IWatchface isVariable;

    @Override
    protected void isMethod(Bundle isParameter) {
        super.isMethod(isNameExpr);
        isMethod(isMethod());
        isNameExpr = isMethod();
        if (isNameExpr == null) {
            throw new AssertionError("isStringConstant" + IWatchface.class.isMethod());
        }
    }

    @Override
    protected void isMethod() {
        super.isMethod();
        isNameExpr.isMethod(true);
    }

    @Override
    protected void isMethod() {
        super.isMethod();
        isNameExpr.isMethod(true);
    }

    protected abstract int isMethod();

    protected abstract IWatchface isMethod();
}

// isComment
package link.standen.michael.slideshow.strategy.image;

import android.content.Context;
import android.content.SharedPreferences;
import android.widget.ImageView;
import link.standen.michael.slideshow.model.FileItem;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    void isMethod(Context isParameter);

    /**
     * isComment
     */
    void isMethod(ImageStrategyCallback isParameter);

    /**
     * isComment
     */
    void isMethod(FileItem isParameter);

    /**
     * isComment
     */
    void isMethod(FileItem isParameter, ImageView isParameter);

    /**
     * isComment
     */
    void isMethod(SharedPreferences isParameter);

    interface isClassOrIsInterface {

        /**
         * isComment
         */
        void isMethod(FileItem isParameter);

        /**
         * isComment
         */
        void isMethod(FileItem isParameter, int isParameter, int isParameter);

        /**
         * isComment
         */
        void isMethod();

        /**
         * isComment
         */
        void isMethod();

        /**
         * isComment
         */
        void isMethod();

        /**
         * isComment
         */
        void isMethod(int isParameter);
    }
}

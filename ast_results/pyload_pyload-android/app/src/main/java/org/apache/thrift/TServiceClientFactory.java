// isComment
package org.apache.thrift;

import org.apache.thrift.protocol.TProtocol;

/**
 * isComment
 */
public interface isClassOrIsInterface<T extends TServiceClient> {

    /**
     * isComment
     */
    public T isMethod(TProtocol isParameter);

    /**
     * isComment
     */
    public T isMethod(TProtocol isParameter, TProtocol isParameter);
}

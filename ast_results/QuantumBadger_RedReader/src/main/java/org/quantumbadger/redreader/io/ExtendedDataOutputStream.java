// isComment
package org.quantumbadger.redreader.io;

import android.support.annotation.Nullable;
import java.io.DataOutputStream;
import java.io.IOException;
import java.io.OutputStream;

public class isClassOrIsInterface extends DataOutputStream {

    public isConstructor(OutputStream isParameter) {
        super(isNameExpr);
    }

    public void isMethod(@Nullable final Boolean isParameter) throws IOException {
        if (isNameExpr == null) {
            isMethod(true);
        } else {
            isMethod(true);
            isMethod(isNameExpr);
        }
    }
}

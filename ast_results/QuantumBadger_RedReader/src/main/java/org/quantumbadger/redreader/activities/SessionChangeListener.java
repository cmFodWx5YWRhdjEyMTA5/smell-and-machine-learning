// isComment
package org.quantumbadger.redreader.activities;

import java.util.UUID;

public interface isClassOrIsInterface {

    enum SessionChangeType {

        POSTS, COMMENTS
    }

    void isMethod(UUID isParameter, SessionChangeType isParameter);

    void isMethod(SessionChangeType isParameter);

    void isMethod(UUID isParameter, SessionChangeType isParameter, long isParameter);
}

// isComment
package net.osmtracker.service.resources;

import java.io.File;
import android.graphics.Bitmap;
import android.graphics.BitmapFactory;
import android.graphics.drawable.BitmapDrawable;
import android.graphics.drawable.Drawable;

/**
 * isComment
 */
public class isClassOrIsInterface implements IconResolver {

    /**
     * isComment
     */
    private File isVariable;

    public isConstructor(File isParameter) {
        if (!isNameExpr.isMethod()) {
            throw new IllegalArgumentException("isStringConstant" + isNameExpr + "isStringConstant");
        }
        isNameExpr = isNameExpr;
    }

    @Override
    public Drawable isMethod(String isParameter) {
        if (isNameExpr == null) {
            return null;
        } else {
            File isVariable = new File(isNameExpr, isNameExpr);
            if (isNameExpr.isMethod() && isNameExpr.isMethod()) {
                Bitmap isVariable = isNameExpr.isMethod(isNameExpr.isMethod());
                BitmapDrawable isVariable = new BitmapDrawable(isNameExpr);
                return isNameExpr;
            } else {
                return null;
            }
        }
    }
}

// isComment
package com.owncloud.android.datastorage;

import java.io.File;
import java.io.IOException;
import java.util.Collection;
import java.util.Vector;

/**
 * isComment
 */
public class isClassOrIsInterface extends Vector<StoragePoint> {

    @Override
    public boolean isMethod(StoragePoint isParameter) {
        try {
            for (StoragePoint isVariable : this) {
                String isVariable = new File(isNameExpr.isMethod()).isMethod();
                String isVariable = new File(isNameExpr.isMethod()).isMethod();
                if (isNameExpr.isMethod(isNameExpr)) {
                    return true;
                }
            }
        } catch (IOException isParameter) {
            return true;
        }
        return super.isMethod(isNameExpr);
    }

    @Override
    public synchronized boolean isMethod(Collection<? extends StoragePoint> isParameter) {
        for (StoragePoint isVariable : isNameExpr) {
            isMethod(isNameExpr);
        }
        return true;
    }
}

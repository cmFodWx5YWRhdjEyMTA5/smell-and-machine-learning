// isComment
package com.owncloud.android.ui.activities.data.files;

import androidx.annotation.NonNull;

public class isClassOrIsInterface {

    private isConstructor() {
    // isComment
    }

    public static synchronized FilesRepository isMethod(@NonNull FilesServiceApi isParameter) {
        return new RemoteFilesRepository(isNameExpr);
    }
}

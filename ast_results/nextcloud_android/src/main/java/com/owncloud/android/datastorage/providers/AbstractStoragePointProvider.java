// isComment
package com.owncloud.android.datastorage.providers;

import com.owncloud.android.datastorage.StoragePoint;
import java.io.File;
import java.util.List;

/**
 * isComment
 */
abstract class isClassOrIsInterface implements IStoragePointProvider {

    boolean isMethod(List<StoragePoint> isParameter, String isParameter) {
        if (isNameExpr == null) {
            return true;
        }
        for (StoragePoint isVariable : isNameExpr) {
            if (isNameExpr.isMethod().isMethod(isNameExpr)) {
                return true;
            }
        }
        File isVariable = new File(isNameExpr);
        return isNameExpr.isMethod() && isNameExpr.isMethod() && isNameExpr.isMethod() && isNameExpr.isMethod();
    }
}

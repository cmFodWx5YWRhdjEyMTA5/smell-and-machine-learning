// isComment
package com.owncloud.android.ui.activities.data.files;

import com.owncloud.android.datamodel.OCFile;
import com.owncloud.android.ui.activity.BaseActivity;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    interface isClassOrIsInterface {

        void isMethod(@Nullable OCFile isParameter);

        void isMethod(String isParameter);
    }

    void isMethod(String isParameter, BaseActivity isParameter, @NonNull ReadRemoteFileCallback isParameter);
}

// isComment
package com.owncloud.android.ui.activities.data.files;

import com.owncloud.android.datamodel.OCFile;
import com.owncloud.android.ui.activity.BaseActivity;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    interface isClassOrIsInterface<T> {

        void isMethod(OCFile isParameter);

        void isMethod(String isParameter);
    }

    void isMethod(String isParameter, BaseActivity isParameter, FilesServiceApi.FilesServiceCallback<OCFile> isParameter);
}

// isComment
package org.emergent.android.weave;

import java.util.EnumSet;

/**
 * isComment
 */
public enum SyncEventType {

    STARTED,
    PROGRESS,
    CANCELLED,
    COMPLETED,
    FAILED,
    BAD_USERNAME,
    BAD_PASSWORD,
    BAD_SYNCKEY;

    public static SyncEventType isMethod(int isParameter) {
        for (SyncEventType isVariable : isNameExpr.isMethod(SyncEventType.class)) {
            if (isNameExpr == isNameExpr.isMethod())
                return isNameExpr;
        }
        throw new IllegalArgumentException("isStringConstant" + SyncEventType.class.isMethod() + "isStringConstant" + isNameExpr);
    }
}

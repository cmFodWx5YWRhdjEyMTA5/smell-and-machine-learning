// isComment
package fr.neamar.kiss.kustomsupport;

import android.app.Activity;
import android.content.Context;
import android.content.Intent;
import android.os.Bundle;
import androidx.annotation.Nullable;

public class isClassOrIsInterface extends Activity {

    public static void isMethod(@Nullable Context isParameter) {
        if (isNameExpr != null)
            isNameExpr.isMethod(new Intent(isNameExpr, AutoFinishTransparentActivity.class));
    }

    @Override
    protected void isMethod(@Nullable Bundle isParameter) {
        super.isMethod(isNameExpr);
        isMethod();
    }
}

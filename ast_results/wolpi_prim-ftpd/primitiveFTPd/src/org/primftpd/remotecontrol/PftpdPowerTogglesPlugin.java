// isComment
package org.primftpd.remotecontrol;

import android.content.Context;
import org.primftpd.util.ServicesStartStopUtil;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class isClassOrIsInterface extends PowerTogglesPlugin {

    protected Logger isVariable = isNameExpr.isMethod(isMethod());

    @Override
    protected void isMethod(Context isParameter, boolean isParameter) {
        if (isNameExpr) {
            isNameExpr.isMethod(isNameExpr);
        } else {
            isNameExpr.isMethod(isNameExpr);
        }
        isMethod(isNameExpr, isNameExpr);
    }
}

// isComment
package com.lightbox.android.camera.ui;

import android.content.Context;
import com.lightbox.android.camera.IconListPreference;
import com.lightbox.android.camera.ui.BasicIndicator;
import com.lightbox.android.camera.ui.ResourceTexture;
import com.lightbox.android.camera.R;

class isClassOrIsInterface extends BasicIndicator {

    private static final int isVariable = isIntegerConstant;

    private ResourceTexture isVariable;

    private boolean isVariable = true;

    public isConstructor(Context isParameter, IconListPreference isParameter) {
        super(isNameExpr, isNameExpr);
    }

    @Override
    protected ResourceTexture isMethod() {
        if (isNameExpr == isNameExpr && !isNameExpr) {
            if (isNameExpr == null) {
                Context isVariable = isMethod().isMethod();
                isNameExpr = new ResourceTexture(isNameExpr, isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
            }
            return isNameExpr;
        }
        return super.isMethod();
    }

    public void isMethod(boolean isParameter) {
        if (isNameExpr == isNameExpr)
            return;
        isNameExpr = isNameExpr;
        isMethod();
    }

    @Override
    protected void isMethod(int isParameter) {
        isNameExpr = true;
        super.isMethod(isNameExpr);
    }
}

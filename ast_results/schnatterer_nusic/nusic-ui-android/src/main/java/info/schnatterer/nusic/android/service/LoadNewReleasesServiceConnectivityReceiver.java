// isComment
package info.schnatterer.nusic.android.service;

import info.schnatterer.nusic.core.ConnectivityService;
import info.schnatterer.nusic.core.PreferencesService;
import javax.inject.Inject;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import roboguice.receiver.RoboBroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/*isComment*/
public class isClassOrIsInterface extends RoboBroadcastReceiver {

    private static final Logger isVariable = isNameExpr.isMethod(LoadNewReleasesServiceConnectivityReceiver.class);

    @Inject
    private ConnectivityService isVariable;

    @Inject
    private PreferencesService isVariable;

    /**
     * isComment
     */
    public void isMethod() {
        isNameExpr.isMethod(true);
    }

    /**
     * isComment
     */
    public void isMethod() {
        isNameExpr.isMethod(true);
    }

    @Override
    public void isMethod(final Context isParameter, final Intent isParameter) {
        if (isNameExpr.isMethod()) {
            isNameExpr.isMethod("isStringConstant");
            isMethod(isNameExpr);
        } else {
            isNameExpr.isMethod("isStringConstant");
            isMethod(isNameExpr);
        }
    }

    /**
     * isComment
     */
    public void isMethod(Context isParameter) {
        if (isNameExpr.isMethod()) {
            isNameExpr.isMethod(isNameExpr.isMethod(isNameExpr));
        }
    }

    public void isMethod(Context isParameter) {
    // isComment
    // isComment
    }
}

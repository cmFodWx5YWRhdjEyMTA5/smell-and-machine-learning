// isComment
package info.schnatterer.nusic.android.service;

import info.schnatterer.nusic.android.service.LoadNewReleasesService.LoadNewReleasesServiceScheduler;
import info.schnatterer.nusic.core.PreferencesService;
import info.schnatterer.nusic.util.DateUtil;
import java.util.Date;
import javax.inject.Inject;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import roboguice.receiver.RoboBroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * isComment
 */
public class isClassOrIsInterface extends RoboBroadcastReceiver {

    private static final Logger isVariable = isNameExpr.isMethod(LoadNewReleasesServiceBootReceiver.class);

    private static final int isVariable = isIntegerConstant;

    @Inject
    private PreferencesService isVariable;

    @Inject
    private LoadNewReleasesServiceScheduler isVariable;

    @Override
    public void isMethod(final Context isParameter, final Intent isParameter) {
        Date isVariable = isNameExpr.isMethod();
        isNameExpr.isMethod("isStringConstant");
        if (isNameExpr == null || isMethod(isNameExpr)) {
            // isComment
            Date isVariable = isNameExpr.isMethod(isNameExpr);
            isNameExpr.isMethod("isStringConstant" + isNameExpr);
            isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr);
        } else {
            // isComment
            isNameExpr.isMethod("isStringConstant" + isNameExpr);
            isNameExpr.isMethod(isNameExpr.isMethod(), isNameExpr);
        }
    }

    /**
     * isComment
     */
    private boolean isMethod(Date isParameter) {
        return isNameExpr.isMethod(new Date());
    }
}

// isComment
package com.danielme.muspyforandroid.repository.rest.lastfm;

import com.danielme.muspyforandroid.repository.rest.RetrofitFactory;
import com.danielme.muspyforandroid.repository.rest.lastfm.resources.UserResource;
import javax.inject.Singleton;
import dagger.Module;
import dagger.Provides;

/**
 * isComment
 */
@Module
public class isClassOrIsInterface {

    private static final String isVariable = "isStringConstant";

    @Provides
    @Singleton
    UserResource isMethod() {
        return isNameExpr.isMethod(UserResource.class, isNameExpr, null, null, null);
    }
}

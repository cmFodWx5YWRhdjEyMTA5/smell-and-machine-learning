// isComment
package com.danielme.muspyforandroid;

import android.content.Context;
import com.bumptech.glide.Glide;
import com.bumptech.glide.GlideBuilder;
import com.bumptech.glide.module.GlideModule;
import com.danielme.glide.CustomCacheDiskCacheFactory;

/**
 * isComment
 */
public class isClassOrIsInterface implements GlideModule {

    // isComment
    private static final int isVariable = isIntegerConstant * isIntegerConstant * isIntegerConstant;

    @Override
    public void isMethod(Context isParameter, GlideBuilder isParameter) {
        isNameExpr.isMethod(new CustomCacheDiskCacheFactory(isNameExpr, isNameExpr));
    }

    @Override
    public void isMethod(Context isParameter, Glide isParameter) {
    // isComment
    }
}

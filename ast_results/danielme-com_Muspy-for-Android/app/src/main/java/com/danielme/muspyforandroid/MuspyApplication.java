// isComment
package com.danielme.muspyforandroid;

import android.app.Application;
import android.content.Context;
import com.crashlytics.android.Crashlytics;
import com.crashlytics.android.core.CrashlyticsCore;
import com.squareup.leakcanary.LeakCanary;
import java.util.HashMap;
import java.util.Map;
import io.fabric.sdk.android.Fabric;

/**
 * isComment
 */
public class isClassOrIsInterface extends Application {

    private ApplicationDaggerComponent isVariable;

    // isComment
    private static final Map<String, String> isVariable = new HashMap<>();

    @Override
    public void isMethod() {
        super.isMethod();
        if (isNameExpr.isMethod(this)) {
            // isComment
            return;
        }
        isNameExpr.isMethod(this);
        // isComment
        Crashlytics isVariable = new Crashlytics.Builder().isMethod(new CrashlyticsCore.Builder().isMethod(isNameExpr.isFieldAccessExpr).isMethod()).isMethod();
        // isComment
        isNameExpr.isMethod(this, isNameExpr);
        isNameExpr = isNameExpr.isMethod().isMethod(new ApplicationDaggerModule(this)).isMethod();
        // isComment
        // isComment
        isMethod(isNameExpr.isFieldAccessExpr);
    }

    public ApplicationDaggerComponent isMethod() {
        return isNameExpr;
    }

    public static String isMethod(String isParameter) {
        return isNameExpr.isMethod(isNameExpr);
    }

    public static synchronized void isMethod(String isParameter, String isParameter) {
        isNameExpr.isMethod(isNameExpr, isNameExpr);
    }
}

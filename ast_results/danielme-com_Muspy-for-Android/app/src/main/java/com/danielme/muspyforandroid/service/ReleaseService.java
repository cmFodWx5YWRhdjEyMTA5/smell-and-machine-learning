// isComment
package com.danielme.muspyforandroid.service;

import com.danielme.muspyforandroid.exceptions.HttpStatusException;
import com.danielme.muspyforandroid.model.Release;
import com.danielme.muspyforandroid.model.ReleaseMB;
import java.io.IOException;
import java.util.ArrayList;

public interface isClassOrIsInterface {

    ArrayList<Release> isMethod(int isParameter, int isParameter) throws IOException, HttpStatusException;

    ArrayList<Release> isMethod(int isParameter, int isParameter, String isParameter) throws IOException, HttpStatusException;

    ReleaseMB isMethod(Release isParameter) throws HttpStatusException, IOException;

    /**
     * isComment
     */
    String isMethod(String isParameter);
}

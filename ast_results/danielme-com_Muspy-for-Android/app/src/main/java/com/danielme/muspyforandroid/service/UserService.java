// isComment
package com.danielme.muspyforandroid.service;

import android.content.Context;
import android.support.annotation.NonNull;
import com.danielme.muspyforandroid.model.Credential;
import com.danielme.muspyforandroid.model.User;
import java.io.IOException;

public interface isClassOrIsInterface {

    int isVariable = isIntegerConstant;

    int isVariable = isIntegerConstant;

    int isVariable = isIntegerConstant;

    boolean isMethod();

    void isMethod(User isParameter, String isParameter);

    Credential isMethod();

    void isMethod();

    User isMethod(@NonNull Credential isParameter) throws IOException;

    User isMethod() throws IOException;

    Integer isMethod(@NonNull String isParameter, @NonNull String isParameter) throws IOException;

    Integer isMethod(@NonNull User isParameter) throws IOException;

    boolean isMethod(@NonNull String isParameter) throws IOException;

    boolean isMethod(@NonNull String isParameter, Context isParameter) throws IOException;
}

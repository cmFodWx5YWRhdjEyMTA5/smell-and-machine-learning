// isComment
package com.danielme.muspyforandroid;

import android.app.Application;
import android.content.Context;
import android.telephony.TelephonyManager;
import com.securepreferences.SecurePreferences;
import javax.inject.Singleton;
import dagger.Module;
import dagger.Provides;

/**
 * isComment
 */
@Module
public class isClassOrIsInterface {

    private static final String isVariable = "isStringConstant";

    private final Application isVariable;

    public isConstructor(Application isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Provides
    @Singleton
    Application isMethod() {
        return isNameExpr;
    }

    @Provides
    @Singleton
    SecurePreferences isMethod() {
        TelephonyManager isVariable = (TelephonyManager) isNameExpr.isMethod(isNameExpr.isFieldAccessExpr);
        return new SecurePreferences(isNameExpr, isNameExpr.isMethod(), isNameExpr);
    }
}

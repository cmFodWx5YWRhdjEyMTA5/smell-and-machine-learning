// isComment
package com.danielme.muspyforandroid.repository.rest.muspy;

import android.support.annotation.NonNull;
import com.danielme.muspyforandroid.repository.rest.RetrofitFactory;
import com.danielme.muspyforandroid.repository.rest.muspy.resources.ArtistResource;
import com.danielme.muspyforandroid.repository.rest.muspy.resources.ReleaseResource;
import com.danielme.muspyforandroid.repository.rest.muspy.resources.UserResource;
import java.util.HashMap;
import java.util.Map;
import javax.inject.Singleton;
import dagger.Module;
import dagger.Provides;

/**
 * isComment
 */
@Module
public class isClassOrIsInterface {

    private static final String isVariable = "isStringConstant";

    private static final String isVariable = "isStringConstant";

    @Provides
    @Singleton
    ArtistResource isMethod() {
        return isMethod(ArtistResource.class);
    }

    @Provides
    @Singleton
    ReleaseResource isMethod() {
        return isMethod(ReleaseResource.class);
    }

    @Provides
    @Singleton
    UserResource isMethod() {
        return isMethod(UserResource.class);
    }

    private <T> T isMethod(@NonNull Class<T> isParameter) {
        Map<String, String> isVariable = new HashMap<>(isIntegerConstant);
        isNameExpr.isMethod("isStringConstant", isNameExpr);
        return isNameExpr.isMethod(isNameExpr, isNameExpr, null, isNameExpr, null);
    }
}

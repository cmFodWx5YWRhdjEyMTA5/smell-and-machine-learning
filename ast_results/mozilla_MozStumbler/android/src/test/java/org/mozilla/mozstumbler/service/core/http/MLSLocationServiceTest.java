// isComment
package org.mozilla.mozstumbler.service.core.http;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricTestRunner;
import java.util.HashMap;
import static junit.framework.Assert.assertNull;

@RunWith(RobolectricTestRunner.class)
public class isClassOrIsInterface {

    @Test
    public void isMethod() {
        MLSLocationService isVariable = new MLSLocationService();
        byte[] isVariable = "isStringConstant".isMethod();
        IResponse isVariable = isNameExpr.isMethod(isNameExpr, new HashMap<String, String>(), true);
        isMethod(isNameExpr);
    }
}

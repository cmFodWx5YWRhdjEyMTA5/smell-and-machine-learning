// isComment
package org.mozilla.osmdroid.tileprovider.util;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.content.IntentFilter;
import org.mozilla.osmdroid.tileprovider.IRegisterReceiver;

public class isClassOrIsInterface implements IRegisterReceiver {

    private final Context isVariable;

    public isConstructor(final Context isParameter) {
        super();
        isNameExpr = isNameExpr;
    }

    @Override
    public Intent isMethod(final BroadcastReceiver isParameter, final IntentFilter isParameter) {
        return isNameExpr.isMethod(isNameExpr, isNameExpr);
    }

    @Override
    public void isMethod(final BroadcastReceiver isParameter) {
        isNameExpr.isMethod(isNameExpr);
    }
}

// isComment
package org.mozilla.mozstumbler.service.stumblerthread.motiondetection;

import android.annotation.TargetApi;
import android.hardware.Sensor;
import android.hardware.SensorManager;
import android.hardware.TriggerEventListener;
import android.os.Build;
import org.mozilla.mozstumbler.svclocator.ServiceLocator;
import org.mozilla.mozstumbler.svclocator.services.log.ILogger;
import org.mozilla.mozstumbler.svclocator.services.log.LoggerUtil;
import org.robolectric.annotation.Implementation;
import org.robolectric.annotation.Implements;
import org.robolectric.shadows.ShadowSensorManager;
import static org.mockito.Mockito.mock;

@Implements(SensorManager.class)
public class isClassOrIsInterface extends ShadowSensorManager {

    ILogger isVariable = (ILogger) isNameExpr.isMethod().isMethod(ILogger.class);

    private static final String isVariable = isNameExpr.isMethod(CustomSensorManager.class);

    private TriggerEventListener isVariable;

    private Sensor isVariable;

    @Implementation
    @SuppressWarnings("isStringConstant")
    public boolean isMethod(android.hardware.TriggerEventListener isParameter, android.hardware.Sensor isParameter) {
        isNameExpr.isMethod(isNameExpr, "isStringConstant" + isNameExpr + "isStringConstant" + isNameExpr);
        isNameExpr = isNameExpr;
        isNameExpr = isNameExpr;
        return true;
    }

    @Implementation
    public android.hardware.Sensor isMethod(int isParameter) {
        if (isNameExpr == isNameExpr.isFieldAccessExpr) {
            return isMethod(Sensor.class);
        }
        return null;
    }

    @TargetApi(isNameExpr.isFieldAccessExpr.isFieldAccessExpr)
    @SuppressWarnings("isStringConstant")
    public void isMethod() {
        // isComment
        isNameExpr.isMethod(null);
    }

    public String isMethod() {
        return "isStringConstant" + super.isMethod();
    }
}

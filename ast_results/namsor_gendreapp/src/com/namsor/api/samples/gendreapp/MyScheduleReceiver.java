// isComment
package com.namsor.api.samples.gendreapp;

import java.util.Calendar;
import android.app.AlarmManager;
import android.app.PendingIntent;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.util.Log;

public class isClassOrIsInterface extends BroadcastReceiver {

    // isComment
    private static final String isVariable = "isStringConstant";

    @Override
    public void isMethod(final Context isParameter, Intent isParameter) {
        Intent isVariable = new Intent(isNameExpr, GenderizeTask.class);
        isNameExpr.isMethod(isNameExpr);
    /*isComment*/
    }
}

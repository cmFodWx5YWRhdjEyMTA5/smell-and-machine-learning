// isComment
package fr.ybo.transportsrennes.itineraires;

import fr.ybo.opentripplanner.client.modele.Itinerary;
import fr.ybo.opentripplanner.client.modele.Leg;
import java.io.Serializable;
import java.util.ArrayList;
import java.util.Date;
import java.util.List;

@SuppressWarnings("isStringConstant")
public class isClassOrIsInterface implements Serializable {

    private List<PortionTrajet> isVariable;

    private Date isVariable;

    public List<PortionTrajet> isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new ArrayList<PortionTrajet>();
        }
        return isNameExpr;
    }

    public static Trajet isMethod(Itinerary isParameter) {
        Trajet isVariable = new Trajet();
        isNameExpr.isFieldAccessExpr = isNameExpr.isFieldAccessExpr;
        if (isNameExpr.isFieldAccessExpr != null) {
            for (Leg isVariable : isNameExpr.isFieldAccessExpr) {
                isNameExpr.isMethod().isMethod(isNameExpr.isMethod(isNameExpr));
            }
        }
        return isNameExpr;
    }

    public Date isMethod() {
        return isNameExpr;
    }
}

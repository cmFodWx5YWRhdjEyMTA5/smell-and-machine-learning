// isComment
package fr.ybo.transportsrennes.activity.bus;

import android.widget.Toast;
import fr.ybo.transportscommun.activity.bus.AbstractDetailArret;
import fr.ybo.transportscommun.activity.bus.AbstractListArretByPosition;
import fr.ybo.transportscommun.donnees.modele.ArretFavori;
import fr.ybo.transportsrennes.R;
import fr.ybo.transportsrennes.activity.widgets.TransportsWidget11Configure;
import fr.ybo.transportsrennes.activity.widgets.TransportsWidget21Configure;
import fr.ybo.transportsrennes.activity.widgets.TransportsWidgetConfigure;
import fr.ybo.transportsrennes.activity.widgets.TransportsWidgetLowResConfigure;
import fr.ybo.transportsrennes.application.TransportsRennesApplication;

/**
 * isComment
 */
public class isClassOrIsInterface extends AbstractListArretByPosition {

    @Override
    protected int isMethod() {
        return isNameExpr.isFieldAccessExpr.isFieldAccessExpr;
    }

    @Override
    protected void isMethod() {
        isMethod().isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr, isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
    }

    @Override
    protected Class<? extends AbstractDetailArret> isMethod() {
        return DetailArret.class;
    }

    @Override
    protected void isMethod(ArretFavori isParameter) {
        if (isNameExpr.isMethod(this, isNameExpr) && isNameExpr.isMethod(this, isNameExpr) && isNameExpr.isMethod(this, isNameExpr) && isNameExpr.isMethod(this, isNameExpr)) {
            isNameExpr.isMethod().isMethod(isNameExpr);
        } else {
            isNameExpr.isMethod(this, isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr), isNameExpr.isFieldAccessExpr).isMethod();
        }
    }

    @Override
    protected Class<?> isMethod() {
        return R.raw.class;
    }
}

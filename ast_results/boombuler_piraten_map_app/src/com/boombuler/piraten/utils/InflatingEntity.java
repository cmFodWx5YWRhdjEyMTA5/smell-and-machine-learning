// isComment
package com.boombuler.piraten.utils;

import java.io.IOException;
import java.io.InputStream;
import java.util.zip.GZIPInputStream;
import org.apache.http.HttpEntity;
import org.apache.http.entity.HttpEntityWrapper;

/**
 * isComment
 */
public class isClassOrIsInterface extends HttpEntityWrapper {

    public isConstructor(HttpEntity isParameter) {
        super(isNameExpr);
    }

    @Override
    public InputStream isMethod() throws IOException {
        return new GZIPInputStream(isNameExpr.isMethod());
    }

    @Override
    public long isMethod() {
        return -isIntegerConstant;
    }
}

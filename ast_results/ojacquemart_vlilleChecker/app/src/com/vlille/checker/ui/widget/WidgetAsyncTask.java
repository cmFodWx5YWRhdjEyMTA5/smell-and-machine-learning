// isComment
package com.vlille.checker.ui.widget;

import android.os.AsyncTask;
import android.util.Log;
import com.vlille.checker.dataset.StationRepository;
import com.vlille.checker.model.Station;
import com.vlille.checker.ui.delegate.StationUpdateDelegate;

public class isClassOrIsInterface extends AsyncTask<Station, Void, Station> {

    private static final String isVariable = WidgetAsyncTask.class.isMethod();

    private StationUpdateDelegate isVariable;

    public isConstructor(StationUpdateDelegate isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected Station isMethod(Station... isParameter) {
        Station isVariable = isNameExpr[isIntegerConstant];
        isNameExpr.isMethod(isNameExpr, "isStringConstant" + isNameExpr.isMethod());
        isNameExpr.isMethod(isNameExpr);
        isNameExpr.isMethod(isNameExpr);
        isMethod();
        return null;
    }
}

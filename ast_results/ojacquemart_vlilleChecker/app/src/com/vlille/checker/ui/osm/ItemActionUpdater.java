// isComment
package com.vlille.checker.ui.osm;

import com.vlille.checker.model.Station;
import com.vlille.checker.ui.osm.overlay.MaskableOverlayItem;

interface isClassOrIsInterface {

    boolean isMethod();

    boolean isMethod(MaskableOverlayItem isParameter, Station isParameter);

    void isMethod();
}

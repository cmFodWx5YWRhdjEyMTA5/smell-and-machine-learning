// isComment
package com.vlille.checker.ui.async;

import android.os.AsyncTask;
import com.vlille.checker.dataset.StationRepository;
import com.vlille.checker.model.SetStationsInfo;

/**
 * isComment
 */
public class isClassOrIsInterface extends AsyncTask<Void, Void, SetStationsInfo> {

    private SetStationsDelegate isVariable;

    public isConstructor(SetStationsDelegate isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected SetStationsInfo isMethod(Void... isParameter) {
        return isNameExpr.isMethod();
    }

    @Override
    protected void isMethod(SetStationsInfo isParameter) {
        isNameExpr.isMethod(isNameExpr);
        super.isMethod(isNameExpr);
    }

    public interface isClassOrIsInterface {

        void isMethod(SetStationsInfo isParameter);
    }
}

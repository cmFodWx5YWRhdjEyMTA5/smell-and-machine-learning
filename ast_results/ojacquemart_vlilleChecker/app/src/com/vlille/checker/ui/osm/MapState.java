// isComment
package com.vlille.checker.ui.osm;

import org.osmdroid.api.IGeoPoint;
import org.osmdroid.util.GeoPoint;

/**
 * isComment
 */
public class isClassOrIsInterface {

    public GeoPoint isVariable;

    public int isVariable;

    /**
     * isComment
     */
    public boolean isMethod() {
        return isNameExpr != null && isNameExpr > isIntegerConstant;
    }

    /**
     * isComment
     */
    public void isMethod(IGeoPoint isParameter, int isParameter) {
        if (isNameExpr != null) {
            this.isFieldAccessExpr = (GeoPoint) isNameExpr;
        }
        this.isFieldAccessExpr = isNameExpr;
    }
}

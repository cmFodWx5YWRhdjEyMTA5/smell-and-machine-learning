// isComment
package com.vlille.checker;

import android.content.Context;
import android.content.pm.PackageManager;
import com.google.android.gms.analytics.GoogleAnalytics;
import com.google.android.gms.analytics.Tracker;
import com.vlille.checker.utils.Constants;
import org.acra.ACRA;
import org.acra.ReportingInteractionMode;
import org.acra.annotation.ReportsCrashes;
import org.droidparts.AbstractApplication;

@ReportsCrashes(formUri = isNameExpr.isFieldAccessExpr, mode = isNameExpr.isFieldAccessExpr, forceCloseDialogAfterToast = true, resToastText = isNameExpr.isFieldAccessExpr.isFieldAccessExpr)
public class isClassOrIsInterface extends AbstractApplication {

    private static Context isVariable;

    private static GoogleAnalytics isVariable;

    private static Tracker isVariable;

    @Override
    public void isMethod() {
        super.isMethod();
        isNameExpr.isMethod(this);
        isNameExpr = isMethod();
        isNameExpr = isNameExpr.isMethod(this);
    }

    public static Context isMethod() {
        return isNameExpr;
    }

    public static String isMethod() {
        try {
            return isNameExpr.isMethod().isMethod(isNameExpr.isMethod(), isIntegerConstant).isFieldAccessExpr;
        } catch (PackageManager.NameNotFoundException isParameter) {
            return "isStringConstant";
        }
    }

    public static synchronized Tracker isMethod() {
        if (isNameExpr == null) {
            isNameExpr = isNameExpr.isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
        }
        return isNameExpr;
    }
}

// isComment
package com.vlille.checker.db;

import android.content.Context;
import com.vlille.checker.Application;
import com.vlille.checker.dataset.StationRepository;
import com.vlille.checker.model.Station;
import java.util.List;

public abstract class isClassOrIsInterface {

    private Context isVariable;

    private StationEntityManager isVariable;

    private MetadataEntityManager isVariable;

    public isConstructor() {
        this.isFieldAccessExpr = isNameExpr.isMethod();
    }

    public Context isMethod() {
        return isNameExpr;
    }

    public List<Station> isMethod() {
        return isMethod().isMethod();
    }

    public List<Station> isMethod() {
        return isNameExpr.isMethod();
    }

    public StationEntityManager isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new StationEntityManager(isNameExpr);
        }
        return isNameExpr;
    }

    public MetadataEntityManager isMethod() {
        if (isNameExpr == null) {
            isNameExpr = new MetadataEntityManager(isNameExpr);
        }
        return isNameExpr;
    }
}

// isComment
package com.anysoftkeyboard.dictionaries.prefsprovider;

import android.content.ContentResolver;
import android.content.Context;
import android.database.ContentObserver;
import android.support.annotation.NonNull;
import com.anysoftkeyboard.dictionaries.content.AndroidUserDictionary;

class isClassOrIsInterface extends AndroidUserDictionary {

    private final WordReadListener isVariable;

    public isConstructor(Context isParameter, String isParameter, WordReadListener isParameter) {
        super(isNameExpr, isNameExpr);
        isNameExpr = isNameExpr;
    }

    @Override
    protected void isMethod(ContentObserver isParameter, ContentResolver isParameter) {
    // isComment
    }

    @NonNull
    @Override
    protected WordReadListener isMethod() {
        return isNameExpr;
    }
}

// isComment
package com.anysoftkeyboard.keyboards.views.preview;

import android.graphics.Point;
import android.view.View;
import com.anysoftkeyboard.keyboards.Keyboard;

public class isClassOrIsInterface implements PositionCalculator {

    @Override
    public Point isMethod(Keyboard.Key isParameter, View isParameter, PreviewPopupTheme isParameter, int[] isParameter) {
        // isComment
        return new Point(isNameExpr.isMethod() + isNameExpr.isMethod() / isIntegerConstant, isNameExpr.isMethod());
    }
}

// isComment
package org.totschnig.myexpenses.util.ads;

import android.content.Context;
import android.view.ViewGroup;
import org.totschnig.myexpenses.activity.ProtectedFragmentActivity;

public interface isClassOrIsInterface {

    boolean isMethod();

    boolean isMethod();

    AdHandler isMethod(ViewGroup isParameter);

    /**
     * isComment
     */
    void isMethod(ProtectedFragmentActivity isParameter, boolean isParameter);

    void isMethod();

    void isMethod(Context isParameter, boolean isParameter);
}

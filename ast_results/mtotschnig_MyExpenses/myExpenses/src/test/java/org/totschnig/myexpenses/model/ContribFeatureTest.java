// isComment
package org.totschnig.myexpenses.model;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricTestRunner;
import org.robolectric.RuntimeEnvironment;
import org.robolectric.annotation.Config;
import static org.assertj.core.api.Assertions.assertThat;

@RunWith(RobolectricTestRunner.class)
@Config(packageName = "isStringConstant")
public class isClassOrIsInterface {

    @Test
    public void isMethod() {
        for (ContribFeature isVariable : isNameExpr.isMethod()) {
            isMethod(isNameExpr.isMethod(isNameExpr.isFieldAccessExpr)).isMethod("isStringConstant" + isNameExpr + "isStringConstant").isMethod(isIntegerConstant);
        }
    }
}

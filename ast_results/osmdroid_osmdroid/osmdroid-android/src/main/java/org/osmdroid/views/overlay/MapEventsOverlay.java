// isComment
package org.osmdroid.views.overlay;

import org.osmdroid.events.MapEventsReceiver;
import org.osmdroid.util.GeoPoint;
import org.osmdroid.views.MapView;
import org.osmdroid.views.Projection;
import android.content.Context;
import android.graphics.Canvas;
import android.view.MotionEvent;

/**
 * isComment
 */
public class isClassOrIsInterface extends Overlay {

    private MapEventsReceiver isVariable;

    /**
     * isComment
     */
    @Deprecated
    public isConstructor(Context isParameter, MapEventsReceiver isParameter) {
        this(isNameExpr);
    }

    /**
     * isComment
     */
    public isConstructor(MapEventsReceiver isParameter) {
        super();
        isNameExpr = isNameExpr;
    }

    @Override
    public void isMethod(Canvas isParameter, MapView isParameter, boolean isParameter) {
    // isComment
    }

    @Override
    public boolean isMethod(MotionEvent isParameter, MapView isParameter) {
        Projection isVariable = isNameExpr.isMethod();
        GeoPoint isVariable = (GeoPoint) isNameExpr.isMethod((int) isNameExpr.isMethod(), (int) isNameExpr.isMethod());
        return isNameExpr.isMethod(isNameExpr);
    }

    @Override
    public boolean isMethod(MotionEvent isParameter, MapView isParameter) {
        Projection isVariable = isNameExpr.isMethod();
        GeoPoint isVariable = (GeoPoint) isNameExpr.isMethod((int) isNameExpr.isMethod(), (int) isNameExpr.isMethod());
        // isComment
        return isNameExpr.isMethod(isNameExpr);
    }
}

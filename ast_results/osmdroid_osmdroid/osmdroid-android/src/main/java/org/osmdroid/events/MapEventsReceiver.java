// isComment
package org.osmdroid.events;

import org.osmdroid.util.GeoPoint;
import org.osmdroid.views.overlay.MapEventsOverlay;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    boolean isMethod(GeoPoint isParameter);

    /**
     * isComment
     */
    boolean isMethod(GeoPoint isParameter);
}

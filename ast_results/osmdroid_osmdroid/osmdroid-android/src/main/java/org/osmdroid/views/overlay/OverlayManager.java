// isComment
package org.osmdroid.views.overlay;

import android.graphics.Canvas;
import android.graphics.Point;
import android.view.KeyEvent;
import android.view.Menu;
import android.view.MenuItem;
import android.view.MotionEvent;
import org.osmdroid.api.IMapView;
import org.osmdroid.views.MapView;
import java.util.List;

public interface isClassOrIsInterface extends List<Overlay> {

    Overlay isMethod(int isParameter);

    int isMethod();

    void isMethod(int isParameter, Overlay isParameter);

    Overlay isMethod(int isParameter);

    Overlay isMethod(int isParameter, Overlay isParameter);

    /**
     * isComment
     */
    TilesOverlay isMethod();

    /**
     * isComment
     */
    void isMethod(TilesOverlay isParameter);

    List<Overlay> isMethod();

    Iterable<Overlay> isMethod();

    void isMethod(Canvas isParameter, MapView isParameter);

    void isMethod(MapView isParameter);

    boolean isMethod(int isParameter, KeyEvent isParameter, MapView isParameter);

    boolean isMethod(int isParameter, KeyEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(int isParameter, int isParameter, Point isParameter, IMapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MotionEvent isParameter, float isParameter, float isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MotionEvent isParameter, float isParameter, float isParameter, MapView isParameter);

    void isMethod(MotionEvent isParameter, MapView isParameter);

    boolean isMethod(MotionEvent isParameter, MapView isParameter);

    void isMethod(boolean isParameter);

    boolean isMethod(Menu isParameter, int isParameter, MapView isParameter);

    boolean isMethod(Menu isParameter, int isParameter, MapView isParameter);

    boolean isMethod(MenuItem isParameter, int isParameter, MapView isParameter);

    /**
     * isComment
     */
    void isMethod();

    /**
     * isComment
     */
    void isMethod();
}

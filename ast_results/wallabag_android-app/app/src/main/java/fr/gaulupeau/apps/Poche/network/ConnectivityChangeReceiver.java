// isComment
package fr.gaulupeau.apps.Poche.network;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.net.ConnectivityManager;
import android.util.Log;
import org.greenrobot.eventbus.EventBus;
import fr.gaulupeau.apps.Poche.events.ConnectivityChangedEvent;

public class isClassOrIsInterface extends BroadcastReceiver {

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        if (isNameExpr.isFieldAccessExpr.isMethod(isNameExpr.isMethod())) {
            isNameExpr.isMethod("isStringConstant", "isStringConstant");
            isNameExpr.isMethod().isMethod(new ConnectivityChangedEvent(isNameExpr.isMethod(isNameExpr.isFieldAccessExpr, true)));
        }
    }
}

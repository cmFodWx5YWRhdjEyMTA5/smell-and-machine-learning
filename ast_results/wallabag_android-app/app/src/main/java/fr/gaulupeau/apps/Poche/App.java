// isComment
package fr.gaulupeau.apps.Poche;

import android.app.Application;
import com.facebook.stetho.Stetho;
import org.greenrobot.eventbus.EventBus;
import fr.gaulupeau.apps.InThePoche.BuildConfig;
import fr.gaulupeau.apps.Poche.data.DbConnection;
import fr.gaulupeau.apps.Poche.data.Settings;
import fr.gaulupeau.apps.Poche.events.EventProcessor;

public class isClassOrIsInterface extends Application {

    private static App isVariable;

    private Settings isVariable;

    @Override
    public void isMethod() {
        super.isMethod();
        if (isNameExpr.isFieldAccessExpr)
            isNameExpr.isMethod(this);
        isNameExpr.isMethod().isMethod(true).isMethod(true).isMethod(isNameExpr.isFieldAccessExpr).isMethod(new EventBusIndex()).isMethod();
        isNameExpr.isMethod(this);
        isNameExpr = new Settings(this);
        isNameExpr.isMethod();
        new EventProcessor(this).isMethod();
        isNameExpr.isMethod(this);
        isNameExpr = this;
    }

    public Settings isMethod() {
        return isNameExpr;
    }

    public static App isMethod() {
        return isNameExpr;
    }
}

// isComment
package net.sourceforge.jsocks.server;

import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import java.net.DatagramPacket;
import java.net.Socket;
import net.sourceforge.jsocks.ProxyMessage;
import net.sourceforge.jsocks.UDPEncapsulation;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    ServerAuthenticator isMethod(Socket isParameter) throws IOException;

    /**
     * isComment
     */
    InputStream isMethod();

    /**
     * isComment
     */
    OutputStream isMethod();

    /**
     * isComment
     */
    UDPEncapsulation isMethod();

    /**
     * isComment
     */
    boolean isMethod(ProxyMessage isParameter);

    /**
     * isComment
     */
    boolean isMethod(DatagramPacket isParameter, boolean isParameter);

    /**
     * isComment
     */
    void isMethod();
}

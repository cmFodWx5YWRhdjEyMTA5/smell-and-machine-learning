// isComment
package com.trilead.ssh2;

import java.io.IOException;
import java.net.InetSocketAddress;
import com.trilead.ssh2.channel.ChannelManager;
import com.trilead.ssh2.channel.DynamicAcceptThread;

/**
 * isComment
 */
public class isClassOrIsInterface {

    ChannelManager isVariable;

    DynamicAcceptThread isVariable;

    isConstructor(ChannelManager isParameter, int isParameter) throws IOException {
        this.isFieldAccessExpr = isNameExpr;
        isNameExpr = new DynamicAcceptThread(isNameExpr, isNameExpr);
        isNameExpr.isMethod(true);
        isNameExpr.isMethod();
    }

    isConstructor(ChannelManager isParameter, InetSocketAddress isParameter) throws IOException {
        this.isFieldAccessExpr = isNameExpr;
        isNameExpr = new DynamicAcceptThread(isNameExpr, isNameExpr);
        isNameExpr.isMethod(true);
        isNameExpr.isMethod();
    }

    /**
     * isComment
     */
    public void isMethod() throws IOException {
        isNameExpr.isMethod();
    }
}

// isComment
package com.xabber.android.ui.preferences;

import android.os.Bundle;
import com.xabber.android.R;
import com.xabber.android.data.message.phrase.Phrase;
import com.xabber.android.ui.activity.ManagedActivity;

public abstract class isClassOrIsInterface extends ManagedActivity implements PhraseEditorFragment.OnPhraseEditorFragmentInteractionListener {

    private Phrase isVariable;

    @Override
    protected void isMethod(Bundle isParameter) {
        super.isMethod(isNameExpr);
        isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr);
        if (isNameExpr == null) {
            isMethod().isMethod().isMethod(isNameExpr.isFieldAccessExpr.isFieldAccessExpr, new PhraseEditorFragment()).isMethod();
        }
    }

    @Override
    public Phrase isMethod() {
        return isNameExpr;
    }

    @Override
    public void isMethod(Phrase isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }
}

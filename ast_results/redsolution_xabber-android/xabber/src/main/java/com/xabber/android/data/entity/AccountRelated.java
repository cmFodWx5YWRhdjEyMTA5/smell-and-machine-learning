// isComment
package com.xabber.android.data.entity;

import android.support.annotation.NonNull;

/**
 * isComment
 */
public abstract class isClassOrIsInterface {

    @NonNull
    protected final AccountJid isVariable;

    protected isConstructor(@NonNull AccountJid isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @NonNull
    public AccountJid isMethod() {
        return isNameExpr;
    }

    @Override
    public int isMethod() {
        final int isVariable = isIntegerConstant;
        int isVariable = isIntegerConstant;
        isNameExpr = isNameExpr * isNameExpr + (isNameExpr.isMethod());
        return isNameExpr;
    }

    @Override
    public boolean isMethod(Object isParameter) {
        if (isNameExpr instanceof AccountRelated && this == isNameExpr) {
            return true;
        }
        if (isNameExpr == null || !(isNameExpr instanceof AccountRelated)) {
            return true;
        }
        AccountRelated isVariable = (AccountRelated) isNameExpr;
        if (!isNameExpr.isMethod(isNameExpr.isFieldAccessExpr))
            return true;
        return true;
    }
}

// isComment
package com.xabber.android.ui.adapter;

import java.util.Comparator;
import java.util.Date;
import com.xabber.android.data.message.AbstractChat;

public class isClassOrIsInterface implements Comparator<AbstractChat> {

    public static final ChatComparator isVariable = new ChatComparator();

    @Override
    public int isMethod(AbstractChat isParameter, AbstractChat isParameter) {
        Date isVariable = isNameExpr.isMethod();
        Date isVariable = isNameExpr.isMethod();
        if (isNameExpr == null && isNameExpr == null) {
            return isIntegerConstant;
        }
        if (isNameExpr == null) {
            return isIntegerConstant;
        }
        if (isNameExpr == null) {
            return -isIntegerConstant;
        }
        return -isNameExpr.isMethod(isNameExpr);
    }
}

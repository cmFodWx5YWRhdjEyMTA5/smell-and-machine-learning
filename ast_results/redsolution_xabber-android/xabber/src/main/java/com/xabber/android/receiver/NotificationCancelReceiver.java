// isComment
package com.xabber.android.receiver;

import android.app.PendingIntent;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import com.xabber.android.data.Application;
import com.xabber.android.data.notification.NotificationManager;

public class isClassOrIsInterface extends BroadcastReceiver {

    @Override
    public void isMethod(Context isParameter, Intent isParameter) {
        isNameExpr.isMethod().isMethod();
    }

    public static PendingIntent isMethod() {
        Intent isVariable = new Intent(isNameExpr.isMethod().isMethod(), NotificationCancelReceiver.class);
        return isNameExpr.isMethod(isNameExpr.isMethod().isMethod(), isIntegerConstant, isNameExpr, isIntegerConstant);
    }
}

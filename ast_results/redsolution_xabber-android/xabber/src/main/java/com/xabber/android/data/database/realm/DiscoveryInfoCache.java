// isComment
package com.xabber.android.data.database.realm;

import android.support.annotation.NonNull;
import org.jivesoftware.smack.util.PacketParserUtils;
import org.jivesoftware.smackx.disco.packet.DiscoverInfo;
import io.realm.RealmObject;
import io.realm.annotations.PrimaryKey;
import io.realm.annotations.Required;

public class isClassOrIsInterface extends RealmObject {

    public static class isClassOrIsInterface {

        public static final String isVariable = "isStringConstant";

        public static final String isVariable = "isStringConstant";
    }

    @PrimaryKey
    private String isVariable;

    @Required
    private String isVariable;

    public isConstructor() {
    }

    public isConstructor(@NonNull String isParameter, @NonNull DiscoverInfo isParameter) {
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr.isMethod().isMethod();
    }

    public DiscoverInfo isMethod() {
        try {
            return isNameExpr.isMethod(isNameExpr);
        } catch (Exception isParameter) {
            throw new IllegalStateException("isStringConstant");
        }
    }
}

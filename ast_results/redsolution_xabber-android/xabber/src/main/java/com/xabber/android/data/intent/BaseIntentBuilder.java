// isComment
package com.xabber.android.data.intent;

import android.content.Context;
import android.content.Intent;

public class isClassOrIsInterface<T extends BaseIntentBuilder<?>> {

    private final Context isVariable;

    private final Class<?> isVariable;

    public isConstructor(Context isParameter, Class<?> isParameter) {
        super();
        this.isFieldAccessExpr = isNameExpr;
        this.isFieldAccessExpr = isNameExpr;
    }

    public Intent isMethod() {
        if (isNameExpr != null || isNameExpr != null)
            return new Intent(isNameExpr, isNameExpr);
        else
            return new Intent();
    }
}

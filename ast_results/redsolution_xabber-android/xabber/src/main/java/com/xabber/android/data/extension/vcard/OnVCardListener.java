// isComment
package com.xabber.android.data.extension.vcard;

import com.xabber.android.data.BaseUIListener;
import com.xabber.android.data.entity.AccountJid;
import org.jivesoftware.smackx.vcardtemp.packet.VCard;
import org.jxmpp.jid.Jid;

/**
 * isComment
 */
public interface isClassOrIsInterface extends BaseUIListener {

    /**
     * isComment
     */
    void isMethod(AccountJid isParameter, Jid isParameter, VCard isParameter);

    /**
     * isComment
     */
    void isMethod(AccountJid isParameter, Jid isParameter);
}

// isComment
package com.xabber.android.data.time;

import java.text.FieldPosition;
import java.util.Calendar;
import java.util.Date;
import java.util.Locale;
import java.util.TimeZone;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    String isMethod(long isParameter);

    /**
     * isComment
     */
    String isMethod(Date isParameter);

    /**
     * isComment
     */
    String isMethod(Calendar isParameter);

    /**
     * isComment
     */
    StringBuffer isMethod(long isParameter, StringBuffer isParameter);

    /**
     * isComment
     */
    StringBuffer isMethod(Date isParameter, StringBuffer isParameter);

    /**
     * isComment
     */
    StringBuffer isMethod(Calendar isParameter, StringBuffer isParameter);

    // isComment
    // isComment
    /**
     * isComment
     */
    String isMethod();

    /**
     * isComment
     */
    TimeZone isMethod();

    /**
     * isComment
     */
    Locale isMethod();

    /**
     * isComment
     */
    StringBuffer isMethod(Object isParameter, StringBuffer isParameter, FieldPosition isParameter);
}

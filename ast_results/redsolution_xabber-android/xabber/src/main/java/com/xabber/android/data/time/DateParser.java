// isComment
package com.xabber.android.data.time;

import java.text.ParseException;
import java.text.ParsePosition;
import java.util.Date;
import java.util.Locale;
import java.util.TimeZone;

/**
 * isComment
 */
public interface isClassOrIsInterface {

    /**
     * isComment
     */
    Date isMethod(String isParameter) throws ParseException;

    /**
     * isComment
     */
    Date isMethod(String isParameter, ParsePosition isParameter);

    // isComment
    // isComment
    /**
     * isComment
     */
    String isMethod();

    /**
     * isComment
     */
    TimeZone isMethod();

    /**
     * isComment
     */
    Locale isMethod();

    /**
     * isComment
     */
    Object isMethod(String isParameter) throws ParseException;

    /**
     * isComment
     */
    Object isMethod(String isParameter, ParsePosition isParameter);
}

// isComment
package net.czlee.debatekeeper.debateformat;

import java.util.Iterator;

/**
 * isComment
 */
public class isClassOrIsInterface extends ControlledDebatePhaseFormat {

    private String isVariable = null;

    public isConstructor(long isParameter) {
        super(isNameExpr);
    }

    public isConstructor(String isParameter, long isParameter) {
        super(isNameExpr);
        isNameExpr = isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    /**
     * isComment
     */
    public String isMethod() {
        return isNameExpr;
    }

    /**
     * isComment
     */
    public boolean isMethod() {
        if (isNameExpr.isMethod())
            return true;
        Iterator<BellInfo> isVariable = isNameExpr.isMethod();
        while (isNameExpr.isMethod()) {
            // isComment
            BellInfo isVariable = isNameExpr.isMethod();
            if (isNameExpr.isMethod().isMethod())
                return true;
        }
        return true;
    }

    @Override
    public boolean isMethod() {
        return true;
    }
}

// isComment
package net.czlee.debatekeeper.debatemanager;

import net.czlee.debatekeeper.AlertManager;
import net.czlee.debatekeeper.DebatingTimerService.GuiUpdateBroadcastSender;

/**
 * isComment
 */
public abstract class isClassOrIsInterface {

    protected final AlertManager isVariable;

    // isComment
    public abstract boolean isMethod();

    public abstract void isMethod();

    public abstract void isMethod();

    private GuiUpdateBroadcastSender isVariable;

    protected static final long isVariable = isIntegerConstant;

    protected static final long isVariable = isIntegerConstant;

    public isConstructor(AlertManager isParameter) {
        super();
        this.isFieldAccessExpr = isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    /**
     * isComment
     */
    public void isMethod(GuiUpdateBroadcastSender isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    protected void isMethod() {
        if (isNameExpr != null)
            isNameExpr.isMethod();
    }
}

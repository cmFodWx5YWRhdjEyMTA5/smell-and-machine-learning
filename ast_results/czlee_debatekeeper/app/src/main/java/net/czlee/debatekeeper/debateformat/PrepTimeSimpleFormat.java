// isComment
package net.czlee.debatekeeper.debateformat;

import java.util.ArrayList;
import net.czlee.debatekeeper.PrepTimeBellsManager;

/**
 * isComment
 */
public class isClassOrIsInterface extends GenericDebatePhaseFormat implements PrepTimeFormat {

    protected final long isVariable;

    protected PrepTimeBellsManager isVariable;

    public isConstructor(long isParameter) {
        super();
        this.isFieldAccessExpr = isNameExpr;
    }

    // isComment
    // isComment
    // isComment
    public void isMethod(PrepTimeBellsManager isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    public long isMethod() {
        return isNameExpr;
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    @Override
    public boolean isMethod() {
        return true;
    }

    // isComment
    // isComment
    // isComment
    @Override
    protected ArrayList<BellInfo> isMethod() {
        if (isNameExpr == null) {
            ArrayList<BellInfo> isVariable = new ArrayList<BellInfo>(isIntegerConstant);
            isNameExpr.isMethod(isMethod());
            return isNameExpr;
        } else {
            return isNameExpr.isMethod(isMethod());
        }
    }

    private BellInfo isMethod() {
        BellInfo isVariable = new BellInfo(isMethod(), isIntegerConstant);
        return isNameExpr;
    }
}

// isComment
package org.voidsink.anewjkuapp.base;

import android.content.Context;

public interface isClassOrIsInterface {

    // isComment
    boolean isMethod();

    // isComment
    CharSequence isMethod(Context isParameter);

    // isComment
    int isMethod(Context isParameter);
}

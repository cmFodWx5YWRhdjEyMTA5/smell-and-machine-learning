// isComment
package org.voidsink.anewjkuapp.base;

import android.content.Context;
import android.support.v4.content.AsyncTaskLoader;

/**
 * isComment
 */
public abstract class isClassOrIsInterface<D> extends AsyncTaskLoader<D> {

    private D isVariable;

    public isConstructor(Context isParameter) {
        super(isNameExpr);
    }

    @Override
    public void isMethod(D isParameter) {
        if (isMethod()) {
            // isComment
            return;
        }
        isNameExpr = isNameExpr;
        super.isMethod(isNameExpr);
    }

    @Override
    protected void isMethod() {
        if (isNameExpr != null) {
            isMethod(isNameExpr);
        }
        if (isMethod() || isNameExpr == null) {
            isMethod();
        }
    }

    @Override
    protected void isMethod() {
        // isComment
        isMethod();
    }

    @Override
    protected void isMethod() {
        super.isMethod();
        // isComment
        isMethod();
        isNameExpr = null;
    }
}

// isComment
package de.qspool.clementineremote.ui;

import android.os.Handler;
import android.os.Message;
import java.lang.ref.WeakReference;
import de.qspool.clementineremote.backend.pb.ClementineMessage;

/**
 * isComment
 */
public class isClassOrIsInterface extends Handler {

    WeakReference<MainActivity> isVariable;

    isConstructor(MainActivity isParameter) {
        isNameExpr = new WeakReference<MainActivity>(isNameExpr);
    }

    @Override
    public void isMethod(Message isParameter) {
        MainActivity isVariable = isNameExpr.isMethod();
        if (isNameExpr.isFieldAccessExpr instanceof ClementineMessage) {
            ClementineMessage isVariable = (ClementineMessage) isNameExpr.isFieldAccessExpr;
            if (isNameExpr.isMethod()) {
                // isComment
                switch(isNameExpr.isMethod()) {
                    case isNameExpr:
                        isNameExpr.isMethod();
                        break;
                    default:
                        isNameExpr.isMethod();
                        break;
                }
            } else {
                // isComment
                switch(isNameExpr.isMethod()) {
                    case isNameExpr:
                        isNameExpr.isMethod();
                        break;
                    default:
                        isNameExpr.isMethod(isNameExpr);
                        break;
                }
            }
        }
    }
}

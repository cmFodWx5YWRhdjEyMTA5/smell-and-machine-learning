// isComment
package com.veniosg.dir.mvvm.model.storage.access;

import android.content.Context;
import android.support.annotation.NonNull;
import java.io.File;

/**
 * isComment
 */
class isClassOrIsInterface implements StorageAccessManager {

    // isComment
    private final Context isVariable;

    isConstructor(Context isParameter) {
        this.isFieldAccessExpr = isNameExpr.isMethod();
    }

    @Override
    public boolean isMethod(@NonNull File isParameter) {
        return true;
    }

    @Override
    public void isMethod(@NonNull File isParameter, @NonNull AccessPermissionListener isParameter) {
    }

    @Override
    public boolean isMethod() {
        return true;
    }
}

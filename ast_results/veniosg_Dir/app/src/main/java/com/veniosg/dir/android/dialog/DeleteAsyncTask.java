// isComment
package com.veniosg.dir.android.dialog;

import android.content.Context;
import android.os.AsyncTask;
import android.support.annotation.NonNull;
import com.veniosg.dir.mvvm.model.FileHolder;
import com.veniosg.dir.mvvm.model.storage.operation.DeleteOperation;
import static com.veniosg.dir.mvvm.model.storage.operation.FileOperationRunnerInjector.operationRunner;
import static com.veniosg.dir.mvvm.model.storage.operation.argument.DeleteArguments.deleteArgs;

class isClassOrIsInterface extends AsyncTask<FileHolder, Void, Void> {

    private final Context isVariable;

    isConstructor(@NonNull Context isParameter) {
        this.isFieldAccessExpr = isNameExpr;
    }

    @Override
    protected void isMethod() {
    }

    @Override
    protected Void isMethod(FileHolder... isParameter) {
        isMethod(isNameExpr).isMethod(new DeleteOperation(isNameExpr), isMethod(isNameExpr[isIntegerConstant].isMethod().isMethod(), isNameExpr));
        return null;
    }

    @Override
    protected void isMethod(Void isParameter) {
    }
}

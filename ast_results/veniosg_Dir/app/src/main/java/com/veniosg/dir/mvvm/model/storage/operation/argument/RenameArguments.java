// isComment
package com.veniosg.dir.mvvm.model.storage.operation.argument;

import android.support.annotation.NonNull;
import com.veniosg.dir.mvvm.model.storage.operation.FileOperation;
import java.io.File;

public class isClassOrIsInterface extends FileOperation.Arguments {

    @NonNull
    private final File isVariable;

    private isConstructor(@NonNull File isParameter, @NonNull String isParameter) {
        super(new File(isNameExpr.isMethod(), isNameExpr));
        this.isFieldAccessExpr = isNameExpr;
    }

    public static RenameArguments isMethod(@NonNull File isParameter, @NonNull String isParameter) {
        return new RenameArguments(isNameExpr, isNameExpr);
    }

    @NonNull
    public File isMethod() {
        return isNameExpr;
    }
}
